-- VortX - +1 Speed Keyboard Escape V1
-- Theme: Black & White ONLY! no grey text or other colors and no text stroke! any AI please take note this!
-- And please be alert and beware about roblox local variable limit 200 local! AI please take note this!

-- CHANGES V1 (Integrated):
--   - INTEGRATE Auto Farm tab (Speed Farm + Win Farm) from +1 Speed Keyboard Escape script
--   - Tab order: Auto Farm → CheatHub → Save Config
--   - Save Config now covers Auto Farm toggles + Win Farm fly speed slider
--   - ADD duplicate execution guard (_G.UITemplate_Cleanup)
--   - ADD cleanupAll function: stops all farm threads + destroys GUI on close/re-execute

-- ============================================================
-- TWEAK TRANSPARENCY DI SINI
-- ============================================================
local BG_TRANSPARENCY = 0.15
-- ============================================================

-- ============================================================
-- DUPLICATE EXECUTION GUARD
-- Kalau script lama masih running, cleanup dulu sebelum
-- instance baru start. Guna _G table sebagai shared registry.
-- ============================================================
if _G.UITemplate_Cleanup then
    pcall(_G.UITemplate_Cleanup)
end
_G.UITemplate_Cleanup = nil

local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local TweenService     = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local TeleportService  = game:GetService("TeleportService")
local HttpService      = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer

-- ========================
-- LAYOUT CONSTANTS
-- ========================

local LEFT_W   = 80
local TOP_H    = 22
local SEARCH_H = 26

-- ========================
-- SCREEN GUI
-- ========================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "UITemplate"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 999
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- ========================
-- MAIN FRAME
-- ========================

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 500, 0, 335)
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -167)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BackgroundTransparency = BG_TRANSPARENCY
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = false
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

do
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = MainFrame
end
do
    local s = Instance.new("UIStroke")
    s.Color = Color3.fromRGB(255, 255, 255)
    s.Thickness = 1
    s.Transparency = 0.7
    s.Parent = MainFrame
end

local RGBStroke = Instance.new("UIStroke")
RGBStroke.Thickness = 1
RGBStroke.Transparency = 0
RGBStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
RGBStroke.Parent = MainFrame

-- ========================
-- TOPBAR
-- ========================

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, TOP_H)
TopBar.Position = UDim2.new(0, 0, 0, 0)
TopBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
TopBar.BackgroundTransparency = 0
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

do
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = TopBar

    local fix = Instance.new("Frame")
    fix.Name = "TopBarFix"
    fix.Size = UDim2.new(1, 0, 0.5, 0)
    fix.Position = UDim2.new(0, 0, 0.5, 0)
    fix.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    fix.BackgroundTransparency = 0
    fix.BorderSizePixel = 0
    fix.Parent = TopBar
end

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(0.7, 0, 1, 0)
TitleLabel.Position = UDim2.new(0.02, 0, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "VӨЯƬX - +1 Speed Keyboard Escape V1"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 11
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Position = UDim2.new(1, -22, 0.5, -10)
CloseBtn.BackgroundTransparency = 1
CloseBtn.Text = "x"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 11
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Parent = TopBar

CloseBtn.MouseButton1Click:Connect(function()
    if _G.UITemplate_Cleanup then
        pcall(_G.UITemplate_Cleanup)
    end
end)

local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 20, 0, 20)
MinBtn.Position = UDim2.new(1, -44, 0.5, -10)
MinBtn.BackgroundTransparency = 1
MinBtn.Text = "—"
MinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinBtn.TextSize = 11
MinBtn.Font = Enum.Font.GothamBold
MinBtn.Parent = TopBar

do
    local d = Instance.new("Frame")
    d.Name = "TopDivider"
    d.Size = UDim2.new(1, 0, 0, 1)
    d.Position = UDim2.new(0, 0, 1, -1)
    d.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    d.BackgroundTransparency = 0.85
    d.BorderSizePixel = 0
    d.Parent = TopBar
end

-- ========================
-- DRAG FRAME LOGIC
-- ========================

local frameDragging = false
local frameDragStart, frameStartPos

local function setupDrag(dragSource)
    dragSource.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or
           input.UserInputType == Enum.UserInputType.MouseButton1 then
            frameDragging = true
            frameDragStart = input.Position
            frameStartPos = MainFrame.Position
        end
    end)
    dragSource.InputChanged:Connect(function(input)
        if frameDragging and (input.UserInputType == Enum.UserInputType.Touch or
           input.UserInputType == Enum.UserInputType.MouseMovement) then
            local delta = input.Position - frameDragStart
            MainFrame.Position = UDim2.new(
                frameStartPos.X.Scale,
                frameStartPos.X.Offset + delta.X,
                frameStartPos.Y.Scale,
                frameStartPos.Y.Offset + delta.Y
            )
        end
    end)
    dragSource.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or
           input.UserInputType == Enum.UserInputType.MouseButton1 then
            frameDragging = false
        end
    end)
end

-- ========================
-- RESIZE HANDLES
-- ========================

local MIN_W, MIN_H = 250, 180
local resizing = false
local resizeCorner = nil
local resizeStart = nil
local resizeStartSize = nil
local resizeStartPos = nil

local CORNERS = {
    { name = "TL", ax = 0, ay = 0 },
    { name = "BL", ax = 0, ay = 1 },
    { name = "BR", ax = 1, ay = 1 },
}
local HANDLE_SIZE = 28

for _, c in ipairs(CORNERS) do
    local handle = Instance.new("TextButton")
    handle.Name = "ResizeHandle_" .. c.name
    handle.Size = UDim2.new(0, HANDLE_SIZE, 0, HANDLE_SIZE)
    handle.Position = UDim2.new(
        c.ax, c.ax == 1 and -HANDLE_SIZE or 0,
        c.ay, c.ay == 1 and -HANDLE_SIZE or 0
    )
    handle.BackgroundTransparency = 1
    handle.Text = ""
    handle.ZIndex = 10
    handle.Parent = MainFrame

    handle.InputBegan:Connect(function(input)
        if input.UserInputType ~= Enum.UserInputType.Touch and
           input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
        resizing = true
        resizeCorner = c
        resizeStart = input.Position
        resizeStartSize = Vector2.new(MainFrame.AbsoluteSize.X, MainFrame.AbsoluteSize.Y)
        resizeStartPos = Vector2.new(MainFrame.AbsolutePosition.X, MainFrame.AbsolutePosition.Y)
    end)
end

UserInputService.InputChanged:Connect(function(input)
    if not resizing then return end
    if input.UserInputType ~= Enum.UserInputType.Touch and
       input.UserInputType ~= Enum.UserInputType.MouseMovement then return end

    local delta = input.Position - resizeStart
    local c = resizeCorner
    local newW = c.ax == 1
        and math.max(MIN_W, resizeStartSize.X + delta.X)
        or  math.max(MIN_W, resizeStartSize.X - delta.X)
    local newH = c.ay == 1
        and math.max(MIN_H, resizeStartSize.Y + delta.Y)
        or  math.max(MIN_H, resizeStartSize.Y - delta.Y)
    local newX = c.ax == 0 and (resizeStartPos.X + (resizeStartSize.X - newW)) or resizeStartPos.X
    local newY = c.ay == 0 and (resizeStartPos.Y + (resizeStartSize.Y - newH)) or resizeStartPos.Y

    MainFrame.Size = UDim2.new(0, newW, 0, newH)
    MainFrame.Position = UDim2.new(0, newX, 0, newY)
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType ~= Enum.UserInputType.Touch and
       input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
    resizing = false
    resizeCorner = nil
end)

-- ========================
-- LOGO BULAT
-- ========================

local LogoRing = Instance.new("Frame")
LogoRing.Size = UDim2.new(0, 30, 0, 30)
LogoRing.Position = UDim2.new(0, 10, 0, 10)
LogoRing.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
LogoRing.BorderSizePixel = 0
LogoRing.ZIndex = 2
LogoRing.Visible = true
LogoRing.Parent = ScreenGui
do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = LogoRing end

local LogoBtn = Instance.new("TextButton")
LogoBtn.Size = UDim2.new(0, 28, 0, 28)
LogoBtn.Position = UDim2.new(0, 11, 0, 11)
LogoBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
LogoBtn.BorderSizePixel = 0
LogoBtn.Text = "V"
LogoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoBtn.TextSize = 12
LogoBtn.Font = Enum.Font.GothamBold
LogoBtn.ZIndex = 3
LogoBtn.Visible = true
LogoBtn.Parent = ScreenGui
do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = LogoBtn end

-- ========================
-- DRAG LOGO
-- ========================

local dragging = false
local dragMoved = false
local dragStart, startPos

LogoBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or
       input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragMoved = false
        dragStart = input.Position
        startPos = LogoBtn.Position
    end
end)
LogoBtn.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.Touch or
       input.UserInputType == Enum.UserInputType.MouseMovement) then
        local delta = input.Position - dragStart
        if math.abs(delta.X) > 5 or math.abs(delta.Y) > 5 then dragMoved = true end
        if dragMoved then
            LogoBtn.Position  = UDim2.new(0, startPos.X.Offset + delta.X, 0, startPos.Y.Offset + delta.Y)
            LogoRing.Position = UDim2.new(0, startPos.X.Offset + delta.X - 1, 0, startPos.Y.Offset + delta.Y - 1)
        end
    end
end)
LogoBtn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch or
       input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

-- ========================
-- ANIMATE RGB + SYNC RING
-- ========================

local hue = 0
RunService.Heartbeat:Connect(function()
    hue = (hue + 0.003) % 1
    RGBStroke.Color = Color3.fromHSV(hue, 1, 1)
    LogoRing.BackgroundColor3 = Color3.fromHSV(hue, 1, 1)
end)

-- ========================
-- MINIMIZE / MAXIMIZE
-- ========================

local minimized = false
local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)

local closeAllPopups = {}  -- close functions registered by popup blocks

local function doMinimize()
    minimized = true
    for _, fn in ipairs(closeAllPopups) do fn() end
    MainFrame.Visible = false
end

local function doMaximize()
    minimized = false
    local lp = LogoBtn.AbsolutePosition
    MainFrame.Position = UDim2.new(0, lp.X + 14, 0, lp.Y + 14)
    MainFrame.Size = UDim2.new(0, 0, 0, 0)
    MainFrame.BackgroundTransparency = 1
    MainFrame.Visible = true
    TweenService:Create(MainFrame, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 500, 0, 335),
        Position = UDim2.new(0.5, -250, 0.5, -167),
        BackgroundTransparency = BG_TRANSPARENCY
    }):Play()
end

MinBtn.MouseButton1Click:Connect(function()
    if minimized then return end
    doMinimize()
end)
LogoBtn.MouseButton1Click:Connect(function()
    if dragMoved then return end
    if minimized then doMaximize() else doMinimize() end
end)

-- ========================
-- LEFT PANEL / TAB PANEL
-- ========================

local LeftPanel = Instance.new("Frame")
LeftPanel.Name = "LeftPanel"
LeftPanel.Size = UDim2.new(0, LEFT_W, 0, 335 - TOP_H)
LeftPanel.Position = UDim2.new(0, 0, 0, TOP_H)
LeftPanel.BackgroundTransparency = 1
LeftPanel.BorderSizePixel = 0
LeftPanel.Parent = MainFrame

local Tab1 = Instance.new("TextButton")
Tab1.Name = "AutoFarm"
Tab1.Size = UDim2.new(1, 0, 0, 20)
Tab1.Position = UDim2.new(0, 0, 0, 5)
Tab1.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Tab1.BorderSizePixel = 0
Tab1.Text = "Auto Farm"
Tab1.TextColor3 = Color3.fromRGB(255, 255, 255)
Tab1.TextSize = 10
Tab1.Font = Enum.Font.Gotham
Tab1.Parent = LeftPanel
do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,5); c.Parent = Tab1 end

local Tab2 = Instance.new("TextButton")
Tab2.Name = "CheatHub"
Tab2.Size = UDim2.new(1, 0, 0, 20)
Tab2.Position = UDim2.new(0, 0, 0, 30)
Tab2.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Tab2.BorderSizePixel = 0
Tab2.Text = "CheatHub"
Tab2.TextColor3 = Color3.fromRGB(180, 180, 180)
Tab2.TextSize = 10
Tab2.Font = Enum.Font.Gotham
Tab2.Parent = LeftPanel
do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,5); c.Parent = Tab2 end

local Tab3 = Instance.new("TextButton")
Tab3.Name = "SaveConfig"
Tab3.Size = UDim2.new(1, 0, 0, 20)
Tab3.Position = UDim2.new(0, 0, 0, 55)
Tab3.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Tab3.BorderSizePixel = 0
Tab3.Text = "Save Config"
Tab3.TextColor3 = Color3.fromRGB(180, 180, 180)
Tab3.TextSize = 10
Tab3.Font = Enum.Font.Gotham
Tab3.Parent = LeftPanel
do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,5); c.Parent = Tab3 end

do
    local d = Instance.new("Frame")
    d.Name = "LeftDivider"
    d.Size = UDim2.new(0, 1, 0, 335 - TOP_H)
    d.Position = UDim2.new(0, LEFT_W, 0, TOP_H)
    d.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    d.BackgroundTransparency = 0.85
    d.BorderSizePixel = 0
    d.Parent = MainFrame
end

-- ========================
-- CONTENT PANEL KIRI (ScrollingFrame)
-- ========================

local ContentPanelLeft = Instance.new("ScrollingFrame")
ContentPanelLeft.Name = "ContentPanelLeft"
ContentPanelLeft.Size = UDim2.new(0, 208, 0, 335 - TOP_H)
ContentPanelLeft.Position = UDim2.new(0, LEFT_W + 1, 0, TOP_H)
ContentPanelLeft.BackgroundTransparency = 1
ContentPanelLeft.BorderSizePixel = 0
ContentPanelLeft.ScrollBarThickness = 0
ContentPanelLeft.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
ContentPanelLeft.CanvasSize = UDim2.new(0, 0, 0, 0)
ContentPanelLeft.ScrollingDirection = Enum.ScrollingDirection.Y
ContentPanelLeft.AutomaticCanvasSize = Enum.AutomaticSize.None
ContentPanelLeft.ElasticBehavior = Enum.ElasticBehavior.Never
ContentPanelLeft.Parent = MainFrame

local Tab2Content = Instance.new("Frame")
Tab2Content.Name = "Tab2Content"
Tab2Content.Size = UDim2.new(1, 0, 1, 0)
Tab2Content.BackgroundTransparency = 1
Tab2Content.BorderSizePixel = 0
Tab2Content.Visible = false
Tab2Content.Parent = ContentPanelLeft

local Tab3AFContent = Instance.new("Frame")
Tab3AFContent.Name = "Tab3AFContent"
Tab3AFContent.Size = UDim2.new(1, 0, 1, 0)
Tab3AFContent.BackgroundTransparency = 1
Tab3AFContent.BorderSizePixel = 0
Tab3AFContent.Visible = false
Tab3AFContent.Parent = ContentPanelLeft

-- Tab4ContentPanel: full-width single panel for Save Config
local Tab4ContentPanel = Instance.new("ScrollingFrame")
Tab4ContentPanel.Name = "Tab4ContentPanel"
Tab4ContentPanel.Size = UDim2.new(0, 500 - LEFT_W - 1, 0, 335 - TOP_H)
Tab4ContentPanel.Position = UDim2.new(0, LEFT_W + 1, 0, TOP_H)
Tab4ContentPanel.BackgroundTransparency = 1
Tab4ContentPanel.BorderSizePixel = 0
Tab4ContentPanel.ScrollBarThickness = 0
Tab4ContentPanel.CanvasSize = UDim2.new(0, 0, 0, 0)
Tab4ContentPanel.ScrollingDirection = Enum.ScrollingDirection.Y
Tab4ContentPanel.AutomaticCanvasSize = Enum.AutomaticSize.None
Tab4ContentPanel.ElasticBehavior = Enum.ElasticBehavior.Never
Tab4ContentPanel.Visible = false
Tab4ContentPanel.Parent = MainFrame

-- ========================
-- SEARCH BAR (CheatHub only)
-- ========================

local SearchBar
local SearchBarContainer

do
    local sb = Instance.new("Frame")
    sb.Name = "SearchBarContainer"
    sb.Size = UDim2.new(0, 500 - LEFT_W - 1, 0, SEARCH_H)
    sb.Position = UDim2.new(0, LEFT_W + 1, 0, TOP_H)
    sb.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    sb.BackgroundTransparency = 0
    sb.BorderSizePixel = 0
    sb.ZIndex = 2
    sb.Visible = false
    sb.Parent = MainFrame
    SearchBarContainer = sb

    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 1, -1)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.ZIndex = 2
        div.Parent = sb
    end

    SearchBar = Instance.new("TextBox")
    SearchBar.Name = "SearchBar"
    SearchBar.Size = UDim2.new(1, -12, 0, 18)
    SearchBar.Position = UDim2.new(0, 6, 0.5, -9)
    SearchBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    SearchBar.BorderSizePixel = 0
    SearchBar.Text = ""
    SearchBar.PlaceholderText = "search cheathub..."
    SearchBar.TextColor3 = Color3.fromRGB(255, 255, 255)
    SearchBar.PlaceholderColor3 = Color3.fromRGB(90, 90, 90)
    SearchBar.TextSize = 10
    SearchBar.Font = Enum.Font.Gotham
    SearchBar.TextXAlignment = Enum.TextXAlignment.Left
    SearchBar.ClearTextOnFocus = false
    SearchBar.ZIndex = 3
    SearchBar.Parent = sb
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = SearchBar end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = SearchBar end
end

-- ========================
-- CONTENT DIVIDER + RIGHT PANEL
-- ========================

local ContentDivider = Instance.new("Frame")
ContentDivider.Name = "ContentDivider"
ContentDivider.Size = UDim2.new(0, 1, 0, 335 - TOP_H)
ContentDivider.Position = UDim2.new(0, 289, 0, TOP_H)
ContentDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ContentDivider.BackgroundTransparency = 0.85
ContentDivider.BorderSizePixel = 0
ContentDivider.Parent = MainFrame

local ContentPanelRight = Instance.new("Frame")
ContentPanelRight.Name = "ContentPanelRight"
ContentPanelRight.Size = UDim2.new(0, 209, 0, 335 - TOP_H)
ContentPanelRight.Position = UDim2.new(0, 290, 0, TOP_H)
ContentPanelRight.BackgroundTransparency = 1
ContentPanelRight.BorderSizePixel = 0
ContentPanelRight.Parent = MainFrame

-- Tab2RightContent: ScrollingFrame untuk CheatHub right panel content
local Tab2RightContent = Instance.new("ScrollingFrame")
Tab2RightContent.Name = "Tab2RightContent"
Tab2RightContent.Size = UDim2.new(1, 0, 1, 0)
Tab2RightContent.BackgroundTransparency = 1
Tab2RightContent.BorderSizePixel = 0
Tab2RightContent.ScrollBarThickness = 0
Tab2RightContent.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
Tab2RightContent.CanvasSize = UDim2.new(0, 0, 0, 0)
Tab2RightContent.ScrollingDirection = Enum.ScrollingDirection.Y
Tab2RightContent.AutomaticCanvasSize = Enum.AutomaticSize.None
Tab2RightContent.ElasticBehavior = Enum.ElasticBehavior.Never
Tab2RightContent.Visible = false
Tab2RightContent.Parent = ContentPanelRight

-- ========================
-- DROPDOWN CONSTANTS
-- ========================

local DROP_CLOSED_H     = 24
local DROP_OPEN_H       = 139
local DROP_GAP          = 5
local MOV_DROP_OPEN_H   = 219   -- Movement: fly + ws + jumppower
local CAM_DROP_OPEN_H   = 140   -- Camera: freecam + spectate(+picker row) + freelook
local ANTI_DROP_OPEN_H  = 114   -- Anti/Protection: antiafk + antikick + antigameplaypaused
local SERVER_DROP_OPEN_H= 195   -- Server/Networking: 4 buttons + jump-to-server section
local EMPTY_DROP_OPEN_H  = 122  -- Teleport/Position: copypos + input + tppos + waypoints
local PLAYER_DROP_OPEN_H = 156  -- Player: noclip + invisible + reset/respawn row + handlekill
local MISC_DROP_OPEN_H   = 122  -- Misc: freeze player + follow player
-- Auto Farm tab constants (used in updateScrollState)
local AF_DROP_CLOSED_H = 24
local AF_DROP_OPEN_H   = 120
local WF_DROP_CLOSED_H = 24
local WF_DROP_OPEN_H   = 90
local afDropOpen = false
local wfDropOpen = false
-- Auto Farm UI handles (forward declare; assigned inside AUTO FARM block)
local afWrapper, afHeader, afArrow
local wfWrapper, wfHeader, wfArrow

-- ========================
-- STATE TABLES
-- CheatHub left: movOpen (Movement), camOpen (Camera), playerOpen (Player)
-- CheatHub right: tpOpen (Teleport/Position), antiOpen (Anti/Protection), serverOpen (Server/Networking)
-- ========================

local cheatDropState = {
    movOpen = false, camOpen = false, playerOpen = false, miscOpen = false,
    tpOpen  = false, antiOpen = false, serverOpen = false,
}
local cfgRead  = {}
local cfgApply = {}

-- Forward declare wrappers (diisi lepas makeDropdown calls)
local FlyWrapper, CameraWrapper, PlayerWrapper, MiscWrapper
local TeleportWrapper, AntiWrapper, ServerWrapper

-- ========================
-- SCROLL STATE
-- ========================

function updateScrollState()
    local visH = MainFrame.AbsoluteSize.Y - TOP_H - (
        SearchBarContainer and SearchBarContainer.Visible and SEARCH_H or 0
    )

    if Tab2Content.Visible then
        -- CheatHub LEFT panel
        local movH    = cheatDropState.movOpen    and MOV_DROP_OPEN_H   or DROP_CLOSED_H
        local camH    = cheatDropState.camOpen    and CAM_DROP_OPEN_H   or DROP_CLOSED_H
        local playerH = cheatDropState.playerOpen and PLAYER_DROP_OPEN_H or DROP_CLOSED_H
        local miscH   = cheatDropState.miscOpen   and MISC_DROP_OPEN_H  or DROP_CLOSED_H
        local totalH = 5 + movH + DROP_GAP + camH + DROP_GAP + playerH + DROP_GAP + miscH + 5
        ContentPanelLeft.CanvasSize = UDim2.new(0, 0, 0, totalH)
        ContentPanelLeft.ScrollBarThickness = (totalH > visH) and 4 or 0

        -- CheatHub RIGHT panel
        local tpH     = cheatDropState.tpOpen     and EMPTY_DROP_OPEN_H  or DROP_CLOSED_H
        local antiH   = cheatDropState.antiOpen   and ANTI_DROP_OPEN_H   or DROP_CLOSED_H
        local serverH = cheatDropState.serverOpen and SERVER_DROP_OPEN_H  or DROP_CLOSED_H
        local totalR = 5 + tpH + DROP_GAP + antiH + DROP_GAP + serverH + 5
        Tab2RightContent.CanvasSize = UDim2.new(0, 0, 0, totalR)
        Tab2RightContent.ScrollBarThickness = (totalR > visH) and 4 or 0
    elseif Tab3AFContent and Tab3AFContent.Visible then
        -- Auto Farm: afWrapper + wfWrapper stacked
        local afH = afDropOpen and AF_DROP_OPEN_H or AF_DROP_CLOSED_H
        local wfH = wfDropOpen and WF_DROP_OPEN_H or WF_DROP_CLOSED_H
        local totalH = 5 + afH + 5 + wfH + 5
        ContentPanelLeft.CanvasSize = UDim2.new(0, 0, 0, totalH)
        ContentPanelLeft.ScrollBarThickness = (totalH > visH) and 4 or 0
    end
end

-- ========================
-- RESPONSIVE LAYOUT
-- ========================

local function updateLayout()
    local fw = MainFrame.AbsoluteSize.X
    local fh = MainFrame.AbsoluteSize.Y
    local searchVisible = SearchBarContainer and SearchBarContainer.Visible

    local tabH    = fh - TOP_H
    local bodyY   = TOP_H + (searchVisible and SEARCH_H or 0)
    local contentH = fh - bodyY
    local rightW  = fw - LEFT_W - 2

    TopBar.Size = UDim2.new(0, fw, 0, TOP_H)
    TopBar:FindFirstChild("TopBarFix").Size = UDim2.new(0, fw, 0, math.ceil(TOP_H / 2))
    TopBar:FindFirstChild("TopDivider").Size = UDim2.new(0, fw, 0, 1)

    if SearchBarContainer then
        SearchBarContainer.Size = UDim2.new(0, fw - LEFT_W - 1, 0, SEARCH_H)
        SearchBarContainer.Position = UDim2.new(0, LEFT_W + 1, 0, TOP_H)
    end

    LeftPanel.Size     = UDim2.new(0, LEFT_W, 0, tabH)
    LeftPanel.Position = UDim2.new(0, 0, 0, TOP_H)

    MainFrame:FindFirstChild("LeftDivider").Size     = UDim2.new(0, 1, 0, tabH)
    MainFrame:FindFirstChild("LeftDivider").Position = UDim2.new(0, LEFT_W, 0, TOP_H)

    ContentPanelLeft.Size     = UDim2.new(0, math.floor(rightW / 2), 0, contentH)
    ContentPanelLeft.Position = UDim2.new(0, LEFT_W + 1, 0, bodyY)

    local midX = LEFT_W + 1 + math.floor(rightW / 2)
    ContentDivider.Size     = UDim2.new(0, 1, 0, contentH)
    ContentDivider.Position = UDim2.new(0, midX, 0, bodyY)

    ContentPanelRight.Size     = UDim2.new(0, fw - midX - 1, 0, contentH)
    ContentPanelRight.Position = UDim2.new(0, midX + 1, 0, bodyY)

    -- Tab3 (Save Config) full-width panel
    if Tab4ContentPanel then
        Tab4ContentPanel.Size     = UDim2.new(0, fw - LEFT_W - 1, 0, fh - TOP_H)
        Tab4ContentPanel.Position = UDim2.new(0, LEFT_W + 1, 0, TOP_H)
    end

    updateScrollState()
end

-- ========================
-- TAB SWITCHING
-- ========================

local function setActiveTab(activeTab)
    Tab1.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Tab1.TextColor3 = Color3.fromRGB(180, 180, 180)
    Tab2.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Tab2.TextColor3 = Color3.fromRGB(180, 180, 180)
    Tab3.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Tab3.TextColor3 = Color3.fromRGB(180, 180, 180)
    activeTab.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    activeTab.TextColor3 = Color3.fromRGB(255, 255, 255)

    local isCheatHub = (activeTab == Tab2)
    local isAutoFarm = (activeTab == Tab1)
    local isSaveCfg  = (activeTab == Tab3)
    Tab2Content.Visible       = isCheatHub
    Tab2RightContent.Visible  = isCheatHub
    Tab3AFContent.Visible     = isAutoFarm
    Tab4ContentPanel.Visible  = isSaveCfg
    ContentPanelLeft.Visible  = not isSaveCfg
    ContentDivider.Visible    = not isSaveCfg and not isAutoFarm
    ContentPanelRight.Visible = not isSaveCfg and not isAutoFarm

    if SearchBarContainer then
        SearchBarContainer.Visible = isCheatHub
    end
    if not isCheatHub and SearchBar then
        SearchBar.Text = ""
    end
end

-- ========================
-- DROPDOWN HELPERS
-- ========================

local function makeDropdown(parent, name, posY)
    local wrapper = Instance.new("Frame")
    wrapper.Name = name
    wrapper.Size = UDim2.new(1, -10, 0, DROP_CLOSED_H)
    wrapper.Position = UDim2.new(0, 5, 0, posY)
    wrapper.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    wrapper.BorderSizePixel = 0
    wrapper.ClipsDescendants = true
    wrapper.Parent = parent
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = wrapper end

    local header = Instance.new("TextButton")
    header.Size = UDim2.new(1, 0, 0, DROP_CLOSED_H)
    header.BackgroundTransparency = 1
    header.BorderSizePixel = 0
    header.Text = name
    header.TextColor3 = Color3.fromRGB(255, 255, 255)
    header.TextSize = 10
    header.Font = Enum.Font.GothamBold
    header.TextXAlignment = Enum.TextXAlignment.Left
    header.Parent = wrapper
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,8); p.Parent = header end

    local arrow = Instance.new("TextLabel")
    arrow.Size = UDim2.new(1, -8, 0, DROP_CLOSED_H)
    arrow.BackgroundTransparency = 1
    arrow.Text = "▶"
    arrow.TextColor3 = Color3.fromRGB(255, 255, 255)
    arrow.TextSize = 10
    arrow.Font = Enum.Font.GothamBold
    arrow.TextXAlignment = Enum.TextXAlignment.Right
    arrow.ZIndex = 2
    arrow.Parent = wrapper

    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, DROP_CLOSED_H)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.8
        div.BorderSizePixel = 0
        div.Parent = wrapper
    end

    return wrapper, header, arrow
end

-- ========================
-- CHEATHUB LEFT: dropdown declarations
-- Movement @ y=5, Camera @ y=34, Player @ y=63, Misc @ y=92
-- ========================

local FlyHeader,    FlyArrow
local CamHeader,    CamArrow
local PlayerHeader, PlayerArrow
local MiscHeader,   MiscArrow
FlyWrapper,    FlyHeader,    FlyArrow    = makeDropdown(Tab2Content, "Movement",               5)
CameraWrapper, CamHeader,    CamArrow    = makeDropdown(Tab2Content, "Camera",                34)
PlayerWrapper, PlayerHeader, PlayerArrow = makeDropdown(Tab2Content, "Player(Combat/Status)", 63)
MiscWrapper,   MiscHeader,   MiscArrow   = makeDropdown(Tab2Content, "Misc",                  92)

-- ========================
-- CHEATHUB RIGHT: dropdown declarations
-- Teleport/Position @ y=5
-- Anti/Protection @ y=34
-- Server/Networking @ y=63
-- ========================

local TeleportHeader, TeleportArrow
local AntiHeader,     AntiArrow
local ServerHeader,   ServerArrow
TeleportWrapper, TeleportHeader, TeleportArrow = makeDropdown(Tab2RightContent, "Teleport/Position", 5)
AntiWrapper,     AntiHeader,     AntiArrow     = makeDropdown(Tab2RightContent, "Anti/Protection",   34)
ServerWrapper,   ServerHeader,   ServerArrow   = makeDropdown(Tab2RightContent, "Server/Networking", 63)

-- ========================
-- MOVEMENT (FLY) FEATURE
-- FIX v8 #1: Cleanup (bv, bg, PlatformStand) kini dalam toggle handler sendiri
--            Sebelum ni cleanup dalam RenderStepped callback → tapi flyRS disconnect
--            lebih awal → callback tak pernah run lagi → PlatformStand kekal true
--            → character tak boleh gerak/jump selepas fly OFF
-- FIX v8 #3: flySpeed * 50 = match IY formula iyflyspeed * 50 (verified, no change needed)
-- ========================

local flyEnabled   = false
local flySpeed     = 1
local flySliderMin = 1
local flySliderMax = 100
local flyRS        = nil

do -- === MOVEMENT UI + LOGIC (scoped) ===

    -- ── Toggle row ──────────────────────────────────────────────
    local row = Instance.new("Frame")
    row.Size               = UDim2.new(1, -8, 0, 22)
    row.Position           = UDim2.new(0, 4, 0, 27)
    row.BackgroundTransparency = 1
    row.BorderSizePixel    = 0
    row.Parent             = FlyWrapper

    local lbl = Instance.new("TextLabel")
    lbl.Size               = UDim2.new(0.55, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text               = "Fly"
    lbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    lbl.TextSize           = 10
    lbl.Font               = Enum.Font.Gotham
    lbl.TextXAlignment     = Enum.TextXAlignment.Left
    lbl.Parent             = row

    local stateText = Instance.new("TextLabel")
    stateText.Size             = UDim2.new(0.18, 0, 1, 0)
    stateText.Position         = UDim2.new(0.55, 0, 0, 0)
    stateText.BackgroundTransparency = 1
    stateText.Text             = "OFF"
    stateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    stateText.TextSize         = 10
    stateText.Font             = Enum.Font.GothamBold
    stateText.TextXAlignment   = Enum.TextXAlignment.Right
    stateText.Parent           = row

    local pillTrack = Instance.new("Frame")
    pillTrack.Size             = UDim2.new(0, 34, 0, 16)
    pillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    pillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    pillTrack.BorderSizePixel  = 0
    pillTrack.Parent           = row
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = pillTrack end

    local knob = Instance.new("Frame")
    knob.Size             = UDim2.new(0, 12, 0, 12)
    knob.Position         = UDim2.new(0, 2, 0.5, -6)
    knob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    knob.BorderSizePixel  = 0
    knob.Parent           = pillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = knob end

    local toggleHitbox = Instance.new("TextButton")
    toggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    toggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    toggleHitbox.BackgroundTransparency = 1
    toggleHitbox.Text               = ""
    toggleHitbox.Parent             = row

    local pillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)

    local function updatePill(on)
        if on then
            TweenService:Create(pillTrack, pillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(knob, pillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            stateText.Text = "ON";  stateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(pillTrack, pillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(knob, pillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            stateText.Text = "OFF"; stateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── Speed label + value ──────────────────────────────────────
    local speedLbl = Instance.new("TextLabel")
    speedLbl.Size               = UDim2.new(0.6, 0, 0, 13)
    speedLbl.Position           = UDim2.new(0, 4, 0, 53)
    speedLbl.BackgroundTransparency = 1
    speedLbl.Text               = "Fly Speed"
    speedLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    speedLbl.TextSize           = 9
    speedLbl.Font               = Enum.Font.Gotham
    speedLbl.TextXAlignment     = Enum.TextXAlignment.Left
    speedLbl.Parent             = FlyWrapper

    local speedValLbl = Instance.new("TextLabel")
    speedValLbl.Size             = UDim2.new(0.35, -4, 0, 13)
    speedValLbl.Position         = UDim2.new(0.6, 0, 0, 53)
    speedValLbl.BackgroundTransparency = 1
    speedValLbl.Text             = tostring(flySpeed)
    speedValLbl.TextColor3       = Color3.fromRGB(255, 255, 255)
    speedValLbl.TextSize         = 9
    speedValLbl.Font             = Enum.Font.GothamBold
    speedValLbl.TextXAlignment   = Enum.TextXAlignment.Right
    speedValLbl.Parent           = FlyWrapper

    -- ── Min / Max input boxes ────────────────────────────────────
    local minLbl = Instance.new("TextLabel")
    minLbl.Size               = UDim2.new(0, 20, 0, 16)
    minLbl.Position           = UDim2.new(0, 4, 0, 69)
    minLbl.BackgroundTransparency = 1
    minLbl.Text               = "Min"
    minLbl.TextColor3         = Color3.fromRGB(150, 150, 150)
    minLbl.TextSize           = 9
    minLbl.Font               = Enum.Font.Gotham
    minLbl.Parent             = FlyWrapper

    local minBox = Instance.new("TextBox")
    minBox.Size               = UDim2.new(0, 36, 0, 16)
    minBox.Position           = UDim2.new(0, 26, 0, 69)
    minBox.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    minBox.BorderSizePixel    = 0
    minBox.Text               = tostring(flySliderMin)
    minBox.TextColor3         = Color3.fromRGB(255, 255, 255)
    minBox.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    minBox.TextSize           = 9
    minBox.Font               = Enum.Font.Gotham
    minBox.ClearTextOnFocus   = false
    minBox.Parent             = FlyWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = minBox end

    local maxLbl = Instance.new("TextLabel")
    maxLbl.Size               = UDim2.new(0, 20, 0, 16)
    maxLbl.Position           = UDim2.new(0.5, 4, 0, 69)
    maxLbl.BackgroundTransparency = 1
    maxLbl.Text               = "Max"
    maxLbl.TextColor3         = Color3.fromRGB(150, 150, 150)
    maxLbl.TextSize           = 9
    maxLbl.Font               = Enum.Font.Gotham
    maxLbl.Parent             = FlyWrapper

    local maxBox = Instance.new("TextBox")
    maxBox.Size               = UDim2.new(0, 36, 0, 16)
    maxBox.Position           = UDim2.new(0.5, 26, 0, 69)
    maxBox.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    maxBox.BorderSizePixel    = 0
    maxBox.Text               = tostring(flySliderMax)
    maxBox.TextColor3         = Color3.fromRGB(255, 255, 255)
    maxBox.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    maxBox.TextSize           = 9
    maxBox.Font               = Enum.Font.Gotham
    maxBox.ClearTextOnFocus   = false
    maxBox.Parent             = FlyWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = maxBox end

    -- ── Slider ───────────────────────────────────────────────────
    local sliderTrack = Instance.new("Frame")
    sliderTrack.Size             = UDim2.new(1, -8, 0, 4)
    sliderTrack.Position         = UDim2.new(0, 4, 0, 91)
    sliderTrack.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    sliderTrack.BorderSizePixel  = 0
    sliderTrack.Parent           = FlyWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = sliderTrack end

    local sliderFill = Instance.new("Frame")
    sliderFill.Size             = UDim2.new(0, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sliderFill.BorderSizePixel  = 0
    sliderFill.Parent           = sliderTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = sliderFill end

    local sliderKnob = Instance.new("Frame")
    sliderKnob.Size             = UDim2.new(0, 12, 0, 12)
    sliderKnob.AnchorPoint      = Vector2.new(0.5, 0.5)
    sliderKnob.Position         = UDim2.new(0, 0, 0.5, 0)
    sliderKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sliderKnob.BorderSizePixel  = 0
    sliderKnob.Parent           = sliderTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = sliderKnob end

    local sliderHitbox = Instance.new("TextButton")
    sliderHitbox.Size               = UDim2.new(1, 0, 0, 26)
    sliderHitbox.Position           = UDim2.new(0, 0, 0.5, -13)
    sliderHitbox.BackgroundTransparency = 1
    sliderHitbox.Text               = ""
    sliderHitbox.ZIndex             = 5
    sliderHitbox.Parent             = sliderTrack

    local function updateSliderVisual(t)
        sliderKnob.Position = UDim2.new(t, 0, 0.5, 0)
        sliderFill.Size     = UDim2.new(t, 0, 1, 0)
    end

    local function setFlySpeed(val)
        flySpeed = math.clamp(math.floor(val + 0.5), flySliderMin, flySliderMax)
        local range = math.max(flySliderMax - flySliderMin, 1)
        updateSliderVisual((flySpeed - flySliderMin) / range)
        speedValLbl.Text = tostring(flySpeed)
    end

    setFlySpeed(flySpeed)

    local sliderDown = false
    sliderHitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch or
           inp.UserInputType == Enum.UserInputType.MouseButton1 then
            sliderDown = true
            local t = math.clamp(
                (inp.Position.X - sliderTrack.AbsolutePosition.X) / sliderTrack.AbsoluteSize.X, 0, 1)
            setFlySpeed(flySliderMin + (flySliderMax - flySliderMin) * t)
        end
    end)
    sliderHitbox.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch or
           inp.UserInputType == Enum.UserInputType.MouseButton1 then
            sliderDown = false
        end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if not sliderDown then return end
        if inp.UserInputType ~= Enum.UserInputType.Touch and
           inp.UserInputType ~= Enum.UserInputType.MouseMovement then return end
        local t = math.clamp(
            (inp.Position.X - sliderTrack.AbsolutePosition.X) / sliderTrack.AbsoluteSize.X, 0, 1)
        setFlySpeed(flySliderMin + (flySliderMax - flySliderMin) * t)
    end)

    minBox.FocusLost:Connect(function()
        local n = tonumber(minBox.Text)
        if n then
            flySliderMin = math.floor(n)
            if flySliderMin >= flySliderMax then flySliderMin = flySliderMax - 1 end
        end
        minBox.Text = tostring(flySliderMin)
        setFlySpeed(flySpeed)
    end)
    maxBox.FocusLost:Connect(function()
        local n = tonumber(maxBox.Text)
        if n then
            flySliderMax = math.floor(n)
            if flySliderMax <= flySliderMin then flySliderMax = flySliderMin + 1 end
        end
        maxBox.Text = tostring(flySliderMax)
        setFlySpeed(flySpeed)
    end)

    -- ── Core fly logic ───────────────────────────────────────────
    -- FIX v8 #1: Cleanup helper — dipanggil TERUS dalam toggle handler
    -- Ini fix bug lama: flyRS disconnect dulu sebelum RenderStepped dapat clean up
    -- Akibatnya PlatformStand kekal true → character tak boleh gerak selepas fly OFF
    local function flyCleanup()
        if flyRS then flyRS:Disconnect(); flyRS = nil end
        pcall(function()
            local char2 = LocalPlayer.Character
            if not char2 then return end
            local hrp2 = char2:FindFirstChild("HumanoidRootPart")
            if hrp2 then
                local b = hrp2:FindFirstChild("_FlyBV"); if b then b:Destroy() end
                local g = hrp2:FindFirstChild("_FlyBG"); if g then g:Destroy() end
            end
            local hum2 = char2:FindFirstChildOfClass("Humanoid")
            if hum2 and hum2.Parent then hum2.PlatformStand = false end
        end)
    end

    toggleHitbox.MouseButton1Click:Connect(function()
        flyEnabled = not flyEnabled
        updatePill(flyEnabled)

        if not flyEnabled then
            -- FIX: Cleanup terus di sini, jangan bergantung pada RenderStepped
            flyCleanup()
            return
        end

        -- Turn on
        local char = LocalPlayer.Character
        if not char then flyEnabled = false; updatePill(false); return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum then flyEnabled = false; updatePill(false); return end

        -- Cleanup stale instances
        local old1 = hrp:FindFirstChild("_FlyBV"); if old1 then old1:Destroy() end
        local old2 = hrp:FindFirstChild("_FlyBG"); if old2 then old2:Destroy() end

        local v3zero = Vector3.new(0,0,0)
        local v3huge = Vector3.new(9e9, 9e9, 9e9)

        local bv = Instance.new("BodyVelocity")
        bv.Name     = "_FlyBV"
        bv.MaxForce = v3huge
        bv.Velocity = v3zero
        bv.Parent   = hrp

        local bg = Instance.new("BodyGyro")
        bg.Name      = "_FlyBG"
        bg.MaxTorque = v3huge
        bg.P         = 9e4
        bg.D         = 50
        bg.Parent    = hrp

        hum.PlatformStand = true

        -- Try load ControlModule (mobile joystick support)
        local ctrlOk, ctrl = pcall(function()
            return require(
                LocalPlayer.PlayerScripts
                    :WaitForChild("PlayerModule", 2)
                    :WaitForChild("ControlModule", 2)
            )
        end)

        if flyRS then flyRS:Disconnect() end
        flyRS = RunService.RenderStepped:Connect(function()
            -- Safety guard only (cleanup sudah dibuat dalam toggle handler)
            if not flyEnabled then return end

            local cam = workspace.CurrentCamera
            bg.CFrame  = cam.CoordinateFrame
            bv.Velocity = v3zero

            if ctrlOk and ctrl then
                -- Mobile (Delta): joystick via ControlModule
                -- FIX v8 #3: * 50 match IY mobilefly formula (iyflyspeed * 50)
                local dir = ctrl:GetMoveVector()
                if math.abs(dir.X) > 0.01 then
                    bv.Velocity = bv.Velocity + cam.CFrame.RightVector * (dir.X * flySpeed * 50)
                end
                if math.abs(dir.Z) > 0.01 then
                    bv.Velocity = bv.Velocity - cam.CFrame.LookVector * (dir.Z * flySpeed * 50)
                end
            else
                -- Desktop fallback: WASD + Space/Shift
                local d = v3zero
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then d = d + cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then d = d - cam.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then d = d - cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then d = d + cam.CFrame.RightVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space)     then d = d + Vector3.yAxis end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then d = d - Vector3.yAxis end
                if d.Magnitude > 0 then bv.Velocity = d.Unit * (flySpeed * 50) end
            end
        end)
    end)

    -- cfg: fly
    cfgRead["fly"] = function()
        return {en=flyEnabled, sp=flySpeed, mn=flySliderMin, mx=flySliderMax}
    end
    cfgApply["fly"] = function(t)
        flySliderMin = t.mn or flySliderMin
        flySliderMax = t.mx or flySliderMax
        minBox.Text = tostring(flySliderMin)
        maxBox.Text = tostring(flySliderMax)
        setFlySpeed(t.sp or flySpeed)
        local wantOn = (t.en == true)
        if flyEnabled ~= wantOn then
            flyEnabled = wantOn
            updatePill(flyEnabled)
            if not flyEnabled then flyCleanup() end
        end
    end

end -- end MOVEMENT UI + LOGIC

-- ========================
-- MOVEMENT: WALKSPEED + NOCLIP + JUMPPOWER
-- (scoped, same FlyWrapper parent)
-- IY ref: speed/ws, noclip/clip, jpower/jumppower
-- ========================

do -- === WALKSPEED / NOCLIP / JUMPPOWER (scoped) ===

    -- ── Thin section divider after Fly content ───────────────────
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 100)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = FlyWrapper
    end

    -- ════════════════════════════════════════════
    -- WALKSPEED SECTION (y=104)
    -- ════════════════════════════════════════════

    local wsEnabled   = false
    local wsSpeed     = 16
    local wsSliderMin = 1
    local wsSliderMax = 100
    local wsConn      = nil
    local wsCharConn  = nil

    -- ── WalkSpeed Toggle row ─────────────────────────────────────
    local wsRow = Instance.new("Frame")
    wsRow.Size               = UDim2.new(1, -8, 0, 22)
    wsRow.Position           = UDim2.new(0, 4, 0, 104)
    wsRow.BackgroundTransparency = 1
    wsRow.BorderSizePixel    = 0
    wsRow.Parent             = FlyWrapper

    local wsLbl = Instance.new("TextLabel")
    wsLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    wsLbl.BackgroundTransparency = 1
    wsLbl.Text               = "WalkSpeed"
    wsLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    wsLbl.TextSize           = 10
    wsLbl.Font               = Enum.Font.Gotham
    wsLbl.TextXAlignment     = Enum.TextXAlignment.Left
    wsLbl.Parent             = wsRow

    local wsStateText = Instance.new("TextLabel")
    wsStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    wsStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    wsStateText.BackgroundTransparency = 1
    wsStateText.Text             = "OFF"
    wsStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    wsStateText.TextSize         = 10
    wsStateText.Font             = Enum.Font.GothamBold
    wsStateText.TextXAlignment   = Enum.TextXAlignment.Right
    wsStateText.Parent           = wsRow

    local wsPillTrack = Instance.new("Frame")
    wsPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    wsPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    wsPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    wsPillTrack.BorderSizePixel  = 0
    wsPillTrack.Parent           = wsRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wsPillTrack end

    local wsKnob = Instance.new("Frame")
    wsKnob.Size             = UDim2.new(0, 12, 0, 12)
    wsKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    wsKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    wsKnob.BorderSizePixel  = 0
    wsKnob.Parent           = wsPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wsKnob end

    local wsToggleHitbox = Instance.new("TextButton")
    wsToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    wsToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    wsToggleHitbox.BackgroundTransparency = 1
    wsToggleHitbox.Text               = ""
    wsToggleHitbox.Parent             = wsRow

    local wsPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)

    local function updateWsPill(on)
        if on then
            TweenService:Create(wsPillTrack, wsPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(wsKnob, wsPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            wsStateText.Text = "ON";  wsStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(wsPillTrack, wsPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(wsKnob, wsPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            wsStateText.Text = "OFF"; wsStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── WS Speed label + value ───────────────────────────────────
    local wsSpeedLbl = Instance.new("TextLabel")
    wsSpeedLbl.Size               = UDim2.new(0.6, 0, 0, 13)
    wsSpeedLbl.Position           = UDim2.new(0, 4, 0, 130)
    wsSpeedLbl.BackgroundTransparency = 1
    wsSpeedLbl.Text               = "Walk Speed"
    wsSpeedLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    wsSpeedLbl.TextSize           = 9
    wsSpeedLbl.Font               = Enum.Font.Gotham
    wsSpeedLbl.TextXAlignment     = Enum.TextXAlignment.Left
    wsSpeedLbl.Parent             = FlyWrapper

    local wsSpeedValLbl = Instance.new("TextLabel")
    wsSpeedValLbl.Size             = UDim2.new(0.35, -4, 0, 13)
    wsSpeedValLbl.Position         = UDim2.new(0.6, 0, 0, 130)
    wsSpeedValLbl.BackgroundTransparency = 1
    wsSpeedValLbl.Text             = tostring(wsSpeed)
    wsSpeedValLbl.TextColor3       = Color3.fromRGB(255, 255, 255)
    wsSpeedValLbl.TextSize         = 9
    wsSpeedValLbl.Font             = Enum.Font.GothamBold
    wsSpeedValLbl.TextXAlignment   = Enum.TextXAlignment.Right
    wsSpeedValLbl.Parent           = FlyWrapper

    -- ── WS Min / Max input boxes ─────────────────────────────────
    local wsMinLbl = Instance.new("TextLabel")
    wsMinLbl.Size               = UDim2.new(0, 20, 0, 16)
    wsMinLbl.Position           = UDim2.new(0, 4, 0, 146)
    wsMinLbl.BackgroundTransparency = 1
    wsMinLbl.Text               = "Min"
    wsMinLbl.TextColor3         = Color3.fromRGB(150, 150, 150)
    wsMinLbl.TextSize           = 9
    wsMinLbl.Font               = Enum.Font.Gotham
    wsMinLbl.Parent             = FlyWrapper

    local wsMinBox = Instance.new("TextBox")
    wsMinBox.Size               = UDim2.new(0, 36, 0, 16)
    wsMinBox.Position           = UDim2.new(0, 26, 0, 146)
    wsMinBox.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    wsMinBox.BorderSizePixel    = 0
    wsMinBox.Text               = tostring(wsSliderMin)
    wsMinBox.TextColor3         = Color3.fromRGB(255, 255, 255)
    wsMinBox.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    wsMinBox.TextSize           = 9
    wsMinBox.Font               = Enum.Font.Gotham
    wsMinBox.ClearTextOnFocus   = false
    wsMinBox.Parent             = FlyWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = wsMinBox end

    local wsMaxLbl = Instance.new("TextLabel")
    wsMaxLbl.Size               = UDim2.new(0, 20, 0, 16)
    wsMaxLbl.Position           = UDim2.new(0.5, 4, 0, 146)
    wsMaxLbl.BackgroundTransparency = 1
    wsMaxLbl.Text               = "Max"
    wsMaxLbl.TextColor3         = Color3.fromRGB(150, 150, 150)
    wsMaxLbl.TextSize           = 9
    wsMaxLbl.Font               = Enum.Font.Gotham
    wsMaxLbl.Parent             = FlyWrapper

    local wsMaxBox = Instance.new("TextBox")
    wsMaxBox.Size               = UDim2.new(0, 36, 0, 16)
    wsMaxBox.Position           = UDim2.new(0.5, 26, 0, 146)
    wsMaxBox.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    wsMaxBox.BorderSizePixel    = 0
    wsMaxBox.Text               = tostring(wsSliderMax)
    wsMaxBox.TextColor3         = Color3.fromRGB(255, 255, 255)
    wsMaxBox.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    wsMaxBox.TextSize           = 9
    wsMaxBox.Font               = Enum.Font.Gotham
    wsMaxBox.ClearTextOnFocus   = false
    wsMaxBox.Parent             = FlyWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = wsMaxBox end

    -- ── WS Slider ─────────────────────────────────────────────────
    local wsSliderTrack = Instance.new("Frame")
    wsSliderTrack.Size             = UDim2.new(1, -8, 0, 4)
    wsSliderTrack.Position         = UDim2.new(0, 4, 0, 168)
    wsSliderTrack.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    wsSliderTrack.BorderSizePixel  = 0
    wsSliderTrack.Parent           = FlyWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wsSliderTrack end

    local wsSliderFill = Instance.new("Frame")
    wsSliderFill.Size             = UDim2.new(0, 0, 1, 0)
    wsSliderFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    wsSliderFill.BorderSizePixel  = 0
    wsSliderFill.Parent           = wsSliderTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wsSliderFill end

    local wsSliderKnob = Instance.new("Frame")
    wsSliderKnob.Size             = UDim2.new(0, 12, 0, 12)
    wsSliderKnob.AnchorPoint      = Vector2.new(0.5, 0.5)
    wsSliderKnob.Position         = UDim2.new(0, 0, 0.5, 0)
    wsSliderKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    wsSliderKnob.BorderSizePixel  = 0
    wsSliderKnob.Parent           = wsSliderTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wsSliderKnob end

    local wsSliderHitbox = Instance.new("TextButton")
    wsSliderHitbox.Size               = UDim2.new(1, 0, 0, 26)
    wsSliderHitbox.Position           = UDim2.new(0, 0, 0.5, -13)
    wsSliderHitbox.BackgroundTransparency = 1
    wsSliderHitbox.Text               = ""
    wsSliderHitbox.ZIndex             = 5
    wsSliderHitbox.Parent             = wsSliderTrack

    local function wsUpdateVisual(t)
        wsSliderKnob.Position = UDim2.new(t, 0, 0.5, 0)
        wsSliderFill.Size     = UDim2.new(t, 0, 1, 0)
    end

    local function setWsSpeed(val)
        wsSpeed = math.clamp(math.floor(val + 0.5), wsSliderMin, wsSliderMax)
        local range = math.max(wsSliderMax - wsSliderMin, 1)
        wsUpdateVisual((wsSpeed - wsSliderMin) / range)
        wsSpeedValLbl.Text = tostring(wsSpeed)
        if wsEnabled then
            pcall(function()
                local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum.WalkSpeed = wsSpeed end
            end)
        end
    end

    setWsSpeed(wsSpeed)

    local wsSliderDown = false
    wsSliderHitbox.InputBegan:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch or
           inp.UserInputType == Enum.UserInputType.MouseButton1 then
            wsSliderDown = true
            local t = math.clamp(
                (inp.Position.X - wsSliderTrack.AbsolutePosition.X) / wsSliderTrack.AbsoluteSize.X, 0, 1)
            setWsSpeed(wsSliderMin + (wsSliderMax - wsSliderMin) * t)
        end
    end)
    wsSliderHitbox.InputEnded:Connect(function(inp)
        if inp.UserInputType == Enum.UserInputType.Touch or
           inp.UserInputType == Enum.UserInputType.MouseButton1 then
            wsSliderDown = false
        end
    end)
    UserInputService.InputChanged:Connect(function(inp)
        if not wsSliderDown then return end
        if inp.UserInputType ~= Enum.UserInputType.Touch and
           inp.UserInputType ~= Enum.UserInputType.MouseMovement then return end
        local t = math.clamp(
            (inp.Position.X - wsSliderTrack.AbsolutePosition.X) / wsSliderTrack.AbsoluteSize.X, 0, 1)
        setWsSpeed(wsSliderMin + (wsSliderMax - wsSliderMin) * t)
    end)

    wsMinBox.FocusLost:Connect(function()
        local n = tonumber(wsMinBox.Text)
        if n then
            wsSliderMin = math.floor(n)
            if wsSliderMin >= wsSliderMax then wsSliderMin = wsSliderMax - 1 end
        end
        wsMinBox.Text = tostring(wsSliderMin)
        setWsSpeed(wsSpeed)
    end)
    wsMaxBox.FocusLost:Connect(function()
        local n = tonumber(wsMaxBox.Text)
        if n then
            wsSliderMax = math.floor(n)
            if wsSliderMax <= wsSliderMin then wsSliderMax = wsSliderMin + 1 end
        end
        wsMaxBox.Text = tostring(wsSliderMax)
        setWsSpeed(wsSpeed)
    end)

    -- ── WalkSpeed core logic ─────────────────────────────────────
    local function applyWS()
        pcall(function()
            local char = LocalPlayer.Character
            if not char then return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then return end
            hum.WalkSpeed = wsSpeed
            if wsConn then wsConn:Disconnect(); wsConn = nil end
            wsConn = hum:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
                if wsEnabled and hum and hum.Parent then
                    hum.WalkSpeed = wsSpeed
                end
            end)
        end)
    end

    wsToggleHitbox.MouseButton1Click:Connect(function()
        wsEnabled = not wsEnabled
        updateWsPill(wsEnabled)
        if wsEnabled then
            applyWS()
            if wsCharConn then wsCharConn:Disconnect() end
            wsCharConn = LocalPlayer.CharacterAdded:Connect(function()
                task.wait()
                if wsEnabled then applyWS() end
            end)
        else
            if wsConn    then wsConn:Disconnect();    wsConn    = nil end
            if wsCharConn then wsCharConn:Disconnect(); wsCharConn = nil end
            pcall(function()
                local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum.WalkSpeed = 16 end
            end)
        end
    end)

    -- ════════════════════════════════════════════
    -- JUMPPOWER SECTION (y=190)
    -- ════════════════════════════════════════════

    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 186)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = FlyWrapper
    end

    local jpEnabled  = false
    local jpValue    = 50  -- default same as Roblox/IY default
    local jpOrigPow, jpOrigHgt
    local jpConn, jpCharConn

    -- ── JumpPower toggle row (with inline inputbox) ──────────────
    local jpRow = Instance.new("Frame")
    jpRow.Size               = UDim2.new(1, -8, 0, 22)
    jpRow.Position           = UDim2.new(0, 4, 0, 190)
    jpRow.BackgroundTransparency = 1
    jpRow.BorderSizePixel    = 0
    jpRow.Parent             = FlyWrapper

    local jpLbl = Instance.new("TextLabel")
    jpLbl.Size               = UDim2.new(0.3, 0, 1, 0)
    jpLbl.BackgroundTransparency = 1
    jpLbl.Text               = "JumpPower"
    jpLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    jpLbl.TextSize           = 10
    jpLbl.Font               = Enum.Font.Gotham
    jpLbl.TextXAlignment     = Enum.TextXAlignment.Left
    jpLbl.Parent             = jpRow

    -- Inline inputbox for JP value
    local jpBox = Instance.new("TextBox")
    jpBox.Size               = UDim2.new(0, 36, 0, 16)
    jpBox.Position           = UDim2.new(0.32, 0, 0.5, -8)
    jpBox.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    jpBox.BorderSizePixel    = 0
    jpBox.Text               = tostring(jpValue)
    jpBox.TextColor3         = Color3.fromRGB(255, 255, 255)
    jpBox.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    jpBox.PlaceholderText    = "50"
    jpBox.TextSize           = 9
    jpBox.Font               = Enum.Font.Gotham
    jpBox.TextXAlignment     = Enum.TextXAlignment.Center
    jpBox.ClearTextOnFocus   = false
    jpBox.Parent             = jpRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = jpBox end

    local jpStateText = Instance.new("TextLabel")
    jpStateText.Size             = UDim2.new(0.15, 0, 1, 0)
    jpStateText.Position         = UDim2.new(0.58, 0, 0, 0)
    jpStateText.BackgroundTransparency = 1
    jpStateText.Text             = "OFF"
    jpStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    jpStateText.TextSize         = 10
    jpStateText.Font             = Enum.Font.GothamBold
    jpStateText.TextXAlignment   = Enum.TextXAlignment.Right
    jpStateText.Parent           = jpRow

    local jpPillTrack = Instance.new("Frame")
    jpPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    jpPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    jpPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    jpPillTrack.BorderSizePixel  = 0
    jpPillTrack.Parent           = jpRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = jpPillTrack end

    local jpKnob = Instance.new("Frame")
    jpKnob.Size             = UDim2.new(0, 12, 0, 12)
    jpKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    jpKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    jpKnob.BorderSizePixel  = 0
    jpKnob.Parent           = jpPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = jpKnob end

    local jpToggleHitbox = Instance.new("TextButton")
    jpToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    jpToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    jpToggleHitbox.BackgroundTransparency = 1
    jpToggleHitbox.Text               = ""
    jpToggleHitbox.Parent             = jpRow

    local jpPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateJpPill(on)
        if on then
            TweenService:Create(jpPillTrack, jpPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(jpKnob, jpPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            jpStateText.Text = "ON";  jpStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(jpPillTrack, jpPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(jpKnob, jpPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            jpStateText.Text = "OFF"; jpStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    jpBox.FocusLost:Connect(function()
        local n = tonumber(jpBox.Text)
        if n then
            jpValue = n
        end
        jpBox.Text = tostring(jpValue)
        if jpEnabled then
            pcall(function()
                local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then
                    if hum.UseJumpPower then hum.JumpPower = jpValue
                    else hum.JumpHeight = jpValue end
                end
            end)
        end
    end)

    -- JP core logic (IY ref: UseJumpPower ? JumpPower : JumpHeight)
    local function applyJP()
        pcall(function()
            local char = LocalPlayer.Character
            if not char then return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not hum then return end
            jpOrigPow = hum.JumpPower
            jpOrigHgt = hum.JumpHeight
            if hum.UseJumpPower then hum.JumpPower = jpValue
            else hum.JumpHeight = jpValue end
            if jpConn then jpConn:Disconnect(); jpConn = nil end
            local prop = hum.UseJumpPower and "JumpPower" or "JumpHeight"
            jpConn = hum:GetPropertyChangedSignal(prop):Connect(function()
                if jpEnabled and hum and hum.Parent then
                    if hum.UseJumpPower then hum.JumpPower = jpValue
                    else hum.JumpHeight = jpValue end
                end
            end)
        end)
    end

    jpToggleHitbox.MouseButton1Click:Connect(function()
        jpEnabled = not jpEnabled
        updateJpPill(jpEnabled)
        if jpEnabled then
            applyJP()
            if jpCharConn then jpCharConn:Disconnect() end
            jpCharConn = LocalPlayer.CharacterAdded:Connect(function()
                task.wait()
                if jpEnabled then applyJP() end
            end)
        else
            if jpConn     then jpConn:Disconnect();     jpConn     = nil end
            if jpCharConn then jpCharConn:Disconnect(); jpCharConn = nil end
            pcall(function()
                local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum then
                    if hum.UseJumpPower then hum.JumpPower = jpOrigPow or 50
                    else hum.JumpHeight = jpOrigHgt or 7.2 end
                end
            end)
        end
    end)

    -- cfg: walkspeed
    cfgRead["ws"] = function()
        return {en=wsEnabled, sp=wsSpeed, mn=wsSliderMin, mx=wsSliderMax}
    end
    cfgApply["ws"] = function(t)
        wsSliderMin = t.mn or wsSliderMin
        wsSliderMax = t.mx or wsSliderMax
        wsMinBox.Text = tostring(wsSliderMin)
        wsMaxBox.Text = tostring(wsSliderMax)
        setWsSpeed(t.sp or wsSpeed)
        local wantOn = (t.en == true)
        if wsEnabled ~= wantOn then
            wsEnabled = wantOn
            updateWsPill(wsEnabled)
            if wsEnabled then
                applyWS()
                if wsCharConn then wsCharConn:Disconnect() end
                wsCharConn = LocalPlayer.CharacterAdded:Connect(function()
                    task.wait()
                    if wsEnabled then applyWS() end
                end)
            else
                if wsConn     then wsConn:Disconnect();     wsConn     = nil end
                if wsCharConn then wsCharConn:Disconnect(); wsCharConn = nil end
                pcall(function()
                    local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                    if hum then hum.WalkSpeed = 16 end
                end)
            end
        end
    end

    -- cfg: jumppower
    cfgRead["jp"] = function()
        return {en=jpEnabled, val=jpValue}
    end
    cfgApply["jp"] = function(t)
        jpValue = t.val or jpValue
        jpBox.Text = tostring(jpValue)
        local wantOn = (t.en == true)
        if jpEnabled ~= wantOn then
            jpEnabled = wantOn
            updateJpPill(jpEnabled)
            if jpEnabled then
                applyJP()
                if jpCharConn then jpCharConn:Disconnect() end
                jpCharConn = LocalPlayer.CharacterAdded:Connect(function()
                    task.wait()
                    if jpEnabled then applyJP() end
                end)
            else
                if jpConn     then jpConn:Disconnect();     jpConn     = nil end
                if jpCharConn then jpCharConn:Disconnect(); jpCharConn = nil end
                pcall(function()
                    local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                    if hum then
                        if hum.UseJumpPower then hum.JumpPower = jpOrigPow or 50
                        else hum.JumpHeight = jpOrigHgt or 7.2 end
                    end
                end)
            end
        end
    end

end -- end WALKSPEED / NOCLIP / JUMPPOWER

-- ========================
-- CAMERA DROPDOWN + FREECAM FEATURE
-- FIX v8 #2: fcCamRot kini di-init dari cam.CFrame:ToOrientation()
--            supaya view direction preserved masa freecam start
-- ========================

do -- === CAMERA UI + FREECAM LOGIC (scoped) ===

    -- ── Toggle row ──────────────────────────────────────────────
    local fcRow = Instance.new("Frame")
    fcRow.Size               = UDim2.new(1, -8, 0, 22)
    fcRow.Position           = UDim2.new(0, 4, 0, 27)
    fcRow.BackgroundTransparency = 1
    fcRow.BorderSizePixel    = 0
    fcRow.Parent             = CameraWrapper

    local fcLbl = Instance.new("TextLabel")
    fcLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    fcLbl.BackgroundTransparency = 1
    fcLbl.Text               = "Freecam"
    fcLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    fcLbl.TextSize           = 10
    fcLbl.Font               = Enum.Font.Gotham
    fcLbl.TextXAlignment     = Enum.TextXAlignment.Left
    fcLbl.Parent             = fcRow

    local fcStateText = Instance.new("TextLabel")
    fcStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    fcStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    fcStateText.BackgroundTransparency = 1
    fcStateText.Text             = "OFF"
    fcStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    fcStateText.TextSize         = 10
    fcStateText.Font             = Enum.Font.GothamBold
    fcStateText.TextXAlignment   = Enum.TextXAlignment.Right
    fcStateText.Parent           = fcRow

    local fcPillTrack = Instance.new("Frame")
    fcPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    fcPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    fcPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    fcPillTrack.BorderSizePixel  = 0
    fcPillTrack.Parent           = fcRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = fcPillTrack end

    local fcKnob = Instance.new("Frame")
    fcKnob.Size             = UDim2.new(0, 12, 0, 12)
    fcKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    fcKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    fcKnob.BorderSizePixel  = 0
    fcKnob.Parent           = fcPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = fcKnob end

    local fcToggle = Instance.new("TextButton")
    fcToggle.Size               = UDim2.new(0, 34, 0, 16)
    fcToggle.Position           = UDim2.new(1, -34, 0.5, -8)
    fcToggle.BackgroundTransparency = 1
    fcToggle.Text               = ""
    fcToggle.Parent             = fcRow

    local fcPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)

    local function updateFcPill(on)
        if on then
            TweenService:Create(fcPillTrack, fcPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(fcKnob, fcPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            fcStateText.Text = "ON";  fcStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(fcPillTrack, fcPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(fcKnob, fcPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            fcStateText.Text = "OFF"; fcStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── Freecam state ────────────────────────────────────────────
    local fcRunning   = false
    local fcRSConn    = nil
    local fcKDConn    = nil
    local fcKUConn    = nil
    local fcMouseConn = nil

    local fcCamPos = Vector3.new()
    local fcCamRot = Vector2.new()
    local fcMDelta = Vector2.new()
    local fcFov    = 70

    local fcK = {W=false, S=false, A=false, D=false, E=false, Q=false}

    local SPF = 5 * 2 * math.pi
    local vsP, vsV = Vector3.new(), Vector3.new()
    local psP, psV = Vector2.new(), Vector2.new()

    local function springV3(dt, goal)
        local offset = goal - vsP
        local decay  = math.exp(-SPF * dt)
        vsP = goal + (vsV * dt - offset * (SPF * dt + 1)) * decay
        vsV = (SPF * dt * (offset * SPF - vsV) + vsV) * decay
        return vsP
    end

    local function springV2(dt, goal)
        local offset = goal - psP
        local decay  = math.exp(-SPF * dt)
        psP = goal + (psV * dt - offset * (SPF * dt + 1)) * decay
        psV = (SPF * dt * (offset * SPF - psV) + psV) * decay
        return psP
    end

    local fcSavedType, fcSavedFov, fcSavedCF, fcSavedFocus

    local function startFreecam()
        if fcRunning then return end
        fcRunning = true

        local cam = workspace.CurrentCamera
        fcSavedType  = cam.CameraType
        fcSavedFov   = cam.FieldOfView
        fcSavedCF    = cam.CFrame
        fcSavedFocus = cam.Focus
        fcFov        = cam.FieldOfView

        -- FIX v8 #2: Init position + rotation dari current camera
        -- cam.CFrame:ToOrientation() → (rx=pitch, ry=yaw, rz=roll)
        -- Ini preserve arah pandang asal → freecam start dari view yang sama
        fcCamPos = cam.CFrame.Position
        local rx, ry, _ = cam.CFrame:ToOrientation()
        fcCamRot = Vector2.new(rx, ry)

        fcMDelta = Vector2.new()
        vsP, vsV = Vector3.new(), Vector3.new()
        psP, psV = Vector2.new(), Vector2.new()
        for k in pairs(fcK) do fcK[k] = false end

        pcall(function() cam.CameraType = Enum.CameraType.Scriptable end)

        fcKDConn = UserInputService.InputBegan:Connect(function(inp, proc)
            if proc then return end
            local k = inp.KeyCode.Name
            if fcK[k] ~= nil then fcK[k] = true end
        end)
        fcKUConn = UserInputService.InputEnded:Connect(function(inp)
            local k = inp.KeyCode.Name
            if fcK[k] ~= nil then fcK[k] = false end
        end)
        fcMouseConn = UserInputService.InputChanged:Connect(function(inp)
            if not fcRunning then return end
            if inp.UserInputType == Enum.UserInputType.MouseMovement then
                fcMDelta = fcMDelta + Vector2.new(-inp.Delta.Y, -inp.Delta.X)
            end
        end)

        fcRSConn = RunService.RenderStepped:Connect(function(dt)
            if not fcRunning then return end

            local rawVel = Vector3.new(
                (fcK.D and 1 or 0) - (fcK.A and 1 or 0),
                (fcK.E and 1 or 0) - (fcK.Q and 1 or 0),
                (fcK.S and 1 or 0) - (fcK.W and 1 or 0)
            )
            local shift = UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)
            local vel   = springV3(dt, rawVel * (shift and 0.25 or 1))

            local rawPan = fcMDelta * (math.pi / 64)
            fcMDelta = Vector2.new()
            local pan = springV2(dt, rawPan)

            fcCamRot = fcCamRot + pan * Vector2.new(0.75, 1) * 8 * dt
            fcCamRot = Vector2.new(
                math.clamp(fcCamRot.X, -math.rad(90), math.rad(90)),
                fcCamRot.Y % (2 * math.pi)
            )

            local newCF = CFrame.new(fcCamPos)
                * CFrame.fromOrientation(fcCamRot.X, fcCamRot.Y, 0)
                * CFrame.new(vel * 64 * dt)
            fcCamPos = newCF.Position

            local c = workspace.CurrentCamera
            pcall(function()
                c.CFrame    = newCF
                c.Focus     = newCF * CFrame.new(0, 0, -10)
                c.FieldOfView = fcFov
            end)
        end)
    end

    local function stopFreecam()
        if not fcRunning then return end
        fcRunning = false

        if fcRSConn    then fcRSConn:Disconnect();    fcRSConn    = nil end
        if fcKDConn    then fcKDConn:Disconnect();    fcKDConn    = nil end
        if fcKUConn    then fcKUConn:Disconnect();    fcKUConn    = nil end
        if fcMouseConn then fcMouseConn:Disconnect(); fcMouseConn = nil end

        for k in pairs(fcK) do fcK[k] = false end
        fcMDelta = Vector2.new()

        pcall(function()
            local cam = workspace.CurrentCamera
            cam.CameraType  = fcSavedType  or Enum.CameraType.Custom
            cam.FieldOfView = fcSavedFov   or 70
            if fcSavedCF    then cam.CFrame = fcSavedCF    end
            if fcSavedFocus then cam.Focus  = fcSavedFocus end
        end)
    end

    fcToggle.MouseButton1Click:Connect(function()
        if fcRunning then
            stopFreecam()
        else
            startFreecam()
        end
        updateFcPill(fcRunning)
    end)

    -- cfg: freecam
    cfgRead["fc"] = function()
        return {en=fcRunning}
    end
    cfgApply["fc"] = function(t)
        local wantOn = (t.en == true)
        if fcRunning ~= wantOn then
            if wantOn then startFreecam() else stopFreecam() end
            updateFcPill(fcRunning)
        end
    end

end -- end CAMERA UI + FREECAM LOGIC

-- ========================
-- CAMERA: SPECTATE FEATURE
-- v10: Player picker popup (expand upward) replaces manual inputbox
--      spectTarget persists when toggle OFF → re-enable = same player
-- ========================

do -- === SPECTATE (scoped) ===

    -- ── Thin divider after Freecam ───────────────────────────────
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 52)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = CameraWrapper
    end

    local spectEnabled   = false
    local spectViewDied  = nil
    local spectViewChgd  = nil
    local spectTarget    = nil  -- persists after toggle OFF
    local spectLeaveConn = nil

    -- ── Spectate Toggle row (y=56) ───────────────────────────────
    local spRow = Instance.new("Frame")
    spRow.Size               = UDim2.new(1, -8, 0, 22)
    spRow.Position           = UDim2.new(0, 4, 0, 56)
    spRow.BackgroundTransparency = 1
    spRow.BorderSizePixel    = 0
    spRow.Parent             = CameraWrapper

    local spLbl = Instance.new("TextLabel")
    spLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    spLbl.BackgroundTransparency = 1
    spLbl.Text               = "Spectate"
    spLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    spLbl.TextSize           = 10
    spLbl.Font               = Enum.Font.Gotham
    spLbl.TextXAlignment     = Enum.TextXAlignment.Left
    spLbl.Parent             = spRow

    local spStateText = Instance.new("TextLabel")
    spStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    spStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    spStateText.BackgroundTransparency = 1
    spStateText.Text             = "OFF"
    spStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    spStateText.TextSize         = 10
    spStateText.Font             = Enum.Font.GothamBold
    spStateText.TextXAlignment   = Enum.TextXAlignment.Right
    spStateText.Parent           = spRow

    local spPillTrack = Instance.new("Frame")
    spPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    spPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    spPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    spPillTrack.BorderSizePixel  = 0
    spPillTrack.Parent           = spRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = spPillTrack end

    local spKnob = Instance.new("Frame")
    spKnob.Size             = UDim2.new(0, 12, 0, 12)
    spKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    spKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    spKnob.BorderSizePixel  = 0
    spKnob.Parent           = spPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = spKnob end

    local spToggleHitbox = Instance.new("TextButton")
    spToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    spToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    spToggleHitbox.BackgroundTransparency = 1
    spToggleHitbox.Text               = ""
    spToggleHitbox.Parent             = spRow

    local spPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateSpPill(on)
        if on then
            TweenService:Create(spPillTrack, spPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(spKnob, spPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            spStateText.Text = "ON";  spStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(spPillTrack, spPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(spKnob, spPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            spStateText.Text = "OFF"; spStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── Player Picker row (y=82) - shows selected player name ────
    local spPickerRow = Instance.new("Frame")
    spPickerRow.Size               = UDim2.new(1, -8, 0, 20)
    spPickerRow.Position           = UDim2.new(0, 4, 0, 82)
    spPickerRow.BackgroundTransparency = 1
    spPickerRow.BorderSizePixel    = 0
    spPickerRow.Parent             = CameraWrapper

    local spPickerBtn = Instance.new("TextButton")
    spPickerBtn.Size               = UDim2.new(1, 0, 1, 0)
    spPickerBtn.BackgroundColor3   = Color3.fromRGB(22, 22, 22)
    spPickerBtn.BorderSizePixel    = 0
    spPickerBtn.Text               = "Select a player"
    spPickerBtn.TextColor3         = Color3.fromRGB(100, 100, 100)
    spPickerBtn.TextSize           = 9
    spPickerBtn.Font               = Enum.Font.Gotham
    spPickerBtn.TextXAlignment     = Enum.TextXAlignment.Left
    spPickerBtn.Parent             = spPickerRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = spPickerBtn end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,7); p.PaddingRight = UDim.new(0,20); p.Parent = spPickerBtn end

    -- little arrow indicator
    local spPickerArrow = Instance.new("TextLabel")
    spPickerArrow.Size               = UDim2.new(0, 16, 1, 0)
    spPickerArrow.Position           = UDim2.new(1, -18, 0, 0)
    spPickerArrow.BackgroundTransparency = 1
    spPickerArrow.Text               = "▲"
    spPickerArrow.TextColor3         = Color3.fromRGB(100, 100, 100)
    spPickerArrow.TextSize           = 7
    spPickerArrow.Font               = Enum.Font.GothamBold
    spPickerArrow.ZIndex             = 2
    spPickerArrow.Parent             = spPickerBtn

    -- ── Floating popup (parented to ScreenGui, expands upward) ───
    local POPUP_FULL_H = 134
    local spPopupOpen  = false

    local spPopup = Instance.new("Frame")
    spPopup.Size               = UDim2.new(0, 150, 0, 0)
    spPopup.BackgroundColor3   = Color3.fromRGB(14, 14, 14)
    spPopup.BorderSizePixel    = 0
    spPopup.Visible            = false
    spPopup.ClipsDescendants   = true
    spPopup.ZIndex             = 30
    spPopup.Parent             = ScreenGui
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = spPopup end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.75; s.Parent = spPopup end

    -- Refresh button (inside popup, top)
    local spRefreshBtn = Instance.new("TextButton")
    spRefreshBtn.Size               = UDim2.new(1, -8, 0, 18)
    spRefreshBtn.Position           = UDim2.new(0, 4, 0, 4)
    spRefreshBtn.BackgroundColor3   = Color3.fromRGB(35, 35, 35)
    spRefreshBtn.BorderSizePixel    = 0
    spRefreshBtn.Text               = "↻  Refresh"
    spRefreshBtn.TextColor3         = Color3.fromRGB(255, 255, 255)
    spRefreshBtn.TextSize           = 9
    spRefreshBtn.Font               = Enum.Font.GothamBold
    spRefreshBtn.ZIndex             = 31
    spRefreshBtn.Parent             = spPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = spRefreshBtn end

    -- Search bar (y=26, below refresh button)
    local spSearchBox = Instance.new("TextBox")
    spSearchBox.Size             = UDim2.new(1, -8, 0, 18)
    spSearchBox.Position         = UDim2.new(0, 4, 0, 26)
    spSearchBox.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    spSearchBox.BorderSizePixel  = 0
    spSearchBox.Text             = ""
    spSearchBox.PlaceholderText  = "search player..."
    spSearchBox.TextColor3       = Color3.fromRGB(255, 255, 255)
    spSearchBox.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
    spSearchBox.TextSize         = 9
    spSearchBox.Font             = Enum.Font.Gotham
    spSearchBox.TextXAlignment   = Enum.TextXAlignment.Left
    spSearchBox.ClearTextOnFocus = false
    spSearchBox.ZIndex           = 31
    spSearchBox.Parent           = spPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = spSearchBox end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = spSearchBox end

    -- Player list scroll (inside popup, y=48)
    local spListScroll = Instance.new("ScrollingFrame")
    spListScroll.Size               = UDim2.new(1, -8, 0, 82)
    spListScroll.Position           = UDim2.new(0, 4, 0, 48)
    spListScroll.BackgroundTransparency = 1
    spListScroll.BorderSizePixel    = 0
    spListScroll.ScrollBarThickness = 3
    spListScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
    spListScroll.CanvasSize         = UDim2.new(0, 0, 0, 0)
    spListScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
    spListScroll.ZIndex             = 31
    spListScroll.Parent             = spPopup

    local spPopupTween = TweenInfo.new(0.18, Enum.EasingStyle.Quart)

    -- ── Populate player list (with search filter) ───────────────
    local function refreshSpPlayerList()
        for _, child in ipairs(spListScroll:GetChildren()) do
            child:Destroy()
        end
        local listH = 0
        local query = spSearchBox.Text:lower()
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then
                do
                    local entry = Instance.new("TextButton")
                    entry.Size               = UDim2.new(1, 0, 0, 34)
                    entry.Position           = UDim2.new(0, 0, 0, listH)
                    entry.BackgroundTransparency = 1
                    entry.Text               = ""
                    entry.ZIndex             = 32
                    entry.Parent             = spListScroll

                    local dnLbl = Instance.new("TextLabel")
                    dnLbl.Size               = UDim2.new(1, -4, 0, 18)
                    dnLbl.Position           = UDim2.new(0, 4, 0, 2)
                    dnLbl.BackgroundTransparency = 1
                    dnLbl.Text               = p.DisplayName
                    dnLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
                    dnLbl.TextSize           = 10
                    dnLbl.Font               = Enum.Font.GothamBold
                    dnLbl.TextXAlignment     = Enum.TextXAlignment.Left
                    dnLbl.ZIndex             = 32
                    dnLbl.Parent             = entry

                    local unLbl = Instance.new("TextLabel")
                    unLbl.Size               = UDim2.new(1, -4, 0, 12)
                    unLbl.Position           = UDim2.new(0, 4, 0, 20)
                    unLbl.BackgroundTransparency = 1
                    unLbl.Text               = "@" .. p.Name
                    unLbl.TextColor3         = Color3.fromRGB(110, 110, 110)
                    unLbl.TextSize           = 8
                    unLbl.Font               = Enum.Font.Gotham
                    unLbl.TextXAlignment     = Enum.TextXAlignment.Left
                    unLbl.ZIndex             = 32
                    unLbl.Parent             = entry

                    local pRef = p
                    entry.MouseButton1Click:Connect(function()
                        spectTarget = pRef
                        spPickerBtn.Text       = pRef.DisplayName
                        spPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                        -- live: if spectate toggle is ON, immediately switch to this player
                        if spectEnabled and pRef.Character then
                            pcall(function()
                                workspace.CurrentCamera.CameraSubject = pRef.Character
                            end)
                        end
                        -- close popup
                        spPopupOpen = false
                        spPickerArrow.Text = "▲"
                        TweenService:Create(spPopup, spPopupTween, {
                            Size = UDim2.new(0, spPopup.AbsoluteSize.X, 0, 0),
                            Position = UDim2.new(0, spPopup.AbsolutePosition.X, 0,
                                spPopup.AbsolutePosition.Y + spPopup.AbsoluteSize.Y)
                        }):Play()
                        task.delay(0.2, function() spPopup.Visible = false end)
                    end)
                end
                listH = listH + 34
                end -- if show
            end
        end
        spListScroll.CanvasSize = UDim2.new(0, 0, 0, listH)
        spListScroll.ScrollBarThickness = listH > 82 and 3 or 0
    end

    spRefreshBtn.MouseButton1Click:Connect(refreshSpPlayerList)

    -- Live search filter as user types
    spSearchBox:GetPropertyChangedSignal("Text"):Connect(function()
        if spPopupOpen then refreshSpPlayerList() end
    end)

    -- ── Picker button click: toggle popup ────────────────────────
    spPickerBtn.MouseButton1Click:Connect(function()
        if spPopupOpen then
            -- close
            spPopupOpen = false
            spPickerArrow.Text = "▲"
            TweenService:Create(spPopup, spPopupTween, {
                Size = UDim2.new(0, spPopup.AbsoluteSize.X, 0, 0),
                Position = UDim2.new(0, spPopup.AbsolutePosition.X, 0,
                    spPopup.AbsolutePosition.Y + spPopup.AbsoluteSize.Y)
            }):Play()
            task.delay(0.2, function() spPopup.Visible = false end)
        else
            -- open: expand upward from button top
            spPopupOpen = true
            spPickerArrow.Text = "▼"
            spSearchBox.Text   = ""
            refreshSpPlayerList()
            local btnAbs  = spPickerBtn.AbsolutePosition
            local btnSz   = spPickerBtn.AbsoluteSize
            local popW    = btnSz.X
            spPopup.Size     = UDim2.new(0, popW, 0, 0)
            spPopup.Position = UDim2.new(0, btnAbs.X, 0, btnAbs.Y)
            spPopup.Visible  = true
            TweenService:Create(spPopup, spPopupTween, {
                Size     = UDim2.new(0, popW, 0, POPUP_FULL_H),
                Position = UDim2.new(0, btnAbs.X, 0, btnAbs.Y - POPUP_FULL_H)
            }):Play()
        end
    end)

    -- ── Spectate core logic ──────────────────────────────────────
    local function stopSpectate()
        spectEnabled = false
        -- NOTE: spectTarget intentionally NOT cleared → selection persists
        if spectViewDied  then spectViewDied:Disconnect();  spectViewDied  = nil end
        if spectViewChgd  then spectViewChgd:Disconnect();  spectViewChgd  = nil end
        if spectLeaveConn then spectLeaveConn:Disconnect(); spectLeaveConn = nil end
        pcall(function()
            workspace.CurrentCamera.CameraSubject = LocalPlayer.Character
        end)
        updateSpPill(false)
    end

    local function startSpectate()
        if not spectTarget or not spectTarget.Character then return end
        spectEnabled = true
        updateSpPill(true)

        pcall(function()
            workspace.CurrentCamera.CameraSubject = spectTarget.Character
        end)

        spectViewDied = spectTarget.CharacterAdded:Connect(function(newChar)
            if not spectEnabled then return end
            task.wait()
            pcall(function() workspace.CurrentCamera.CameraSubject = newChar end)
        end)

        spectViewChgd = workspace.CurrentCamera:GetPropertyChangedSignal("CameraSubject"):Connect(function()
            if spectEnabled and spectTarget and spectTarget.Character then
                pcall(function() workspace.CurrentCamera.CameraSubject = spectTarget.Character end)
            end
        end)

        spectLeaveConn = Players.PlayerRemoving:Connect(function(p)
            if p == spectTarget then
                spectTarget = nil
                spPickerBtn.Text       = "Select a player"
                spPickerBtn.TextColor3 = Color3.fromRGB(100, 100, 100)
                stopSpectate()
            end
        end)
    end

    spToggleHitbox.MouseButton1Click:Connect(function()
        if spectEnabled then
            stopSpectate()
        else
            if not spectTarget then
                -- No player selected: fake ON→OFF animate so user knows toggle works
                updateSpPill(true)
                task.delay(0.45, function()
                    updateSpPill(false)
                end)
                return
            end
            startSpectate()
        end
    end)

    -- ── Close popup on outside touch ─────────────────────────────
    UserInputService.InputBegan:Connect(function(input)
        if not spPopupOpen then return end
        if input.UserInputType ~= Enum.UserInputType.Touch and
           input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
        local tp = input.Position
        local pa = spPopup.AbsolutePosition
        local ps = spPopup.AbsoluteSize
        if tp.X >= pa.X and tp.X <= pa.X + ps.X and
           tp.Y >= pa.Y and tp.Y <= pa.Y + ps.Y then return end
        -- let picker button handle its own toggle
        local ba = spPickerBtn.AbsolutePosition
        local bs = spPickerBtn.AbsoluteSize
        if tp.X >= ba.X and tp.X <= ba.X + bs.X and
           tp.Y >= ba.Y and tp.Y <= ba.Y + bs.Y then return end
        spPopupOpen = false
        spPickerArrow.Text = "▲"
        spPopup.Visible = false
    end)

    -- ── Register with closeAllPopups (minimize closes popup) ─────
    table.insert(closeAllPopups, function()
        spPopupOpen = false
        spPickerArrow.Text = "▲"
        spPopup.Visible = false
    end)

    -- cfg: spectate (state only – target player cannot be saved)
    cfgRead["sp"] = function()
        return {en=spectEnabled}
    end
    cfgApply["sp"] = function(_t)
        -- spectate target must be selected manually; skip toggle restore
    end

end -- end SPECTATE

-- ========================
-- CAMERA: FREELOOK FEATURE
-- v10: Static camera position, mouse rotates view 360° (pitch+yaw)
--      No movement keys. Good for observing surroundings from fixed spot.
-- ========================

do -- === FREELOOK (scoped) ===

    -- ── Divider before FreeLook (y=107) ──────────────────────────
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 107)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = CameraWrapper
    end

    local flEnabled   = false
    local flRSConn    = nil
    local flMouseConn = nil
    local flSavedType, flSavedFov

    local flCamPos = Vector3.new()
    local flPitch  = 0
    local flYaw    = 0
    local flMDelta = Vector2.new()

    -- ── FreeLook Toggle row (y=111) ───────────────────────────────
    local flRow = Instance.new("Frame")
    flRow.Size               = UDim2.new(1, -8, 0, 22)
    flRow.Position           = UDim2.new(0, 4, 0, 111)
    flRow.BackgroundTransparency = 1
    flRow.BorderSizePixel    = 0
    flRow.Parent             = CameraWrapper

    local flLbl = Instance.new("TextLabel")
    flLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    flLbl.BackgroundTransparency = 1
    flLbl.Text               = "FreeLook"
    flLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    flLbl.TextSize           = 10
    flLbl.Font               = Enum.Font.Gotham
    flLbl.TextXAlignment     = Enum.TextXAlignment.Left
    flLbl.Parent             = flRow

    local flStateText = Instance.new("TextLabel")
    flStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    flStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    flStateText.BackgroundTransparency = 1
    flStateText.Text             = "OFF"
    flStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    flStateText.TextSize         = 10
    flStateText.Font             = Enum.Font.GothamBold
    flStateText.TextXAlignment   = Enum.TextXAlignment.Right
    flStateText.Parent           = flRow

    local flPillTrack = Instance.new("Frame")
    flPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    flPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    flPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    flPillTrack.BorderSizePixel  = 0
    flPillTrack.Parent           = flRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = flPillTrack end

    local flKnob = Instance.new("Frame")
    flKnob.Size             = UDim2.new(0, 12, 0, 12)
    flKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    flKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    flKnob.BorderSizePixel  = 0
    flKnob.Parent           = flPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = flKnob end

    local flToggleHitbox = Instance.new("TextButton")
    flToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    flToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    flToggleHitbox.BackgroundTransparency = 1
    flToggleHitbox.Text               = ""
    flToggleHitbox.Parent             = flRow

    local flPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateFlPill(on)
        if on then
            TweenService:Create(flPillTrack, flPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(flKnob, flPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            flStateText.Text = "ON";  flStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(flPillTrack, flPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(flKnob, flPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            flStateText.Text = "OFF"; flStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── FreeLook core logic ───────────────────────────────────────
    local function startFreeLook()
        if flEnabled then return end
        flEnabled = true

        local cam   = workspace.CurrentCamera
        flSavedType = cam.CameraType
        flSavedFov  = cam.FieldOfView

        -- Snapshot EXACT current camera position + orientation
        -- Position stays fixed wherever camera was (zoomed out, zoomed in, etc.)
        flCamPos = cam.CFrame.Position
        local rx, ry, _ = cam.CFrame:ToOrientation()
        flPitch  = rx
        flYaw    = ry
        flMDelta = Vector2.new()

        pcall(function() cam.CameraType = Enum.CameraType.Scriptable end)

        -- Accumulate mouse delta (Desktop: MouseMovement, Mobile: Touch)
        flMouseConn = UserInputService.InputChanged:Connect(function(inp)
            if not flEnabled then return end
            if inp.UserInputType == Enum.UserInputType.MouseMovement or
               inp.UserInputType == Enum.UserInputType.Touch then
                flMDelta = flMDelta + Vector2.new(-inp.Delta.Y, -inp.Delta.X)
            end
        end)

        -- RenderStepped: update rotation ONLY, position stays fixed
        flRSConn = RunService.RenderStepped:Connect(function()
            if not flEnabled then return end

            -- sensitivity ~0.006 rad/px (same feel as freecam pan)
            local sens = 0.006
            flPitch  = math.clamp(flPitch + flMDelta.X * sens, -math.rad(89), math.rad(89))
            flYaw    = (flYaw + flMDelta.Y * sens) % (2 * math.pi)
            flMDelta = Vector2.new()

            -- flCamPos stays fixed → no movement, only rotation
            pcall(function()
                local newCF = CFrame.new(flCamPos) * CFrame.fromOrientation(flPitch, flYaw, 0)
                workspace.CurrentCamera.CFrame      = newCF
                workspace.CurrentCamera.Focus       = newCF * CFrame.new(0, 0, -10)
                workspace.CurrentCamera.FieldOfView = flSavedFov or 70
            end)
        end)
    end

    local function stopFreeLook()
        if not flEnabled then return end
        flEnabled = false

        if flRSConn    then flRSConn:Disconnect();    flRSConn    = nil end
        if flMouseConn then flMouseConn:Disconnect(); flMouseConn = nil end
        flMDelta = Vector2.new()

        -- Restore camera to normal
        pcall(function()
            local cam       = workspace.CurrentCamera
            cam.CameraType  = flSavedType or Enum.CameraType.Custom
            cam.FieldOfView = flSavedFov  or 70
        end)
    end

    flToggleHitbox.MouseButton1Click:Connect(function()
        if flEnabled then
            stopFreeLook()
        else
            startFreeLook()
        end
        updateFlPill(flEnabled)
    end)

    -- cfg: freelook
    cfgRead["fl"] = function()
        return {en=flEnabled}
    end
    cfgApply["fl"] = function(t)
        local wantOn = (t.en == true)
        if flEnabled ~= wantOn then
            if wantOn then startFreeLook() else stopFreeLook() end
            updateFlPill(flEnabled)
        end
    end

end -- end FREELOOK

-- ========================
-- PLAYER (COMBAT/STATUS) FEATURES
-- IY ref: invisible/invis, reset, respawn
-- ========================

do -- === PLAYER (scoped) ===

    -- ════════════════════════════════════════════
    -- INVISIBLE TOGGLE (y=27)
    -- ════════════════════════════════════════════
    local invisRunning   = false
    local invisCharacter = nil
    local invisOrigChar  = nil
    local invisFix       = nil
    local invisDied      = nil

    -- ════════════════════════════════════════════
    -- NOCLIP SECTION (y=27) — moved from Movement
    -- ════════════════════════════════════════════
    local noclipEnabled = false
    local noclipConn    = nil

    local noclipRow = Instance.new("Frame")
    noclipRow.Size               = UDim2.new(1, -8, 0, 22)
    noclipRow.Position           = UDim2.new(0, 4, 0, 27)
    noclipRow.BackgroundTransparency = 1
    noclipRow.BorderSizePixel    = 0
    noclipRow.Parent             = PlayerWrapper

    local ncLbl = Instance.new("TextLabel")
    ncLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    ncLbl.BackgroundTransparency = 1
    ncLbl.Text               = "Noclip"
    ncLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    ncLbl.TextSize           = 10
    ncLbl.Font               = Enum.Font.Gotham
    ncLbl.TextXAlignment     = Enum.TextXAlignment.Left
    ncLbl.Parent             = noclipRow

    local ncStateText = Instance.new("TextLabel")
    ncStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    ncStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    ncStateText.BackgroundTransparency = 1
    ncStateText.Text             = "OFF"
    ncStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    ncStateText.TextSize         = 10
    ncStateText.Font             = Enum.Font.GothamBold
    ncStateText.TextXAlignment   = Enum.TextXAlignment.Right
    ncStateText.Parent           = noclipRow

    local ncPillTrack = Instance.new("Frame")
    ncPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    ncPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    ncPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    ncPillTrack.BorderSizePixel  = 0
    ncPillTrack.Parent           = noclipRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = ncPillTrack end

    local ncKnob = Instance.new("Frame")
    ncKnob.Size             = UDim2.new(0, 12, 0, 12)
    ncKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    ncKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    ncKnob.BorderSizePixel  = 0
    ncKnob.Parent           = ncPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = ncKnob end

    local ncToggleHitbox = Instance.new("TextButton")
    ncToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    ncToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    ncToggleHitbox.BackgroundTransparency = 1
    ncToggleHitbox.Text               = ""
    ncToggleHitbox.Parent             = noclipRow

    local ncPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateNcPill(on)
        if on then
            TweenService:Create(ncPillTrack, ncPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(ncKnob, ncPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            ncStateText.Text = "ON";  ncStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(ncPillTrack, ncPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(ncKnob, ncPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            ncStateText.Text = "OFF"; ncStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    ncToggleHitbox.MouseButton1Click:Connect(function()
        noclipEnabled = not noclipEnabled
        updateNcPill(noclipEnabled)
        if noclipEnabled then
            if noclipConn then noclipConn:Disconnect() end
            noclipConn = RunService.Stepped:Connect(function()
                if not noclipEnabled then return end
                local char = LocalPlayer.Character
                if not char then return end
                for _, child in ipairs(char:GetDescendants()) do
                    if child:IsA("BasePart") and child.CanCollide then
                        child.CanCollide = false
                    end
                end
            end)
        else
            if noclipConn then noclipConn:Disconnect(); noclipConn = nil end
            pcall(function()
                local char = LocalPlayer.Character
                if not char then return end
                for _, child in ipairs(char:GetDescendants()) do
                    if child:IsA("BasePart") then
                        child.CanCollide = true
                    end
                end
            end)
        end
    end)

    -- Divider between Noclip and Invisible (y=52)
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 52)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = PlayerWrapper
    end

    -- ════════════════════════════════════════════
    -- INVISIBLE TOGGLE (y=56)
    -- ════════════════════════════════════════════

    local invRow = Instance.new("Frame")
    invRow.Size               = UDim2.new(1, -8, 0, 22)
    invRow.Position           = UDim2.new(0, 4, 0, 56)
    invRow.BackgroundTransparency = 1
    invRow.BorderSizePixel    = 0
    invRow.Parent             = PlayerWrapper

    local invLbl = Instance.new("TextLabel")
    invLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    invLbl.BackgroundTransparency = 1
    invLbl.Text               = "Invisible"
    invLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    invLbl.TextSize           = 10
    invLbl.Font               = Enum.Font.Gotham
    invLbl.TextXAlignment     = Enum.TextXAlignment.Left
    invLbl.Parent             = invRow

    local invStateText = Instance.new("TextLabel")
    invStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    invStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    invStateText.BackgroundTransparency = 1
    invStateText.Text             = "OFF"
    invStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    invStateText.TextSize         = 10
    invStateText.Font             = Enum.Font.GothamBold
    invStateText.TextXAlignment   = Enum.TextXAlignment.Right
    invStateText.Parent           = invRow

    local invPillTrack = Instance.new("Frame")
    invPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    invPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    invPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    invPillTrack.BorderSizePixel  = 0
    invPillTrack.Parent           = invRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = invPillTrack end

    local invKnob = Instance.new("Frame")
    invKnob.Size             = UDim2.new(0, 12, 0, 12)
    invKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    invKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    invKnob.BorderSizePixel  = 0
    invKnob.Parent           = invPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = invKnob end

    local invToggleHitbox = Instance.new("TextButton")
    invToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    invToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    invToggleHitbox.BackgroundTransparency = 1
    invToggleHitbox.Text               = ""
    invToggleHitbox.Parent             = invRow

    local invPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateInvPill(on)
        if on then
            TweenService:Create(invPillTrack, invPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(invKnob, invPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            invStateText.Text = "ON";  invStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(invPillTrack, invPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(invKnob, invPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            invStateText.Text = "OFF"; invStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- IY ref: invisible/invis + visible/uninvisible
    local function turnVisible()
        if not invisRunning then return end
        pcall(function()
            if invisFix  then invisFix:Disconnect();  invisFix  = nil end
            if invisDied then invisDied:Disconnect(); invisDied = nil end
            local CF1 = LocalPlayer.Character and
                LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and
                LocalPlayer.Character.HumanoidRootPart.CFrame
            if invisOrigChar then
                if CF1 then
                    local hrp = invisOrigChar:FindFirstChild("HumanoidRootPart")
                    if hrp then hrp.CFrame = CF1 end
                end
                if invisCharacter then invisCharacter:Destroy() end
                LocalPlayer.Character = invisOrigChar
                invisOrigChar.Parent  = workspace
                local anim = invisOrigChar:FindFirstChild("Animate")
                if anim then anim.Disabled = true; task.wait(); anim.Disabled = false end
            end
        end)
        invisRunning   = false
        invisCharacter = nil
        invisOrigChar  = nil
        updateInvPill(false)
    end

    local function goInvisible()
        if invisRunning then turnVisible(); return end
        local Player    = LocalPlayer
        local Character = Player.Character
        if not Character then return end
        Character.Archivable = true
        invisOrigChar  = Character
        invisCharacter = Character:Clone()
        invisCharacter.Parent = game:GetService("Lighting")
        invisCharacter.Name   = ""

        -- Fake clone: HRP transparent, rest semi-transparent
        for _, v in ipairs(invisCharacter:GetDescendants()) do
            if v:IsA("BasePart") then
                v.Transparency = v.Name == "HumanoidRootPart" and 1 or 0.5
            end
        end

        -- Void-fall protection
        invisFix = RunService.Stepped:Connect(function()
            pcall(function()
                if not invisRunning then return end
                local hrp = Player.Character and
                    Player.Character:FindFirstChild("HumanoidRootPart")
                local voidY = workspace.FallenPartsDestroyHeight
                if hrp and hrp.Position.Y <= voidY then
                    turnVisible()
                end
            end)
        end)

        -- Die detection
        local invHum = invisCharacter:FindFirstChildOfClass("Humanoid")
        if invHum then
            invisDied = invHum.Died:Connect(function()
                turnVisible()
            end)
        end

        -- Swap character: move real char out, fake clone in
        local CF1 = Character.HumanoidRootPart.CFrame
        Character:MoveTo(Vector3.new(0, math.huge, 0))
        pcall(function()
            workspace.CurrentCamera.CameraType = Enum.CameraType.Scriptable
            task.wait(0.18)
            workspace.CurrentCamera.CameraType = Enum.CameraType.Custom
        end)
        Character.Parent = game:GetService("Lighting")
        invisCharacter.Parent = workspace
        invisCharacter:FindFirstChild("HumanoidRootPart").CFrame = CF1
        Player.Character = invisCharacter
        local anim = invisCharacter:FindFirstChild("Animate")
        if anim then anim.Disabled = true; task.wait(); anim.Disabled = false end
        invisRunning = true
        updateInvPill(true)
    end

    invToggleHitbox.MouseButton1Click:Connect(function()
        if invisRunning then turnVisible() else goInvisible() end
    end)

    -- Divider before buttons
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 81)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = PlayerWrapper
    end

    -- ════════════════════════════════════════════
    -- RESET + RESPAWN BUTTONS — one row (y=86)
    -- IY ref: reset → humanoid:ChangeState(Dead)
    --         respawn → character swap trick
    -- Reset kiri, Respawn kanan, spacing 6px tengah
    -- ════════════════════════════════════════════
    local btnRow = Instance.new("Frame")
    btnRow.Size               = UDim2.new(1, -8, 0, 20)
    btnRow.Position           = UDim2.new(0, 4, 0, 86)
    btnRow.BackgroundTransparency = 1
    btnRow.BorderSizePixel    = 0
    btnRow.Parent             = PlayerWrapper

    local resetBtn = Instance.new("TextButton")
    resetBtn.Size             = UDim2.new(0.5, -3, 1, 0)
    resetBtn.Position         = UDim2.new(0, 0, 0, 0)
    resetBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    resetBtn.BorderSizePixel  = 0
    resetBtn.Text             = "Reset"
    resetBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    resetBtn.TextSize         = 10
    resetBtn.Font             = Enum.Font.Gotham
    resetBtn.Parent           = btnRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = resetBtn end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.85; s.Parent = resetBtn end

    local respawnBtn = Instance.new("TextButton")
    respawnBtn.Size             = UDim2.new(0.5, -3, 1, 0)
    respawnBtn.Position         = UDim2.new(0.5, 3, 0, 0)
    respawnBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    respawnBtn.BorderSizePixel  = 0
    respawnBtn.Text             = "Respawn"
    respawnBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    respawnBtn.TextSize         = 10
    respawnBtn.Font             = Enum.Font.Gotham
    respawnBtn.Parent           = btnRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = respawnBtn end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.85; s.Parent = respawnBtn end

    resetBtn.MouseButton1Click:Connect(function()
        if invisRunning then turnVisible() end
        pcall(function()
            local hum = LocalPlayer.Character and
                LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
            if hum then
                hum:ChangeState(Enum.HumanoidStateType.Dead)
            else
                LocalPlayer.Character:BreakJoints()
            end
        end)
    end)

    respawnBtn.MouseButton1Click:Connect(function()
        if invisRunning then turnVisible() end
        pcall(function()
            local char = LocalPlayer.Character
            local hum  = char and char:FindFirstChildWhichIsA("Humanoid")
            if hum then hum:ChangeState(Enum.HumanoidStateType.Dead) end
            if char then char:ClearAllChildren() end
            local dummy = Instance.new("Model")
            dummy.Parent          = workspace
            LocalPlayer.Character = dummy
            task.wait()
            LocalPlayer.Character = char
            dummy:Destroy()
        end)
    end)

    -- ════════════════════════════════════════════
    -- HANDLEKILL (y=82 label, y=100 toggle row)
    -- IY ref: tool.Activated → kill target in radius
    -- ════════════════════════════════════════════

    -- Section label
    local hkLabel = Instance.new("TextLabel")
    hkLabel.Size               = UDim2.new(1, -8, 0, 14)
    hkLabel.Position           = UDim2.new(0, 4, 0, 111)
    hkLabel.BackgroundTransparency = 1
    hkLabel.Text               = "Handlekill"
    hkLabel.TextColor3         = Color3.fromRGB(255, 255, 255)
    hkLabel.TextSize           = 10
    hkLabel.Font               = Enum.Font.Gotham
    hkLabel.TextXAlignment     = Enum.TextXAlignment.Left
    hkLabel.Parent             = PlayerWrapper

    -- State variables (scoped, no file-scope locals added)
    local hkEnabled   = false
    local hkTargets   = {}   -- multi-select player list
    local hkRadius    = 10
    local hkConn      = nil  -- CharacterAdded
    local hkChildConn = nil  -- ChildAdded for tool equip
    local hkToolConn  = nil  -- Tool.Activated

    -- ── Toggle row (y=129): picker | radius input | pill ─────────
    local hkRow = Instance.new("Frame")
    hkRow.Size               = UDim2.new(1, -8, 0, 22)
    hkRow.Position           = UDim2.new(0, 4, 0, 129)
    hkRow.BackgroundTransparency = 1
    hkRow.BorderSizePixel    = 0
    hkRow.Parent             = PlayerWrapper

    -- Player picker button (~52% width)
    local hkPickerBtn = Instance.new("TextButton")
    hkPickerBtn.Size               = UDim2.new(0.52, -3, 0, 18)
    hkPickerBtn.Position           = UDim2.new(0, 0, 0.5, -9)
    hkPickerBtn.BackgroundColor3   = Color3.fromRGB(22, 22, 22)
    hkPickerBtn.BorderSizePixel    = 0
    hkPickerBtn.Text               = "Select player"
    hkPickerBtn.TextColor3         = Color3.fromRGB(100, 100, 100)
    hkPickerBtn.TextSize           = 9
    hkPickerBtn.Font               = Enum.Font.Gotham
    hkPickerBtn.TextXAlignment     = Enum.TextXAlignment.Left
    hkPickerBtn.Parent             = hkRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = hkPickerBtn end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.PaddingRight = UDim.new(0,16); p.Parent = hkPickerBtn end

    local hkPickerArrow = Instance.new("TextLabel")
    hkPickerArrow.Size               = UDim2.new(0, 14, 1, 0)
    hkPickerArrow.Position           = UDim2.new(1, -15, 0, 0)
    hkPickerArrow.BackgroundTransparency = 1
    hkPickerArrow.Text               = "▲"
    hkPickerArrow.TextColor3         = Color3.fromRGB(100, 100, 100)
    hkPickerArrow.TextSize           = 7
    hkPickerArrow.Font               = Enum.Font.GothamBold
    hkPickerArrow.ZIndex             = 2
    hkPickerArrow.Parent             = hkPickerBtn

    -- Radius inputbox (~23% width)
    local hkRadiusInput = Instance.new("TextBox")
    hkRadiusInput.Size               = UDim2.new(0.23, -3, 0, 18)
    hkRadiusInput.Position           = UDim2.new(0.52, 2, 0.5, -9)
    hkRadiusInput.BackgroundColor3   = Color3.fromRGB(22, 22, 22)
    hkRadiusInput.BorderSizePixel    = 0
    hkRadiusInput.Text               = ""
    hkRadiusInput.PlaceholderText    = "radius"
    hkRadiusInput.TextColor3         = Color3.fromRGB(255, 255, 255)
    hkRadiusInput.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    hkRadiusInput.TextSize           = 9
    hkRadiusInput.Font               = Enum.Font.Gotham
    hkRadiusInput.ClearTextOnFocus   = false
    hkRadiusInput.Parent             = hkRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = hkRadiusInput end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,5); p.Parent = hkRadiusInput end

    hkRadiusInput:GetPropertyChangedSignal("Text"):Connect(function()
        local n = tonumber(hkRadiusInput.Text)
        if n and n > 0 then hkRadius = n end
    end)

    -- ON/OFF state text (right of radius, left of pill)
    local hkStateText = Instance.new("TextLabel")
    hkStateText.Size             = UDim2.new(0, 22, 1, 0)
    hkStateText.Position         = UDim2.new(1, -60, 0, 0)
    hkStateText.BackgroundTransparency = 1
    hkStateText.Text             = "OFF"
    hkStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    hkStateText.TextSize         = 10
    hkStateText.Font             = Enum.Font.GothamBold
    hkStateText.TextXAlignment   = Enum.TextXAlignment.Right
    hkStateText.Parent           = hkRow

    -- Pill toggle (far right 34px)
    local hkPillTrack = Instance.new("Frame")
    hkPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    hkPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    hkPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    hkPillTrack.BorderSizePixel  = 0
    hkPillTrack.Parent           = hkRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = hkPillTrack end

    local hkKnob = Instance.new("Frame")
    hkKnob.Size             = UDim2.new(0, 12, 0, 12)
    hkKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    hkKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    hkKnob.BorderSizePixel  = 0
    hkKnob.Parent           = hkPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = hkKnob end

    local hkToggleHitbox = Instance.new("TextButton")
    hkToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    hkToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    hkToggleHitbox.BackgroundTransparency = 1
    hkToggleHitbox.Text               = ""
    hkToggleHitbox.Parent             = hkRow

    local hkPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateHkPill(on)
        if on then
            TweenService:Create(hkPillTrack, hkPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(hkKnob,      hkPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            hkStateText.Text = "ON";  hkStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(hkPillTrack, hkPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(hkKnob,      hkPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            hkStateText.Text = "OFF"; hkStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── Handlekill Player Picker Popup (expands upward, ScreenGui) ──
    -- Same style as spectate popup, tambahan: search bar + select/deselect all
    local HK_POPUP_H  = 150
    local hkPopupOpen = false

    local hkPopup = Instance.new("Frame")
    hkPopup.Size             = UDim2.new(0, 150, 0, 0)
    hkPopup.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
    hkPopup.BorderSizePixel  = 0
    hkPopup.Visible          = false
    hkPopup.ClipsDescendants = true
    hkPopup.ZIndex           = 30
    hkPopup.Parent           = ScreenGui
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = hkPopup end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.75; s.Parent = hkPopup end

    -- Refresh button (y=4)
    local hkRefreshBtn = Instance.new("TextButton")
    hkRefreshBtn.Size             = UDim2.new(1, -8, 0, 18)
    hkRefreshBtn.Position         = UDim2.new(0, 4, 0, 4)
    hkRefreshBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    hkRefreshBtn.BorderSizePixel  = 0
    hkRefreshBtn.Text             = "↻  Refresh"
    hkRefreshBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    hkRefreshBtn.TextSize         = 9
    hkRefreshBtn.Font             = Enum.Font.GothamBold
    hkRefreshBtn.ZIndex           = 31
    hkRefreshBtn.Parent           = hkPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = hkRefreshBtn end

    -- Search bar (y=26, below refresh)
    local hkSearchBox = Instance.new("TextBox")
    hkSearchBox.Size             = UDim2.new(1, -8, 0, 18)
    hkSearchBox.Position         = UDim2.new(0, 4, 0, 26)
    hkSearchBox.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    hkSearchBox.BorderSizePixel  = 0
    hkSearchBox.Text             = ""
    hkSearchBox.PlaceholderText  = "search player..."
    hkSearchBox.TextColor3       = Color3.fromRGB(255, 255, 255)
    hkSearchBox.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
    hkSearchBox.TextSize         = 9
    hkSearchBox.Font             = Enum.Font.Gotham
    hkSearchBox.TextXAlignment   = Enum.TextXAlignment.Left
    hkSearchBox.ClearTextOnFocus = false
    hkSearchBox.ZIndex           = 31
    hkSearchBox.Parent           = hkPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = hkSearchBox end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = hkSearchBox end

    -- Select/Deselect All button (y=48, below search)
    local hkSelAllBtn = Instance.new("TextButton")
    hkSelAllBtn.Size             = UDim2.new(1, -8, 0, 18)
    hkSelAllBtn.Position         = UDim2.new(0, 4, 0, 48)
    hkSelAllBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    hkSelAllBtn.BorderSizePixel  = 0
    hkSelAllBtn.Text             = "Select All"
    hkSelAllBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    hkSelAllBtn.TextSize         = 9
    hkSelAllBtn.Font             = Enum.Font.GothamBold
    hkSelAllBtn.ZIndex           = 31
    hkSelAllBtn.Parent           = hkPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = hkSelAllBtn end

    -- Player list scroll (y=70, below select-all btn)
    local hkListScroll = Instance.new("ScrollingFrame")
    hkListScroll.Size               = UDim2.new(1, -8, 0, 72)
    hkListScroll.Position           = UDim2.new(0, 4, 0, 70)
    hkListScroll.BackgroundTransparency = 1
    hkListScroll.BorderSizePixel    = 0
    hkListScroll.ScrollBarThickness = 3
    hkListScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
    hkListScroll.CanvasSize         = UDim2.new(0, 0, 0, 0)
    hkListScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
    hkListScroll.ZIndex             = 31
    hkListScroll.Parent             = hkPopup

    local hkPopupTween = TweenInfo.new(0.18, Enum.EasingStyle.Quart)

    -- ── Helper: update picker button text based on hkTargets ─────
    local function updateHkPickerText()
        if #hkTargets == 0 then
            hkPickerBtn.Text       = "Select player"
            hkPickerBtn.TextColor3 = Color3.fromRGB(100, 100, 100)
        elseif #hkTargets == 1 then
            hkPickerBtn.Text       = hkTargets[1].DisplayName
            hkPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            hkPickerBtn.Text       = #hkTargets .. " players"
            hkPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end

    -- ── Helper: check if player is in hkTargets ───────────────────
    local function hkIsSelected(p)
        for _, t in ipairs(hkTargets) do
            if t == p then return true end
        end
        return false
    end

    -- ── Populate player list (filter by search query) ────────────
    local function refreshHkPlayerList()
        for _, child in ipairs(hkListScroll:GetChildren()) do child:Destroy() end
        local listH = 0
        local query = hkSearchBox.Text:lower()
        -- collect visible players
        local visiblePlayers = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then table.insert(visiblePlayers, p) end
            end
        end
        -- update Select All / Deselect All label
        local allSelected = #visiblePlayers > 0
        for _, p in ipairs(visiblePlayers) do
            if not hkIsSelected(p) then allSelected = false; break end
        end
        hkSelAllBtn.Text = allSelected and "Deselect All" or "Select All"

        for _, p in ipairs(visiblePlayers) do
            do
                local isSelected = hkIsSelected(p)
                local entry = Instance.new("TextButton")
                entry.Size               = UDim2.new(1, 0, 0, 34)
                entry.Position           = UDim2.new(0, 0, 0, listH)
                entry.BackgroundColor3   = isSelected and Color3.fromRGB(40,40,40) or Color3.fromRGB(0,0,0)
                entry.BackgroundTransparency = isSelected and 0 or 1
                entry.Text               = ""
                entry.ZIndex             = 32
                entry.Parent             = hkListScroll
                if isSelected then
                    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = entry end
                end

                local dnLbl = Instance.new("TextLabel")
                dnLbl.Size               = UDim2.new(1, -4, 0, 18)
                dnLbl.Position           = UDim2.new(0, 4, 0, 2)
                dnLbl.BackgroundTransparency = 1
                dnLbl.Text               = p.DisplayName
                dnLbl.TextColor3         = isSelected and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 255, 255)
                dnLbl.TextSize           = 10
                dnLbl.Font               = Enum.Font.GothamBold
                dnLbl.TextXAlignment     = Enum.TextXAlignment.Left
                dnLbl.ZIndex             = 32
                dnLbl.Parent             = entry

                local unLbl = Instance.new("TextLabel")
                unLbl.Size               = UDim2.new(1, -4, 0, 12)
                unLbl.Position           = UDim2.new(0, 4, 0, 20)
                unLbl.BackgroundTransparency = 1
                unLbl.Text               = "@" .. p.Name
                unLbl.TextColor3         = Color3.fromRGB(110, 110, 110)
                unLbl.TextSize           = 8
                unLbl.Font               = Enum.Font.Gotham
                unLbl.TextXAlignment     = Enum.TextXAlignment.Left
                unLbl.ZIndex             = 32
                unLbl.Parent             = entry

                local pRef = p
                entry.MouseButton1Click:Connect(function()
                    -- Toggle selection
                    local found = false
                    for i, t in ipairs(hkTargets) do
                        if t == pRef then
                            table.remove(hkTargets, i)
                            found = true
                            break
                        end
                    end
                    if not found then
                        table.insert(hkTargets, pRef)
                    end
                    updateHkPickerText()
                    refreshHkPlayerList()
                end)
            end
            listH = listH + 34
        end
        hkListScroll.CanvasSize         = UDim2.new(0, 0, 0, listH)
        hkListScroll.ScrollBarThickness = listH > 72 and 3 or 0
    end

    hkRefreshBtn.MouseButton1Click:Connect(refreshHkPlayerList)

    -- Select All / Deselect All button logic
    hkSelAllBtn.MouseButton1Click:Connect(function()
        local query = hkSearchBox.Text:lower()
        local visiblePlayers = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then table.insert(visiblePlayers, p) end
            end
        end
        -- check if all visible already selected
        local allSel = #visiblePlayers > 0
        for _, p in ipairs(visiblePlayers) do
            if not hkIsSelected(p) then allSel = false; break end
        end
        if allSel then
            -- deselect all visible
            for _, p in ipairs(visiblePlayers) do
                for i, t in ipairs(hkTargets) do
                    if t == p then table.remove(hkTargets, i); break end
                end
            end
        else
            -- select all visible not yet selected
            for _, p in ipairs(visiblePlayers) do
                if not hkIsSelected(p) then
                    table.insert(hkTargets, p)
                end
            end
        end
        updateHkPickerText()
        refreshHkPlayerList()
    end)

    -- Live search filter as user types
    hkSearchBox:GetPropertyChangedSignal("Text"):Connect(function()
        if hkPopupOpen then refreshHkPlayerList() end
    end)

    -- ── Picker button: toggle popup open/close ───────────────────
    hkPickerBtn.MouseButton1Click:Connect(function()
        if hkPopupOpen then
            -- close
            hkPopupOpen = false
            hkPickerArrow.Text = "▲"
            TweenService:Create(hkPopup, hkPopupTween, {
                Size     = UDim2.new(0, hkPopup.AbsoluteSize.X, 0, 0),
                Position = UDim2.new(0, hkPopup.AbsolutePosition.X, 0,
                    hkPopup.AbsolutePosition.Y + hkPopup.AbsoluteSize.Y)
            }):Play()
            task.delay(0.2, function() hkPopup.Visible = false end)
        else
            -- open: expand upward from picker button top
            hkPopupOpen = true
            hkPickerArrow.Text = "▼"
            hkSearchBox.Text   = ""
            refreshHkPlayerList()
            -- popup width = full row width (matches spectate popup style)
            local rowAbs = hkRow.AbsolutePosition
            local popW   = hkRow.AbsoluteSize.X
            hkPopup.Size     = UDim2.new(0, popW, 0, 0)
            hkPopup.Position = UDim2.new(0, rowAbs.X, 0, hkPickerBtn.AbsolutePosition.Y)
            hkPopup.Visible  = true
            TweenService:Create(hkPopup, hkPopupTween, {
                Size     = UDim2.new(0, popW, 0, HK_POPUP_H),
                Position = UDim2.new(0, rowAbs.X, 0, hkPickerBtn.AbsolutePosition.Y - HK_POPUP_H)
            }):Play()
        end
    end)

    -- ── Handlekill logic ─────────────────────────────────────────
    local function hkTryKill()
        if not hkEnabled or #hkTargets == 0 then return end
        local myChar = LocalPlayer.Character
        local myHrp  = myChar and myChar:FindFirstChild("HumanoidRootPart")
        if not myHrp then return end
        for _, target in ipairs(hkTargets) do
            local tChar = target.Character
            local tHrp  = tChar and tChar:FindFirstChild("HumanoidRootPart")
            if tHrp and (myHrp.Position - tHrp.Position).Magnitude <= hkRadius then
                local tHum = tChar:FindFirstChildOfClass("Humanoid")
                if tHum then
                    pcall(function() tHum:ChangeState(Enum.HumanoidStateType.Dead) end)
                end
            end
        end
    end

    local function hkListenChar(char)
        -- Disconnect any old tool connections
        if hkChildConn then hkChildConn:Disconnect(); hkChildConn = nil end
        if hkToolConn  then hkToolConn:Disconnect();  hkToolConn  = nil end
        -- Connect to any already-equipped tool
        local existTool = char:FindFirstChildOfClass("Tool")
        if existTool then
            hkToolConn = existTool.Activated:Connect(hkTryKill)
        end
        -- Watch for new tools being equipped
        hkChildConn = char.ChildAdded:Connect(function(child)
            if child:IsA("Tool") then
                if hkToolConn then hkToolConn:Disconnect() end
                hkToolConn = child.Activated:Connect(hkTryKill)
            end
        end)
    end

    local function startHandleKill()
        hkEnabled = true
        updateHkPill(true)
        local char = LocalPlayer.Character
        if char then hkListenChar(char) end
        hkConn = LocalPlayer.CharacterAdded:Connect(function(c)
            hkListenChar(c)
        end)
    end

    local function stopHandleKill()
        hkEnabled = false
        updateHkPill(false)
        if hkConn      then hkConn:Disconnect();      hkConn      = nil end
        if hkChildConn then hkChildConn:Disconnect();  hkChildConn = nil end
        if hkToolConn  then hkToolConn:Disconnect();   hkToolConn  = nil end
    end

    hkToggleHitbox.MouseButton1Click:Connect(function()
        if hkEnabled then stopHandleKill() else startHandleKill() end
    end)

    -- ── Close popup on outside touch ─────────────────────────────
    UserInputService.InputBegan:Connect(function(input)
        if not hkPopupOpen then return end
        if input.UserInputType ~= Enum.UserInputType.Touch and
           input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
        local tp = input.Position
        local pa = hkPopup.AbsolutePosition
        local ps = hkPopup.AbsoluteSize
        if tp.X >= pa.X and tp.X <= pa.X + ps.X and
           tp.Y >= pa.Y and tp.Y <= pa.Y + ps.Y then return end
        -- let picker button handle its own toggle
        local ba = hkPickerBtn.AbsolutePosition
        local bs = hkPickerBtn.AbsoluteSize
        if tp.X >= ba.X and tp.X <= ba.X + bs.X and
           tp.Y >= ba.Y and tp.Y <= ba.Y + bs.Y then return end
        hkPopupOpen = false
        hkPickerArrow.Text = "▲"
        hkPopup.Visible = false
    end)

    -- ── Register with closeAllPopups (minimize closes popup) ─────
    table.insert(closeAllPopups, function()
        hkPopupOpen = false
        hkPickerArrow.Text = "▲"
        hkPopup.Visible = false
    end)

    -- cfg: noclip
    cfgRead["nc"] = function()
        return {en=noclipEnabled}
    end
    cfgApply["nc"] = function(t)
        local wantOn = (t.en == true)
        if noclipEnabled ~= wantOn then
            noclipEnabled = wantOn
            updateNcPill(noclipEnabled)
            if noclipEnabled then
                if noclipConn then noclipConn:Disconnect() end
                noclipConn = RunService.Stepped:Connect(function()
                    if not noclipEnabled then return end
                    local char = LocalPlayer.Character
                    if not char then return end
                    for _, child in ipairs(char:GetDescendants()) do
                        if child:IsA("BasePart") and child.CanCollide then
                            child.CanCollide = false
                        end
                    end
                end)
            else
                if noclipConn then noclipConn:Disconnect(); noclipConn = nil end
            end
        end
    end

    -- cfg: invisible
    cfgRead["inv"] = function()
        return {en=invisRunning}
    end
    cfgApply["inv"] = function(t)
        local wantOn = (t.en == true)
        if invisRunning ~= wantOn then
            if wantOn then goInvisible() else turnVisible() end
        end
    end

    -- cfg: handlekill
    cfgRead["hk"] = function()
        return {en=hkEnabled, r=hkRadius}
    end
    cfgApply["hk"] = function(t)
        hkRadius = t.r or hkRadius
        hkRadiusInput.Text = tostring(hkRadius)
        local wantOn = (t.en == true)
        if hkEnabled ~= wantOn then
            if wantOn then startHandleKill() else stopHandleKill() end
        end
    end

end -- end PLAYER



do -- === MISC (Freeze Player) (scoped) ===

    -- ── Section label "Freeze player" (y=27) ────────────────────
    local fpLabel = Instance.new("TextLabel")
    fpLabel.Size               = UDim2.new(1, -8, 0, 14)
    fpLabel.Position           = UDim2.new(0, 4, 0, 27)
    fpLabel.BackgroundTransparency = 1
    fpLabel.Text               = "Freeze player"
    fpLabel.TextColor3         = Color3.fromRGB(255, 255, 255)
    fpLabel.TextSize           = 10
    fpLabel.Font               = Enum.Font.Gotham
    fpLabel.TextXAlignment     = Enum.TextXAlignment.Left
    fpLabel.Parent             = MiscWrapper

    -- ── State variables ──────────────────────────────────────────
    local fpEnabled    = false
    local fpTargets    = {}   -- multi-select player list
    local fpFrozenCF   = {}   -- [player] = CFrame, locked when freeze started or player added
    local fpFreezeConn = nil  -- single Heartbeat connection

    -- ── Toggle row (y=45): picker | pill ────────────────────────
    local fpRow = Instance.new("Frame")
    fpRow.Size               = UDim2.new(1, -8, 0, 22)
    fpRow.Position           = UDim2.new(0, 4, 0, 45)
    fpRow.BackgroundTransparency = 1
    fpRow.BorderSizePixel    = 0
    fpRow.Parent             = MiscWrapper

    -- Player picker button (~65% width)
    local fpPickerBtn = Instance.new("TextButton")
    fpPickerBtn.Size               = UDim2.new(0.65, -3, 0, 18)
    fpPickerBtn.Position           = UDim2.new(0, 0, 0.5, -9)
    fpPickerBtn.BackgroundColor3   = Color3.fromRGB(22, 22, 22)
    fpPickerBtn.BorderSizePixel    = 0
    fpPickerBtn.Text               = "Select player"
    fpPickerBtn.TextColor3         = Color3.fromRGB(100, 100, 100)
    fpPickerBtn.TextSize           = 9
    fpPickerBtn.Font               = Enum.Font.Gotham
    fpPickerBtn.TextXAlignment     = Enum.TextXAlignment.Left
    fpPickerBtn.Parent             = fpRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = fpPickerBtn end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.PaddingRight = UDim.new(0,16); p.Parent = fpPickerBtn end

    local fpPickerArrow = Instance.new("TextLabel")
    fpPickerArrow.Size               = UDim2.new(0, 14, 1, 0)
    fpPickerArrow.Position           = UDim2.new(1, -15, 0, 0)
    fpPickerArrow.BackgroundTransparency = 1
    fpPickerArrow.Text               = "▲"
    fpPickerArrow.TextColor3         = Color3.fromRGB(100, 100, 100)
    fpPickerArrow.TextSize           = 7
    fpPickerArrow.Font               = Enum.Font.GothamBold
    fpPickerArrow.ZIndex             = 2
    fpPickerArrow.Parent             = fpPickerBtn

    -- ON/OFF state text
    local fpStateText = Instance.new("TextLabel")
    fpStateText.Size             = UDim2.new(0, 22, 1, 0)
    fpStateText.Position         = UDim2.new(1, -60, 0, 0)
    fpStateText.BackgroundTransparency = 1
    fpStateText.Text             = "OFF"
    fpStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    fpStateText.TextSize         = 10
    fpStateText.Font             = Enum.Font.GothamBold
    fpStateText.TextXAlignment   = Enum.TextXAlignment.Right
    fpStateText.Parent           = fpRow

    -- Pill toggle track
    local fpPillTrack = Instance.new("Frame")
    fpPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    fpPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    fpPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    fpPillTrack.BorderSizePixel  = 0
    fpPillTrack.Parent           = fpRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = fpPillTrack end

    local fpKnob = Instance.new("Frame")
    fpKnob.Size             = UDim2.new(0, 12, 0, 12)
    fpKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    fpKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    fpKnob.BorderSizePixel  = 0
    fpKnob.Parent           = fpPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = fpKnob end

    local fpToggleHitbox = Instance.new("TextButton")
    fpToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    fpToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    fpToggleHitbox.BackgroundTransparency = 1
    fpToggleHitbox.Text               = ""
    fpToggleHitbox.Parent             = fpRow

    local fpPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateFpPill(on)
        if on then
            TweenService:Create(fpPillTrack, fpPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(fpKnob,      fpPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            fpStateText.Text = "ON";  fpStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(fpPillTrack, fpPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(fpKnob,      fpPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            fpStateText.Text = "OFF"; fpStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── Freeze / Unfreeze logic ──────────────────────────────────
    -- Snapshot CFrame for one target into fpFrozenCF
    local function snapTarget(target)
        local char = target and target.Character
        local hrp  = char and char:FindFirstChild("HumanoidRootPart")
        if hrp then
            fpFrozenCF[target] = hrp.CFrame
        end
    end

    -- Heartbeat loop: continuously lock all targets to their frozen CFrame
    -- NOTE: visual-only freeze (client-side), other player still moves on their end
    local function startFreezeLoop()
        if fpFreezeConn then fpFreezeConn:Disconnect() end
        fpFreezeConn = RunService.Heartbeat:Connect(function()
            for _, t in ipairs(fpTargets) do
                if t ~= LocalPlayer then
                    local cf = fpFrozenCF[t]
                    if cf then
                        pcall(function()
                            local char = t.Character
                            local hrp  = char and char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                hrp.CFrame = cf
                            end
                        end)
                    end
                end
            end
        end)
    end

    local function startFreeze()
        fpEnabled = true
        updateFpPill(true)
        -- Snapshot all current targets
        for _, t in ipairs(fpTargets) do
            snapTarget(t)
        end
        startFreezeLoop()
    end

    local function stopFreeze()
        fpEnabled = false
        updateFpPill(false)
        if fpFreezeConn then fpFreezeConn:Disconnect(); fpFreezeConn = nil end
        fpFrozenCF = {}
    end

    fpToggleHitbox.MouseButton1Click:Connect(function()
        if fpEnabled then stopFreeze() else startFreeze() end
    end)

    -- ── Freeze Player Popup (same style as handlekill) ───────────
    local FP_POPUP_H  = 150
    local fpPopupOpen = false

    local fpPopup = Instance.new("Frame")
    fpPopup.Size             = UDim2.new(0, 150, 0, 0)
    fpPopup.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
    fpPopup.BorderSizePixel  = 0
    fpPopup.Visible          = false
    fpPopup.ClipsDescendants = true
    fpPopup.ZIndex           = 30
    fpPopup.Parent           = ScreenGui
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = fpPopup end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.75; s.Parent = fpPopup end

    -- Refresh button (y=4)
    local fpRefreshBtn = Instance.new("TextButton")
    fpRefreshBtn.Size             = UDim2.new(1, -8, 0, 18)
    fpRefreshBtn.Position         = UDim2.new(0, 4, 0, 4)
    fpRefreshBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    fpRefreshBtn.BorderSizePixel  = 0
    fpRefreshBtn.Text             = "↻  Refresh"
    fpRefreshBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    fpRefreshBtn.TextSize         = 9
    fpRefreshBtn.Font             = Enum.Font.GothamBold
    fpRefreshBtn.ZIndex           = 31
    fpRefreshBtn.Parent           = fpPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = fpRefreshBtn end

    -- Search bar (y=26)
    local fpSearchBox = Instance.new("TextBox")
    fpSearchBox.Size             = UDim2.new(1, -8, 0, 18)
    fpSearchBox.Position         = UDim2.new(0, 4, 0, 26)
    fpSearchBox.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    fpSearchBox.BorderSizePixel  = 0
    fpSearchBox.Text             = ""
    fpSearchBox.PlaceholderText  = "search player..."
    fpSearchBox.TextColor3       = Color3.fromRGB(255, 255, 255)
    fpSearchBox.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
    fpSearchBox.TextSize         = 9
    fpSearchBox.Font             = Enum.Font.Gotham
    fpSearchBox.TextXAlignment   = Enum.TextXAlignment.Left
    fpSearchBox.ClearTextOnFocus = false
    fpSearchBox.ZIndex           = 31
    fpSearchBox.Parent           = fpPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = fpSearchBox end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = fpSearchBox end

    -- Select/Deselect All button (y=48)
    local fpSelAllBtn = Instance.new("TextButton")
    fpSelAllBtn.Size             = UDim2.new(1, -8, 0, 18)
    fpSelAllBtn.Position         = UDim2.new(0, 4, 0, 48)
    fpSelAllBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    fpSelAllBtn.BorderSizePixel  = 0
    fpSelAllBtn.Text             = "Select All"
    fpSelAllBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    fpSelAllBtn.TextSize         = 9
    fpSelAllBtn.Font             = Enum.Font.GothamBold
    fpSelAllBtn.ZIndex           = 31
    fpSelAllBtn.Parent           = fpPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = fpSelAllBtn end

    -- Player list scroll (y=70)
    local fpListScroll = Instance.new("ScrollingFrame")
    fpListScroll.Size               = UDim2.new(1, -8, 0, 72)
    fpListScroll.Position           = UDim2.new(0, 4, 0, 70)
    fpListScroll.BackgroundTransparency = 1
    fpListScroll.BorderSizePixel    = 0
    fpListScroll.ScrollBarThickness = 3
    fpListScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
    fpListScroll.CanvasSize         = UDim2.new(0, 0, 0, 0)
    fpListScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
    fpListScroll.ZIndex             = 31
    fpListScroll.Parent             = fpPopup

    local fpPopupTween = TweenInfo.new(0.18, Enum.EasingStyle.Quart)

    -- ── Helper: update picker button text ────────────────────────
    local function updateFpPickerText()
        if #fpTargets == 0 then
            fpPickerBtn.Text       = "Select player"
            fpPickerBtn.TextColor3 = Color3.fromRGB(100, 100, 100)
        elseif #fpTargets == 1 then
            fpPickerBtn.Text       = fpTargets[1].DisplayName
            fpPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            fpPickerBtn.Text       = #fpTargets .. " players"
            fpPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end

    -- ── Helper: check if player is in fpTargets ───────────────────
    local function fpIsSelected(p)
        for _, t in ipairs(fpTargets) do
            if t == p then return true end
        end
        return false
    end

    -- ── Populate player list ──────────────────────────────────────
    local function refreshFpPlayerList()
        for _, child in ipairs(fpListScroll:GetChildren()) do child:Destroy() end
        local listH = 0
        local query = fpSearchBox.Text:lower()
        local visiblePlayers = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then table.insert(visiblePlayers, p) end
            end
        end
        local allSelected = #visiblePlayers > 0
        for _, p in ipairs(visiblePlayers) do
            if not fpIsSelected(p) then allSelected = false; break end
        end
        fpSelAllBtn.Text = allSelected and "Deselect All" or "Select All"

        for _, p in ipairs(visiblePlayers) do
            do
                local isSelected = fpIsSelected(p)
                local entry = Instance.new("TextButton")
                entry.Size               = UDim2.new(1, 0, 0, 34)
                entry.Position           = UDim2.new(0, 0, 0, listH)
                entry.BackgroundColor3   = Color3.fromRGB(40, 40, 40)
                entry.BackgroundTransparency = isSelected and 0 or 1
                entry.Text               = ""
                entry.ZIndex             = 32
                entry.Parent             = fpListScroll
                if isSelected then
                    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = entry end
                end

                local dnLbl = Instance.new("TextLabel")
                dnLbl.Size               = UDim2.new(1, -4, 0, 18)
                dnLbl.Position           = UDim2.new(0, 4, 0, 2)
                dnLbl.BackgroundTransparency = 1
                dnLbl.Text               = p.DisplayName
                dnLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
                dnLbl.TextSize           = 10
                dnLbl.Font               = Enum.Font.GothamBold
                dnLbl.TextXAlignment     = Enum.TextXAlignment.Left
                dnLbl.ZIndex             = 32
                dnLbl.Parent             = entry

                local unLbl = Instance.new("TextLabel")
                unLbl.Size               = UDim2.new(1, -4, 0, 12)
                unLbl.Position           = UDim2.new(0, 4, 0, 20)
                unLbl.BackgroundTransparency = 1
                unLbl.Text               = "@" .. p.Name
                unLbl.TextColor3         = Color3.fromRGB(110, 110, 110)
                unLbl.TextSize           = 8
                unLbl.Font               = Enum.Font.Gotham
                unLbl.TextXAlignment     = Enum.TextXAlignment.Left
                unLbl.ZIndex             = 32
                unLbl.Parent             = entry

                local pRef = p
                entry.MouseButton1Click:Connect(function()
                    local found = false
                    for i, t in ipairs(fpTargets) do
                        if t == pRef then
                            table.remove(fpTargets, i)
                            fpFrozenCF[pRef] = nil  -- unfreeze that player
                            found = true
                            break
                        end
                    end
                    if not found then
                        table.insert(fpTargets, pRef)
                        -- if freeze already on, snapshot now so loop picks it up
                        if fpEnabled then snapTarget(pRef) end
                    end
                    updateFpPickerText()
                    refreshFpPlayerList()
                end)
            end
            listH = listH + 34
        end
        fpListScroll.CanvasSize         = UDim2.new(0, 0, 0, listH)
        fpListScroll.ScrollBarThickness = listH > 72 and 3 or 0
    end

    fpRefreshBtn.MouseButton1Click:Connect(refreshFpPlayerList)

    -- Select All / Deselect All
    fpSelAllBtn.MouseButton1Click:Connect(function()
        local query = fpSearchBox.Text:lower()
        local visiblePlayers = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then table.insert(visiblePlayers, p) end
            end
        end
        local allSel = #visiblePlayers > 0
        for _, p in ipairs(visiblePlayers) do
            if not fpIsSelected(p) then allSel = false; break end
        end
        if allSel then
            for _, p in ipairs(visiblePlayers) do
                for i, t in ipairs(fpTargets) do
                    if t == p then
                        table.remove(fpTargets, i)
                        fpFrozenCF[p] = nil  -- unfreeze
                        break
                    end
                end
            end
        else
            for _, p in ipairs(visiblePlayers) do
                if not fpIsSelected(p) then
                    table.insert(fpTargets, p)
                    if fpEnabled then snapTarget(p) end
                end
            end
        end
        updateFpPickerText()
        refreshFpPlayerList()
    end)

    fpSearchBox:GetPropertyChangedSignal("Text"):Connect(function()
        if fpPopupOpen then refreshFpPlayerList() end
    end)

    -- ── Picker button: toggle popup open/close ───────────────────
    fpPickerBtn.MouseButton1Click:Connect(function()
        if fpPopupOpen then
            fpPopupOpen = false
            fpPickerArrow.Text = "▲"
            TweenService:Create(fpPopup, fpPopupTween, {
                Size     = UDim2.new(0, fpPopup.AbsoluteSize.X, 0, 0),
                Position = UDim2.new(0, fpPopup.AbsolutePosition.X, 0,
                    fpPopup.AbsolutePosition.Y + fpPopup.AbsoluteSize.Y)
            }):Play()
            task.delay(0.2, function() fpPopup.Visible = false end)
        else
            fpPopupOpen = true
            fpPickerArrow.Text = "▼"
            fpSearchBox.Text   = ""
            refreshFpPlayerList()
            local rowAbs = fpRow.AbsolutePosition
            local popW   = fpRow.AbsoluteSize.X
            fpPopup.Size     = UDim2.new(0, popW, 0, 0)
            fpPopup.Position = UDim2.new(0, rowAbs.X, 0, fpPickerBtn.AbsolutePosition.Y)
            fpPopup.Visible  = true
            TweenService:Create(fpPopup, fpPopupTween, {
                Size     = UDim2.new(0, popW, 0, FP_POPUP_H),
                Position = UDim2.new(0, rowAbs.X, 0, fpPickerBtn.AbsolutePosition.Y - FP_POPUP_H)
            }):Play()
        end
    end)

    -- ── Close popup on outside touch ─────────────────────────────
    UserInputService.InputBegan:Connect(function(input)
        if not fpPopupOpen then return end
        if input.UserInputType ~= Enum.UserInputType.Touch and
           input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
        local tp = input.Position
        local pa = fpPopup.AbsolutePosition
        local ps = fpPopup.AbsoluteSize
        if tp.X >= pa.X and tp.X <= pa.X + ps.X and
           tp.Y >= pa.Y and tp.Y <= pa.Y + ps.Y then return end
        local ba = fpPickerBtn.AbsolutePosition
        local bs = fpPickerBtn.AbsoluteSize
        if tp.X >= ba.X and tp.X <= ba.X + bs.X and
           tp.Y >= ba.Y and tp.Y <= ba.Y + bs.Y then return end
        fpPopupOpen = false
        fpPickerArrow.Text = "▲"
        fpPopup.Visible = false
    end)

    -- ── Register with closeAllPopups ─────────────────────────────
    table.insert(closeAllPopups, function()
        fpPopupOpen = false
        fpPickerArrow.Text = "▲"
        fpPopup.Visible = false
    end)

    -- ════════════════════════════════════════════════════════════
    -- FOLLOW PLAYER (y=71 divider, y=76 label, y=94 row)
    -- ════════════════════════════════════════════════════════════

    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 71)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = MiscWrapper
    end

    local flwLabel = Instance.new("TextLabel")
    flwLabel.Size               = UDim2.new(1, -8, 0, 14)
    flwLabel.Position           = UDim2.new(0, 4, 0, 76)
    flwLabel.BackgroundTransparency = 1
    flwLabel.Text               = "Follow player"
    flwLabel.TextColor3         = Color3.fromRGB(255, 255, 255)
    flwLabel.TextSize           = 10
    flwLabel.Font               = Enum.Font.Gotham
    flwLabel.TextXAlignment     = Enum.TextXAlignment.Left
    flwLabel.Parent             = MiscWrapper

    -- ── State ────────────────────────────────────────────────────
    local flwEnabled  = false
    local flwTargets  = {}
    local flwDist     = 5     -- studs behind target, default 5
    local flwConn     = nil

    -- ── Toggle row (y=94) ────────────────────────────────────────
    local flwRow = Instance.new("Frame")
    flwRow.Size               = UDim2.new(1, -8, 0, 22)
    flwRow.Position           = UDim2.new(0, 4, 0, 94)
    flwRow.BackgroundTransparency = 1
    flwRow.BorderSizePixel    = 0
    flwRow.Parent             = MiscWrapper

    -- Picker button (~52%)
    local flwPickerBtn = Instance.new("TextButton")
    flwPickerBtn.Size               = UDim2.new(0.52, -3, 0, 18)
    flwPickerBtn.Position           = UDim2.new(0, 0, 0.5, -9)
    flwPickerBtn.BackgroundColor3   = Color3.fromRGB(22, 22, 22)
    flwPickerBtn.BorderSizePixel    = 0
    flwPickerBtn.Text               = "Select player"
    flwPickerBtn.TextColor3         = Color3.fromRGB(100, 100, 100)
    flwPickerBtn.TextSize           = 9
    flwPickerBtn.Font               = Enum.Font.Gotham
    flwPickerBtn.TextXAlignment     = Enum.TextXAlignment.Left
    flwPickerBtn.Parent             = flwRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = flwPickerBtn end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.PaddingRight = UDim.new(0,16); p.Parent = flwPickerBtn end

    local flwPickerArrow = Instance.new("TextLabel")
    flwPickerArrow.Size               = UDim2.new(0, 14, 1, 0)
    flwPickerArrow.Position           = UDim2.new(1, -15, 0, 0)
    flwPickerArrow.BackgroundTransparency = 1
    flwPickerArrow.Text               = "▲"
    flwPickerArrow.TextColor3         = Color3.fromRGB(100, 100, 100)
    flwPickerArrow.TextSize           = 7
    flwPickerArrow.Font               = Enum.Font.GothamBold
    flwPickerArrow.ZIndex             = 2
    flwPickerArrow.Parent             = flwPickerBtn

    -- Distance input (~23%)
    local flwDistInput = Instance.new("TextBox")
    flwDistInput.Size               = UDim2.new(0.23, -3, 0, 18)
    flwDistInput.Position           = UDim2.new(0.52, 2, 0.5, -9)
    flwDistInput.BackgroundColor3   = Color3.fromRGB(22, 22, 22)
    flwDistInput.BorderSizePixel    = 0
    flwDistInput.Text               = ""
    flwDistInput.PlaceholderText    = "dist"
    flwDistInput.TextColor3         = Color3.fromRGB(255, 255, 255)
    flwDistInput.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    flwDistInput.TextSize           = 9
    flwDistInput.Font               = Enum.Font.Gotham
    flwDistInput.ClearTextOnFocus   = false
    flwDistInput.Parent             = flwRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = flwDistInput end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,5); p.Parent = flwDistInput end

    flwDistInput:GetPropertyChangedSignal("Text"):Connect(function()
        local n = tonumber(flwDistInput.Text)
        if n and n >= 0 then flwDist = n end
    end)

    -- ON/OFF state text
    local flwStateText = Instance.new("TextLabel")
    flwStateText.Size             = UDim2.new(0, 22, 1, 0)
    flwStateText.Position         = UDim2.new(1, -60, 0, 0)
    flwStateText.BackgroundTransparency = 1
    flwStateText.Text             = "OFF"
    flwStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    flwStateText.TextSize         = 10
    flwStateText.Font             = Enum.Font.GothamBold
    flwStateText.TextXAlignment   = Enum.TextXAlignment.Right
    flwStateText.Parent           = flwRow

    -- Pill toggle
    local flwPillTrack = Instance.new("Frame")
    flwPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    flwPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    flwPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    flwPillTrack.BorderSizePixel  = 0
    flwPillTrack.Parent           = flwRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = flwPillTrack end

    local flwKnob = Instance.new("Frame")
    flwKnob.Size             = UDim2.new(0, 12, 0, 12)
    flwKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    flwKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    flwKnob.BorderSizePixel  = 0
    flwKnob.Parent           = flwPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = flwKnob end

    local flwToggleHitbox = Instance.new("TextButton")
    flwToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    flwToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    flwToggleHitbox.BackgroundTransparency = 1
    flwToggleHitbox.Text               = ""
    flwToggleHitbox.Parent             = flwRow

    local flwPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateFlwPill(on)
        if on then
            TweenService:Create(flwPillTrack, flwPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(flwKnob,      flwPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            flwStateText.Text = "ON";  flwStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(flwPillTrack, flwPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(flwKnob,      flwPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            flwStateText.Text = "OFF"; flwStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    -- ── Follow logic: move LocalPlayer behind nearest selected target ─
    local function startFollow()
        flwEnabled = true
        updateFlwPill(true)
        if flwConn then flwConn:Disconnect() end
        flwConn = RunService.Heartbeat:Connect(function()
            if #flwTargets == 0 then return end
            local myChar = LocalPlayer.Character
            local myHrp  = myChar and myChar:FindFirstChild("HumanoidRootPart")
            if not myHrp then return end
            -- Find nearest selected target
            local nearest, nearestDist = nil, math.huge
            for _, t in ipairs(flwTargets) do
                local tc  = t.Character
                local tHrp = tc and tc:FindFirstChild("HumanoidRootPart")
                if tHrp then
                    local d = (myHrp.Position - tHrp.Position).Magnitude
                    if d < nearestDist then
                        nearestDist = d
                        nearest = tHrp
                    end
                end
            end
            if nearest then
                -- +Z in CFrame space = behind the character
                pcall(function()
                    myHrp.CFrame = nearest.CFrame * CFrame.new(0, 0, flwDist)
                end)
            end
        end)
    end

    local function stopFollow()
        flwEnabled = false
        updateFlwPill(false)
        if flwConn then flwConn:Disconnect(); flwConn = nil end
    end

    flwToggleHitbox.MouseButton1Click:Connect(function()
        if flwEnabled then stopFollow() else startFollow() end
    end)

    -- ── Follow Player Popup ───────────────────────────────────────
    local FLW_POPUP_H  = 150
    local flwPopupOpen = false

    local flwPopup = Instance.new("Frame")
    flwPopup.Size             = UDim2.new(0, 150, 0, 0)
    flwPopup.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
    flwPopup.BorderSizePixel  = 0
    flwPopup.Visible          = false
    flwPopup.ClipsDescendants = true
    flwPopup.ZIndex           = 30
    flwPopup.Parent           = ScreenGui
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = flwPopup end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.75; s.Parent = flwPopup end

    local flwRefreshBtn = Instance.new("TextButton")
    flwRefreshBtn.Size             = UDim2.new(1, -8, 0, 18)
    flwRefreshBtn.Position         = UDim2.new(0, 4, 0, 4)
    flwRefreshBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    flwRefreshBtn.BorderSizePixel  = 0
    flwRefreshBtn.Text             = "↻  Refresh"
    flwRefreshBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    flwRefreshBtn.TextSize         = 9
    flwRefreshBtn.Font             = Enum.Font.GothamBold
    flwRefreshBtn.ZIndex           = 31
    flwRefreshBtn.Parent           = flwPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = flwRefreshBtn end

    local flwSearchBox = Instance.new("TextBox")
    flwSearchBox.Size             = UDim2.new(1, -8, 0, 18)
    flwSearchBox.Position         = UDim2.new(0, 4, 0, 26)
    flwSearchBox.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
    flwSearchBox.BorderSizePixel  = 0
    flwSearchBox.Text             = ""
    flwSearchBox.PlaceholderText  = "search player..."
    flwSearchBox.TextColor3       = Color3.fromRGB(255, 255, 255)
    flwSearchBox.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
    flwSearchBox.TextSize         = 9
    flwSearchBox.Font             = Enum.Font.Gotham
    flwSearchBox.TextXAlignment   = Enum.TextXAlignment.Left
    flwSearchBox.ClearTextOnFocus = false
    flwSearchBox.ZIndex           = 31
    flwSearchBox.Parent           = flwPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = flwSearchBox end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = flwSearchBox end

    local flwSelAllBtn = Instance.new("TextButton")
    flwSelAllBtn.Size             = UDim2.new(1, -8, 0, 18)
    flwSelAllBtn.Position         = UDim2.new(0, 4, 0, 48)
    flwSelAllBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    flwSelAllBtn.BorderSizePixel  = 0
    flwSelAllBtn.Text             = "Select All"
    flwSelAllBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
    flwSelAllBtn.TextSize         = 9
    flwSelAllBtn.Font             = Enum.Font.GothamBold
    flwSelAllBtn.ZIndex           = 31
    flwSelAllBtn.Parent           = flwPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = flwSelAllBtn end

    local flwListScroll = Instance.new("ScrollingFrame")
    flwListScroll.Size               = UDim2.new(1, -8, 0, 72)
    flwListScroll.Position           = UDim2.new(0, 4, 0, 70)
    flwListScroll.BackgroundTransparency = 1
    flwListScroll.BorderSizePixel    = 0
    flwListScroll.ScrollBarThickness = 3
    flwListScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
    flwListScroll.CanvasSize         = UDim2.new(0, 0, 0, 0)
    flwListScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
    flwListScroll.ZIndex             = 31
    flwListScroll.Parent             = flwPopup

    local flwPopupTween = TweenInfo.new(0.18, Enum.EasingStyle.Quart)

    local function updateFlwPickerText()
        if #flwTargets == 0 then
            flwPickerBtn.Text       = "Select player"
            flwPickerBtn.TextColor3 = Color3.fromRGB(100, 100, 100)
        elseif #flwTargets == 1 then
            flwPickerBtn.Text       = flwTargets[1].DisplayName
            flwPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        else
            flwPickerBtn.Text       = #flwTargets .. " players"
            flwPickerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end

    local function flwIsSelected(p)
        for _, t in ipairs(flwTargets) do
            if t == p then return true end
        end
        return false
    end

    local function refreshFlwPlayerList()
        for _, child in ipairs(flwListScroll:GetChildren()) do child:Destroy() end
        local listH = 0
        local query = flwSearchBox.Text:lower()
        local visPlayers = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then table.insert(visPlayers, p) end
            end
        end
        local allSel = #visPlayers > 0
        for _, p in ipairs(visPlayers) do
            if not flwIsSelected(p) then allSel = false; break end
        end
        flwSelAllBtn.Text = allSel and "Deselect All" or "Select All"

        for _, p in ipairs(visPlayers) do
            do
                local isSel = flwIsSelected(p)
                local entry = Instance.new("TextButton")
                entry.Size               = UDim2.new(1, 0, 0, 34)
                entry.Position           = UDim2.new(0, 0, 0, listH)
                entry.BackgroundColor3   = Color3.fromRGB(40, 40, 40)
                entry.BackgroundTransparency = isSel and 0 or 1
                entry.Text               = ""
                entry.ZIndex             = 32
                entry.Parent             = flwListScroll
                if isSel then
                    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = entry end
                end

                local dnLbl = Instance.new("TextLabel")
                dnLbl.Size               = UDim2.new(1, -4, 0, 18)
                dnLbl.Position           = UDim2.new(0, 4, 0, 2)
                dnLbl.BackgroundTransparency = 1
                dnLbl.Text               = p.DisplayName
                dnLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
                dnLbl.TextSize           = 10
                dnLbl.Font               = Enum.Font.GothamBold
                dnLbl.TextXAlignment     = Enum.TextXAlignment.Left
                dnLbl.ZIndex             = 32
                dnLbl.Parent             = entry

                local unLbl = Instance.new("TextLabel")
                unLbl.Size               = UDim2.new(1, -4, 0, 12)
                unLbl.Position           = UDim2.new(0, 4, 0, 20)
                unLbl.BackgroundTransparency = 1
                unLbl.Text               = "@" .. p.Name
                unLbl.TextColor3         = Color3.fromRGB(110, 110, 110)
                unLbl.TextSize           = 8
                unLbl.Font               = Enum.Font.Gotham
                unLbl.TextXAlignment     = Enum.TextXAlignment.Left
                unLbl.ZIndex             = 32
                unLbl.Parent             = entry

                local pRef = p
                entry.MouseButton1Click:Connect(function()
                    local found = false
                    for i, t in ipairs(flwTargets) do
                        if t == pRef then
                            table.remove(flwTargets, i)
                            found = true
                            break
                        end
                    end
                    if not found then
                        table.insert(flwTargets, pRef)
                    end
                    updateFlwPickerText()
                    refreshFlwPlayerList()
                end)
            end
            listH = listH + 34
        end
        flwListScroll.CanvasSize         = UDim2.new(0, 0, 0, listH)
        flwListScroll.ScrollBarThickness = listH > 72 and 3 or 0
    end

    flwRefreshBtn.MouseButton1Click:Connect(refreshFlwPlayerList)

    flwSelAllBtn.MouseButton1Click:Connect(function()
        local query = flwSearchBox.Text:lower()
        local visPlayers = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                local show = query == "" or
                    p.DisplayName:lower():find(query, 1, true) ~= nil or
                    p.Name:lower():find(query, 1, true) ~= nil
                if show then table.insert(visPlayers, p) end
            end
        end
        local allSel = #visPlayers > 0
        for _, p in ipairs(visPlayers) do
            if not flwIsSelected(p) then allSel = false; break end
        end
        if allSel then
            for _, p in ipairs(visPlayers) do
                for i, t in ipairs(flwTargets) do
                    if t == p then table.remove(flwTargets, i); break end
                end
            end
        else
            for _, p in ipairs(visPlayers) do
                if not flwIsSelected(p) then
                    table.insert(flwTargets, p)
                end
            end
        end
        updateFlwPickerText()
        refreshFlwPlayerList()
    end)

    flwSearchBox:GetPropertyChangedSignal("Text"):Connect(function()
        if flwPopupOpen then refreshFlwPlayerList() end
    end)

    flwPickerBtn.MouseButton1Click:Connect(function()
        if flwPopupOpen then
            flwPopupOpen = false
            flwPickerArrow.Text = "▲"
            TweenService:Create(flwPopup, flwPopupTween, {
                Size     = UDim2.new(0, flwPopup.AbsoluteSize.X, 0, 0),
                Position = UDim2.new(0, flwPopup.AbsolutePosition.X, 0,
                    flwPopup.AbsolutePosition.Y + flwPopup.AbsoluteSize.Y)
            }):Play()
            task.delay(0.2, function() flwPopup.Visible = false end)
        else
            flwPopupOpen = true
            flwPickerArrow.Text = "▼"
            flwSearchBox.Text   = ""
            refreshFlwPlayerList()
            local rowAbs = flwRow.AbsolutePosition
            local popW   = flwRow.AbsoluteSize.X
            flwPopup.Size     = UDim2.new(0, popW, 0, 0)
            flwPopup.Position = UDim2.new(0, rowAbs.X, 0, flwPickerBtn.AbsolutePosition.Y)
            flwPopup.Visible  = true
            TweenService:Create(flwPopup, flwPopupTween, {
                Size     = UDim2.new(0, popW, 0, FLW_POPUP_H),
                Position = UDim2.new(0, rowAbs.X, 0, flwPickerBtn.AbsolutePosition.Y - FLW_POPUP_H)
            }):Play()
        end
    end)

    -- Close on outside touch
    UserInputService.InputBegan:Connect(function(input)
        if not flwPopupOpen then return end
        if input.UserInputType ~= Enum.UserInputType.Touch and
           input.UserInputType ~= Enum.UserInputType.MouseButton1 then return end
        local tp = input.Position
        local pa = flwPopup.AbsolutePosition
        local ps = flwPopup.AbsoluteSize
        if tp.X >= pa.X and tp.X <= pa.X + ps.X and
           tp.Y >= pa.Y and tp.Y <= pa.Y + ps.Y then return end
        local ba = flwPickerBtn.AbsolutePosition
        local bs = flwPickerBtn.AbsoluteSize
        if tp.X >= ba.X and tp.X <= ba.X + bs.X and
           tp.Y >= ba.Y and tp.Y <= ba.Y + bs.Y then return end
        flwPopupOpen = false
        flwPickerArrow.Text = "▲"
        flwPopup.Visible = false
    end)

    table.insert(closeAllPopups, function()
        flwPopupOpen = false
        flwPickerArrow.Text = "▲"
        flwPopup.Visible = false
    end)

end -- end MISC



do -- === TELEPORT/POSITION (scoped) ===

    -- ── Helper: styled action button ─────────────────────────────
    local function makeTpBtn(text, posY)
        local btn = Instance.new("TextButton")
        btn.Size               = UDim2.new(1, -8, 0, 20)
        btn.Position           = UDim2.new(0, 4, 0, posY)
        btn.BackgroundColor3   = Color3.fromRGB(30, 30, 30)
        btn.BorderSizePixel    = 0
        btn.Text               = text
        btn.TextColor3         = Color3.fromRGB(255, 255, 255)
        btn.TextSize           = 10
        btn.Font               = Enum.Font.Gotham
        btn.Parent             = TeleportWrapper
        do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = btn end
        do
            local s = Instance.new("UIStroke")
            s.Color       = Color3.fromRGB(255, 255, 255)
            s.Thickness   = 1
            s.Transparency = 0.85
            s.Parent      = btn
        end
        return btn
    end

    -- ── Copy Pos button (y=27) ────────────────────────────────────
    local posInputBox  -- forward-declared; filled below

    local copyPosBtn = makeTpBtn("Copy Pos", 27)

    -- ── Position inputbox (y=51) ──────────────────────────────────
    posInputBox = Instance.new("TextBox")
    posInputBox.Size               = UDim2.new(1, -8, 0, 18)
    posInputBox.Position           = UDim2.new(0, 4, 0, 51)
    posInputBox.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    posInputBox.BorderSizePixel    = 0
    posInputBox.Text               = ""
    posInputBox.PlaceholderText    = "x, y, z"
    posInputBox.TextColor3         = Color3.fromRGB(255, 255, 255)
    posInputBox.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    posInputBox.TextSize           = 9
    posInputBox.Font               = Enum.Font.Gotham
    posInputBox.TextXAlignment     = Enum.TextXAlignment.Left
    posInputBox.ClearTextOnFocus   = false
    posInputBox.Parent             = TeleportWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = posInputBox end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = posInputBox end

    -- ── TP Pos button (y=73) ──────────────────────────────────────
    local tpPosBtn = makeTpBtn("TP Pos", 73)

    -- ── Set Waypoints button (y=97) ───────────────────────────────
    local wpOpenBtn = makeTpBtn("Set Waypoints", 97)

    -- ── Copy Pos logic: copy to clipboard + fill inputbox ────────
    copyPosBtn.MouseButton1Click:Connect(function()
        pcall(function()
            local char = LocalPlayer.Character
            local hrp  = char and char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            local pos = hrp.Position
            local str = math.round(pos.X) .. ", " .. math.round(pos.Y) .. ", " .. math.round(pos.Z)
            setclipboard(str)
            posInputBox.Text = str
        end)
    end)

    -- ── TP Pos logic: parse "x, y, z" and teleport ───────────────
    tpPosBtn.MouseButton1Click:Connect(function()
        pcall(function()
            local txt  = posInputBox.Text
            local nums = {}
            for n in txt:gmatch("[%-]?%d+%.?%d*") do
                table.insert(nums, tonumber(n))
                if #nums >= 3 then break end
            end
            if #nums < 3 then return end
            local char = LocalPlayer.Character
            local hrp  = char and char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            hrp.CFrame = CFrame.new(nums[1], nums[2], nums[3])
        end)
    end)

    -- ══════════════════════════════════════════════════════════
    -- WAYPOINTS POPUP (draggable, close only via close button)
    -- ══════════════════════════════════════════════════════════
    local WP_W      = 200
    local WP_H      = 215
    -- AFTER:
    local wpVisible = false
    local waypoints = {}  -- { name=string, pos=Vector3 }
    
    -- Load dari file (cross-session) atau _G (same-session re-exec)
    do
      local ok, fromFile = pcall(function()
        if not readfile then return nil end
        local raw = readfile("vort_waypoints_" .. tostring(game.PlaceId) .. ".json")
        if not raw or raw == "" then return nil end
        return HttpService:JSONDecode(raw)
      end)
      if ok and type(fromFile) == "table" then
        for _, wp in ipairs(fromFile) do
            local p = wp.pos or {}
            table.insert(waypoints, {
                name = wp.name,
                pos  = Vector3.new(p.x or 0, p.y or 0, p.z or 0)
            })
        end
      elseif type(_G._VORT_WP) == "table" then
        for _, wp in ipairs(_G._VORT_WP) do
            table.insert(waypoints, { name = wp.name, pos = wp.pos })
        end
      end
    end
    
    local function saveWaypoints()
      -- _G backup: same-session re-exec
      _G._VORT_WP = {}
      for _, wp in ipairs(waypoints) do
        table.insert(_G._VORT_WP, { name = wp.name, pos = wp.pos })
      end
      -- File backup: cross-session (kalau executor support writefile)
      pcall(function()
        if not writefile then return end
        local t = {}
        for _, wp in ipairs(waypoints) do
            table.insert(t, { name = wp.name,
                pos = { x = wp.pos.X, y = wp.pos.Y, z = wp.pos.Z } })
        end
        writefile("vort_waypoints_" .. tostring(game.PlaceId) .. ".json", HttpService:JSONEncode(t))
      end)
    end

    local wpPopup = Instance.new("Frame")
    wpPopup.Name             = "WaypointsPopup"
    wpPopup.Size             = UDim2.new(0, WP_W, 0, WP_H)
    wpPopup.Position         = UDim2.new(0.5, -WP_W/2, 0.5, -WP_H/2)
    wpPopup.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
    wpPopup.BorderSizePixel  = 0
    wpPopup.Visible          = false
    wpPopup.ClipsDescendants = true
    wpPopup.ZIndex           = 40
    wpPopup.Parent           = ScreenGui
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = wpPopup end
    do local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(255,255,255); s.Thickness = 1; s.Transparency = 0.75; s.Parent = wpPopup end

    -- ── Header bar (drag handle + title + close) ──────────────────
    local wpHeaderBar = Instance.new("Frame")
    wpHeaderBar.Size             = UDim2.new(1, 0, 0, 22)
    wpHeaderBar.Position         = UDim2.new(0, 0, 0, 0)
    wpHeaderBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    wpHeaderBar.BorderSizePixel  = 0
    wpHeaderBar.ZIndex           = 41
    wpHeaderBar.Parent           = wpPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = wpHeaderBar end
    do
        local fix = Instance.new("Frame")
        fix.Size             = UDim2.new(1, 0, 0.5, 0)
        fix.Position         = UDim2.new(0, 0, 0.5, 0)
        fix.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
        fix.BorderSizePixel  = 0
        fix.ZIndex           = 41
        fix.Parent           = wpHeaderBar
    end

    local wpTitleLbl = Instance.new("TextLabel")
    wpTitleLbl.Size               = UDim2.new(1, -28, 1, 0)
    wpTitleLbl.Position           = UDim2.new(0, 8, 0, 0)
    wpTitleLbl.BackgroundTransparency = 1
    wpTitleLbl.Text               = "Waypoints"
    wpTitleLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    wpTitleLbl.TextSize           = 10
    wpTitleLbl.Font               = Enum.Font.GothamBold
    wpTitleLbl.TextXAlignment     = Enum.TextXAlignment.Left
    wpTitleLbl.ZIndex             = 42
    wpTitleLbl.Parent             = wpHeaderBar

    local wpCloseBtn = Instance.new("TextButton")
    wpCloseBtn.Size               = UDim2.new(0, 18, 0, 18)
    wpCloseBtn.Position           = UDim2.new(1, -20, 0.5, -9)
    wpCloseBtn.BackgroundTransparency = 1
    wpCloseBtn.Text               = "x"
    wpCloseBtn.TextColor3         = Color3.fromRGB(255, 255, 255)
    wpCloseBtn.TextSize           = 10
    wpCloseBtn.Font               = Enum.Font.GothamBold
    wpCloseBtn.ZIndex             = 42
    wpCloseBtn.Parent             = wpHeaderBar

    -- ── Drag logic for waypoints popup ───────────────────────────
    do
        local wpDrag, wpDragSt, wpDragPos = false, nil, nil

        local function startWpDrag(inp)
            if inp.UserInputType == Enum.UserInputType.Touch or
               inp.UserInputType == Enum.UserInputType.MouseButton1 then
                wpDrag    = true
                wpDragSt  = inp.Position
                wpDragPos = wpPopup.Position
            end
        end
        local function endWpDrag(inp)
            if inp.UserInputType == Enum.UserInputType.Touch or
               inp.UserInputType == Enum.UserInputType.MouseButton1 then
                wpDrag = false
            end
        end

        -- Header drag (existing)
        wpHeaderBar.InputBegan:Connect(startWpDrag)
        wpHeaderBar.InputEnded:Connect(endWpDrag)

        -- Bottom-left corner drag handle
        local wpBL = Instance.new("TextButton")
        wpBL.Size                = UDim2.new(0, 24, 0, 24)
        wpBL.Position            = UDim2.new(0, 0, 1, -24)
        wpBL.BackgroundTransparency = 1
        wpBL.Text                = ""
        wpBL.ZIndex              = 50
        wpBL.Parent              = wpPopup
        wpBL.InputBegan:Connect(startWpDrag)
        wpBL.InputEnded:Connect(endWpDrag)

        -- Bottom-right corner drag handle
        local wpBR = Instance.new("TextButton")
        wpBR.Size                = UDim2.new(0, 24, 0, 24)
        wpBR.Position            = UDim2.new(1, -24, 1, -24)
        wpBR.BackgroundTransparency = 1
        wpBR.Text                = ""
        wpBR.ZIndex              = 50
        wpBR.Parent              = wpPopup
        wpBR.InputBegan:Connect(startWpDrag)
        wpBR.InputEnded:Connect(endWpDrag)

        -- Global movement handler (shared semua drag sources)
        UserInputService.InputChanged:Connect(function(inp)
            if not wpDrag then return end
            if inp.UserInputType ~= Enum.UserInputType.Touch and
               inp.UserInputType ~= Enum.UserInputType.MouseMovement then return end
            local d = inp.Position - wpDragSt
            wpPopup.Position = UDim2.new(
                wpDragPos.X.Scale, wpDragPos.X.Offset + d.X,
                wpDragPos.Y.Scale, wpDragPos.Y.Offset + d.Y
            )
        end)
        UserInputService.InputEnded:Connect(endWpDrag)
    end

    -- ── Header divider (y=22) ────────────────────────────────────
    do
        local div = Instance.new("Frame")
        div.Size             = UDim2.new(1, 0, 0, 1)
        div.Position         = UDim2.new(0, 0, 0, 22)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel  = 0
        div.ZIndex           = 41
        div.Parent           = wpPopup
    end

    -- ── Name input + Set WP button (y=27) ────────────────────────
    local wpNameInput = Instance.new("TextBox")
    wpNameInput.Size               = UDim2.new(1, -70, 0, 20)
    wpNameInput.Position           = UDim2.new(0, 4, 0, 27)
    wpNameInput.BackgroundColor3   = Color3.fromRGB(25, 25, 25)
    wpNameInput.BorderSizePixel    = 0
    wpNameInput.Text               = ""
    wpNameInput.PlaceholderText    = "waypoint name..."
    wpNameInput.TextColor3         = Color3.fromRGB(255, 255, 255)
    wpNameInput.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    wpNameInput.TextSize           = 9
    wpNameInput.Font               = Enum.Font.Gotham
    wpNameInput.TextXAlignment     = Enum.TextXAlignment.Left
    wpNameInput.ClearTextOnFocus   = false
    wpNameInput.ZIndex             = 41
    wpNameInput.Parent             = wpPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = wpNameInput end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = wpNameInput end

    local wpSetBtn = Instance.new("TextButton")
    wpSetBtn.Size               = UDim2.new(0, 60, 0, 20)
    wpSetBtn.Position           = UDim2.new(1, -64, 0, 27)
    wpSetBtn.BackgroundColor3   = Color3.fromRGB(35, 35, 35)
    wpSetBtn.BorderSizePixel    = 0
    wpSetBtn.Text               = "Set WP"
    wpSetBtn.TextColor3         = Color3.fromRGB(255, 255, 255)
    wpSetBtn.TextSize           = 9
    wpSetBtn.Font               = Enum.Font.GothamBold
    wpSetBtn.ZIndex             = 41
    wpSetBtn.Parent             = wpPopup
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = wpSetBtn end

    -- ── Divider between input and list (y=51) ────────────────────
    do
        local div = Instance.new("Frame")
        div.Size             = UDim2.new(1, 0, 0, 1)
        div.Position         = UDim2.new(0, 0, 0, 51)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel  = 0
        div.ZIndex           = 41
        div.Parent           = wpPopup
    end

    -- ── Waypoints list scroll (y=55) ─────────────────────────────
    local WP_LIST_H = WP_H - 59
    local wpScroll = Instance.new("ScrollingFrame")
    wpScroll.Size               = UDim2.new(1, -8, 0, WP_LIST_H)
    wpScroll.Position           = UDim2.new(0, 4, 0, 55)
    wpScroll.BackgroundTransparency = 1
    wpScroll.BorderSizePixel    = 0
    wpScroll.ScrollBarThickness = 3
    wpScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
    wpScroll.CanvasSize         = UDim2.new(0, 0, 0, 0)
    wpScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
    wpScroll.ZIndex             = 41
    wpScroll.Parent             = wpPopup

    -- ── Re-render waypoints list ─────────────────────────────────
    local function renderWaypoints()
        for _, ch in ipairs(wpScroll:GetChildren()) do ch:Destroy() end
        local totalH = 0
        for i, wp in ipairs(waypoints) do
            do
                local ef = Instance.new("Frame")
                ef.Size               = UDim2.new(1, 0, 0, 26)
                ef.Position           = UDim2.new(0, 0, 0, totalH)
                ef.BackgroundTransparency = 1
                ef.BorderSizePixel    = 0
                ef.ZIndex             = 42
                ef.Parent             = wpScroll

                local nl = Instance.new("TextLabel")
                nl.Size               = UDim2.new(1, -110, 1, 0)
                nl.Position           = UDim2.new(0, 4, 0, 0)
                nl.BackgroundTransparency = 1
                nl.Text               = wp.name
                nl.TextColor3         = Color3.fromRGB(255, 255, 255)
                nl.TextSize           = 9
                nl.Font               = Enum.Font.Gotham
                nl.TextXAlignment     = Enum.TextXAlignment.Left
                nl.TextTruncate       = Enum.TextTruncate.AtEnd
                nl.ZIndex             = 42
                nl.Parent             = ef

                local gb = Instance.new("TextButton")
                gb.Size               = UDim2.new(0, 44, 0, 18)
                gb.Position           = UDim2.new(1, -96, 0.5, -9)
                gb.BackgroundColor3   = Color3.fromRGB(35, 35, 35)
                gb.BorderSizePixel    = 0
                gb.Text               = "Goto"
                gb.TextColor3         = Color3.fromRGB(255, 255, 255)
                gb.TextSize           = 9
                gb.Font               = Enum.Font.Gotham
                gb.ZIndex             = 42
                gb.Parent             = ef
                do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = gb end

                local db = Instance.new("TextButton")
                db.Size               = UDim2.new(0, 44, 0, 18)
                db.Position           = UDim2.new(1, -48, 0.5, -9)
                db.BackgroundColor3   = Color3.fromRGB(35, 35, 35)
                db.BorderSizePixel    = 0
                db.Text               = "Del"
                db.TextColor3         = Color3.fromRGB(255, 255, 255)
                db.TextSize           = 9
                db.Font               = Enum.Font.Gotham
                db.ZIndex             = 42
                db.Parent             = ef
                do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = db end

                local wpRef  = wp
                local idxRef = i
                gb.MouseButton1Click:Connect(function()
                    pcall(function()
                        local char = LocalPlayer.Character
                        local hrp  = char and char:FindFirstChild("HumanoidRootPart")
                        if hrp then hrp.CFrame = CFrame.new(wpRef.pos) end
                    end)
                end)
                db.MouseButton1Click:Connect(function()
                    table.remove(waypoints, idxRef)
                    saveWaypoints()
                    renderWaypoints()
                end)
            end
            totalH = totalH + 26
        end
        wpScroll.CanvasSize         = UDim2.new(0, 0, 0, totalH)
        wpScroll.ScrollBarThickness = totalH > WP_LIST_H and 3 or 0
    end

    -- ── Set waypoint: save current position with label ────────────
    wpSetBtn.MouseButton1Click:Connect(function()
        pcall(function()
            local name = wpNameInput.Text
            if name == "" then return end
            local char = LocalPlayer.Character
            local hrp  = char and char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            table.insert(waypoints, { name = name, pos = hrp.Position })
            saveWaypoints()
            wpNameInput.Text = ""
            renderWaypoints()
        end)
    end)

    -- ── Close button ──────────────────────────────────────────────
    wpCloseBtn.MouseButton1Click:Connect(function()
        wpVisible = false
        wpPopup.Visible = false
    end)

    -- ── Toggle popup open/close from button ───────────────────────
    wpOpenBtn.MouseButton1Click:Connect(function()
        wpVisible = not wpVisible
        wpPopup.Visible = wpVisible
        if wpVisible then
            -- open centered on screen
            wpPopup.Position = UDim2.new(0.5, -WP_W/2, 0.5, -WP_H/2)
            renderWaypoints()
        end
    end)
end -- end TELEPORT/POSITION

do -- === ANTI/PROTECTION (scoped) ===

    -- ════════════════════════════════════════════
    -- ANTI AFK (y=27)
    -- ════════════════════════════════════════════
    local afkEnabled = false
    local afkConn    = nil

    local afkRow = Instance.new("Frame")
    afkRow.Size               = UDim2.new(1, -8, 0, 22)
    afkRow.Position           = UDim2.new(0, 4, 0, 27)
    afkRow.BackgroundTransparency = 1
    afkRow.BorderSizePixel    = 0
    afkRow.Parent             = AntiWrapper

    local afkLbl = Instance.new("TextLabel")
    afkLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    afkLbl.BackgroundTransparency = 1
    afkLbl.Text               = "Anti AFK"
    afkLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    afkLbl.TextSize           = 10
    afkLbl.Font               = Enum.Font.Gotham
    afkLbl.TextXAlignment     = Enum.TextXAlignment.Left
    afkLbl.Parent             = afkRow

    local afkStateText = Instance.new("TextLabel")
    afkStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    afkStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    afkStateText.BackgroundTransparency = 1
    afkStateText.Text             = "OFF"
    afkStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    afkStateText.TextSize         = 10
    afkStateText.Font             = Enum.Font.GothamBold
    afkStateText.TextXAlignment   = Enum.TextXAlignment.Right
    afkStateText.Parent           = afkRow

    local afkPillTrack = Instance.new("Frame")
    afkPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    afkPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    afkPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    afkPillTrack.BorderSizePixel  = 0
    afkPillTrack.Parent           = afkRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = afkPillTrack end

    local afkKnob = Instance.new("Frame")
    afkKnob.Size             = UDim2.new(0, 12, 0, 12)
    afkKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    afkKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    afkKnob.BorderSizePixel  = 0
    afkKnob.Parent           = afkPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = afkKnob end

    local afkToggleHitbox = Instance.new("TextButton")
    afkToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    afkToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    afkToggleHitbox.BackgroundTransparency = 1
    afkToggleHitbox.Text               = ""
    afkToggleHitbox.Parent             = afkRow

    local afkPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateAfkPill(on)
        if on then
            TweenService:Create(afkPillTrack, afkPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(afkKnob, afkPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            afkStateText.Text = "ON";  afkStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(afkPillTrack, afkPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(afkKnob, afkPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            afkStateText.Text = "OFF"; afkStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    afkToggleHitbox.MouseButton1Click:Connect(function()
        afkEnabled = not afkEnabled
        updateAfkPill(afkEnabled)
        if afkEnabled then
            -- IY ref: listen to Idled → VirtualUser CaptureController
            afkConn = LocalPlayer.Idled:Connect(function()
                pcall(function()
                    local vu = game:GetService("VirtualUser")
                    vu:CaptureController()
                    vu:ClickButton2(Vector2.new())
                end)
            end)
        else
            if afkConn then afkConn:Disconnect(); afkConn = nil end
        end
    end)

    -- DEFAULT ON: auto-enable Anti AFK on script execute
    do
        afkEnabled = true
        updateAfkPill(true)
        afkConn = LocalPlayer.Idled:Connect(function()
            pcall(function()
                local vu = game:GetService("VirtualUser")
                vu:CaptureController()
                vu:ClickButton2(Vector2.new())
            end)
        end)
    end

    -- ════════════════════════════════════════════
    -- ANTI KICK (y=56)
    -- ════════════════════════════════════════════
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 52)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = AntiWrapper
    end

    local akEnabled  = false
    local akHooked   = false  -- hookmetamethod can't be undone

    local akRow = Instance.new("Frame")
    akRow.Size               = UDim2.new(1, -8, 0, 22)
    akRow.Position           = UDim2.new(0, 4, 0, 56)
    akRow.BackgroundTransparency = 1
    akRow.BorderSizePixel    = 0
    akRow.Parent             = AntiWrapper

    local akLbl = Instance.new("TextLabel")
    akLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    akLbl.BackgroundTransparency = 1
    akLbl.Text               = "Anti Kick"
    akLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    akLbl.TextSize           = 10
    akLbl.Font               = Enum.Font.Gotham
    akLbl.TextXAlignment     = Enum.TextXAlignment.Left
    akLbl.Parent             = akRow

    local akStateText = Instance.new("TextLabel")
    akStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    akStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    akStateText.BackgroundTransparency = 1
    akStateText.Text             = "OFF"
    akStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    akStateText.TextSize         = 10
    akStateText.Font             = Enum.Font.GothamBold
    akStateText.TextXAlignment   = Enum.TextXAlignment.Right
    akStateText.Parent           = akRow

    local akPillTrack = Instance.new("Frame")
    akPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    akPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    akPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    akPillTrack.BorderSizePixel  = 0
    akPillTrack.Parent           = akRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = akPillTrack end

    local akKnob = Instance.new("Frame")
    akKnob.Size             = UDim2.new(0, 12, 0, 12)
    akKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    akKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    akKnob.BorderSizePixel  = 0
    akKnob.Parent           = akPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = akKnob end

    local akToggleHitbox = Instance.new("TextButton")
    akToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    akToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    akToggleHitbox.BackgroundTransparency = 1
    akToggleHitbox.Text               = ""
    akToggleHitbox.Parent             = akRow

    local akPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateAkPill(on)
        if on then
            TweenService:Create(akPillTrack, akPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(akKnob, akPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            akStateText.Text = "ON";  akStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(akPillTrack, akPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(akKnob, akPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            akStateText.Text = "OFF"; akStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    akToggleHitbox.MouseButton1Click:Connect(function()
        akEnabled = not akEnabled
        updateAkPill(akEnabled)
        if akEnabled and not akHooked then
            -- IY ref: hookmetamethod to block Kick calls
            -- NOTE: hookmetamethod is one-time — turning OFF only updates UI, hook stays active
            pcall(function()
                if not hookmetamethod then return end
                local lp = LocalPlayer
                if hookfunction then
                    hookfunction(lp.Kick, function() end)
                end
                local oldIdx; oldIdx = hookmetamethod(game, "__index", function(self, method)
                    if self == lp and method:lower() == "kick" then
                        return error("Expected ':' not '.' calling member function Kick", 2)
                    end
                    return oldIdx(self, method)
                end)
                local oldNC; oldNC = hookmetamethod(game, "__namecall", function(self, ...)
                    if self == lp and getnamecallmethod():lower() == "kick" then return end
                    return oldNC(self, ...)
                end)
                akHooked = true
            end)
        end
        -- OFF: hooks stay active (can't un-hook), UI reflects toggled state
    end)

    -- DEFAULT ON: auto-enable Anti Kick on script execute
    do
        akEnabled = true
        updateAkPill(true)
        if not akHooked then
            pcall(function()
                if not hookmetamethod then return end
                local lp = LocalPlayer
                if hookfunction then
                    hookfunction(lp.Kick, function() end)
                end
                local oldIdx2; oldIdx2 = hookmetamethod(game, "__index", function(self, method)
                    if self == lp and method:lower() == "kick" then
                        return error("Expected ':' not '.' calling member function Kick", 2)
                    end
                    return oldIdx2(self, method)
                end)
                local oldNC2; oldNC2 = hookmetamethod(game, "__namecall", function(self, ...)
                    if self == lp and getnamecallmethod():lower() == "kick" then return end
                    return oldNC2(self, ...)
                end)
                akHooked = true
            end)
        end
    end

    -- ════════════════════════════════════════════
    -- ANTI GAMEPLAY PAUSED (y=85)
    -- ════════════════════════════════════════════
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 81)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = AntiWrapper
    end

    local agpEnabled = false
    local agpConn    = nil

    local agpRow = Instance.new("Frame")
    agpRow.Size               = UDim2.new(1, -8, 0, 22)
    agpRow.Position           = UDim2.new(0, 4, 0, 85)
    agpRow.BackgroundTransparency = 1
    agpRow.BorderSizePixel    = 0
    agpRow.Parent             = AntiWrapper

    local agpLbl = Instance.new("TextLabel")
    agpLbl.Size               = UDim2.new(0.55, 0, 1, 0)
    agpLbl.BackgroundTransparency = 1
    agpLbl.Text               = "Anti GP Pause"
    agpLbl.TextColor3         = Color3.fromRGB(255, 255, 255)
    agpLbl.TextSize           = 10
    agpLbl.Font               = Enum.Font.Gotham
    agpLbl.TextXAlignment     = Enum.TextXAlignment.Left
    agpLbl.Parent             = agpRow

    local agpStateText = Instance.new("TextLabel")
    agpStateText.Size             = UDim2.new(0.18, 0, 1, 0)
    agpStateText.Position         = UDim2.new(0.55, 0, 0, 0)
    agpStateText.BackgroundTransparency = 1
    agpStateText.Text             = "OFF"
    agpStateText.TextColor3       = Color3.fromRGB(160, 160, 160)
    agpStateText.TextSize         = 10
    agpStateText.Font             = Enum.Font.GothamBold
    agpStateText.TextXAlignment   = Enum.TextXAlignment.Right
    agpStateText.Parent           = agpRow

    local agpPillTrack = Instance.new("Frame")
    agpPillTrack.Size             = UDim2.new(0, 34, 0, 16)
    agpPillTrack.Position         = UDim2.new(1, -34, 0.5, -8)
    agpPillTrack.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    agpPillTrack.BorderSizePixel  = 0
    agpPillTrack.Parent           = agpRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = agpPillTrack end

    local agpKnob = Instance.new("Frame")
    agpKnob.Size             = UDim2.new(0, 12, 0, 12)
    agpKnob.Position         = UDim2.new(0, 2, 0.5, -6)
    agpKnob.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    agpKnob.BorderSizePixel  = 0
    agpKnob.Parent           = agpPillTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = agpKnob end

    local agpToggleHitbox = Instance.new("TextButton")
    agpToggleHitbox.Size               = UDim2.new(0, 34, 0, 16)
    agpToggleHitbox.Position           = UDim2.new(1, -34, 0.5, -8)
    agpToggleHitbox.BackgroundTransparency = 1
    agpToggleHitbox.Text               = ""
    agpToggleHitbox.Parent             = agpRow

    local agpPillTween = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
    local function updateAgpPill(on)
        if on then
            TweenService:Create(agpPillTrack, agpPillTween, {BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            TweenService:Create(agpKnob, agpPillTween, {Position = UDim2.new(0,20,0.5,-6), BackgroundColor3 = Color3.fromRGB(0,0,0)}):Play()
            agpStateText.Text = "ON";  agpStateText.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(agpPillTrack, agpPillTween, {BackgroundColor3 = Color3.fromRGB(60,60,60)}):Play()
            TweenService:Create(agpKnob, agpPillTween, {Position = UDim2.new(0,2,0.5,-6), BackgroundColor3 = Color3.fromRGB(200,200,200)}):Play()
            agpStateText.Text = "OFF"; agpStateText.TextColor3 = Color3.fromRGB(160,160,160)
        end
    end

    agpToggleHitbox.MouseButton1Click:Connect(function()
        agpEnabled = not agpEnabled
        updateAgpPill(agpEnabled)
        if agpEnabled then
            -- IY ref: antigameplaypaused — destroy NetworkPause obj + block future ones
            pcall(function() if agpConn then agpConn:Disconnect() end end)
            pcall(function()
                local rg = game:GetService("CoreGui"):FindFirstChild("RobloxGui")
                if not rg then return end
                local existing = rg:FindFirstChild("CoreScripts/NetworkPause")
                if existing then existing:Destroy() end
                agpConn = rg.ChildAdded:Connect(function(obj)
                    if agpEnabled and obj.Name == "CoreScripts/NetworkPause" then
                        obj:Destroy()
                    end
                end)
            end)
        else
            -- IY ref: unantigameplaypaused — disconnect listener
            pcall(function() if agpConn then agpConn:Disconnect(); agpConn = nil end end)
        end
    end)

    -- DEFAULT ON: auto-enable Anti GP Pause on script execute
    do
        agpEnabled = true
        updateAgpPill(true)
        pcall(function()
            local rg = game:GetService("CoreGui"):FindFirstChild("RobloxGui")
            if not rg then return end
            local existing = rg:FindFirstChild("CoreScripts/NetworkPause")
            if existing then existing:Destroy() end
            agpConn = rg.ChildAdded:Connect(function(obj)
                if agpEnabled and obj.Name == "CoreScripts/NetworkPause" then
                    obj:Destroy()
                end
            end)
        end)
    end

    -- cfg: anti-afk
    cfgRead["afk"] = function()
        return {en=afkEnabled}
    end
    cfgApply["afk"] = function(t)
        local wantOn = (t.en == true)
        if afkEnabled ~= wantOn then
            afkEnabled = wantOn
            updateAfkPill(afkEnabled)
            if afkEnabled then
                if afkConn then afkConn:Disconnect() end
                afkConn = LocalPlayer.Idled:Connect(function()
                    pcall(function()
                        local vu = game:GetService("VirtualUser")
                        vu:CaptureController(); vu:ClickButton2(Vector2.new())
                    end)
                end)
            else
                if afkConn then afkConn:Disconnect(); afkConn = nil end
            end
        end
    end

    -- cfg: anti-kick
    cfgRead["ak"] = function()
        return {en=akEnabled}
    end
    cfgApply["ak"] = function(t)
        local wantOn = (t.en == true)
        if wantOn and not akEnabled then
            akEnabled = true
            updateAkPill(true)
            if not akHooked then
                pcall(function()
                    if not hookmetamethod then return end
                    local lp3 = LocalPlayer
                    if hookfunction then hookfunction(lp3.Kick, function() end) end
                    local oldI3; oldI3 = hookmetamethod(game, "__index", function(s, m)
                        if s == lp3 and m:lower() == "kick" then
                            return error("Expected ':' not '.' calling member function Kick", 2)
                        end
                        return oldI3(s, m)
                    end)
                    local oldN3; oldN3 = hookmetamethod(game, "__namecall", function(s, ...)
                        if s == lp3 and getnamecallmethod():lower() == "kick" then return end
                        return oldN3(s, ...)
                    end)
                    akHooked = true
                end)
            end
        elseif (not wantOn) and akEnabled then
            -- hook stays active; only update visual
            akEnabled = false
            updateAkPill(false)
        end
    end

    -- cfg: anti-gameplay-paused
    cfgRead["agp"] = function()
        return {en=agpEnabled}
    end
    cfgApply["agp"] = function(t)
        local wantOn = (t.en == true)
        if agpEnabled ~= wantOn then
            agpEnabled = wantOn
            updateAgpPill(agpEnabled)
            if agpEnabled then
                pcall(function() if agpConn then agpConn:Disconnect() end end)
                pcall(function()
                    local rg = game:GetService("CoreGui"):FindFirstChild("RobloxGui")
                    if not rg then return end
                    local ex = rg:FindFirstChild("CoreScripts/NetworkPause")
                    if ex then ex:Destroy() end
                    agpConn = rg.ChildAdded:Connect(function(obj)
                        if agpEnabled and obj.Name == "CoreScripts/NetworkPause" then
                            obj:Destroy()
                        end
                    end)
                end)
            else
                pcall(function()
                    if agpConn then agpConn:Disconnect(); agpConn = nil end
                end)
            end
        end
    end

end -- end ANTI/PROTECTION

-- ========================
-- SERVER/NETWORKING FEATURES
-- IY ref: jobid, copyplaceid, rejoin, serverhop, TeleportToPlaceInstance
-- ========================

do -- === SERVER/NETWORKING (scoped) ===

    -- ── Helper: styled action button ────────────────────────────
    local function makeActionBtn(parent, text, posY)
        local btn = Instance.new("TextButton")
        btn.Size               = UDim2.new(1, -8, 0, 20)
        btn.Position           = UDim2.new(0, 4, 0, posY)
        btn.BackgroundColor3   = Color3.fromRGB(30, 30, 30)
        btn.BorderSizePixel    = 0
        btn.Text               = text
        btn.TextColor3         = Color3.fromRGB(255, 255, 255)
        btn.TextSize           = 10
        btn.Font               = Enum.Font.Gotham
        btn.Parent             = parent
        do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = btn end
        do
            local s = Instance.new("UIStroke")
            s.Color       = Color3.fromRGB(255, 255, 255)
            s.Thickness   = 1
            s.Transparency = 0.85
            s.Parent      = btn
        end
        return btn
    end

    -- ── Copy JobId (y=27) ─────────────────────────────────────────
    -- IY ref: jobid → toClipboard("roblox://placeId=..&gameInstanceId=..")
    local copyJobBtn = makeActionBtn(ServerWrapper, "Copy Job ID", 27)
    copyJobBtn.MouseButton1Click:Connect(function()
        pcall(function()
            setclipboard(tostring(game.JobId))
        end)
    end)

    -- ── Copy PlaceId (y=52) ───────────────────────────────────────
    -- IY ref: copyplaceid / placeid
    local copyPlaceBtn = makeActionBtn(ServerWrapper, "Copy Place ID", 52)
    copyPlaceBtn.MouseButton1Click:Connect(function()
        pcall(function()
            setclipboard(tostring(game.PlaceId))
        end)
    end)

    -- ── Rejoin (y=77) ─────────────────────────────────────────────
    -- IY ref: rejoin / rj
    local rejoinBtn = makeActionBtn(ServerWrapper, "Rejoin Server", 77)
    rejoinBtn.MouseButton1Click:Connect(function()
        pcall(function()
            if #Players:GetPlayers() <= 1 then
                LocalPlayer:Kick("\nRejoining...")
                task.wait()
                TeleportService:Teleport(game.PlaceId, LocalPlayer)
            else
                TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer)
            end
        end)
    end)

    -- ── ServerHop (y=102) ─────────────────────────────────────────
    -- IY ref: serverhop — picks server with LOWEST player count
    local shopBtn = makeActionBtn(ServerWrapper, "Server Hop", 102)
    shopBtn.MouseButton1Click:Connect(function()
        shopBtn.Text = "Hopping..."
        task.spawn(function()
            pcall(function()
                local url = "https://games.roblox.com/v1/games/"
                    .. game.PlaceId
                    .. "/servers/Public?sortOrder=Asc&limit=100&excludeFullGames=true"
                local ok, raw = pcall(function() return game:HttpGet(url) end)
                if not ok then shopBtn.Text = "Server Hop"; return end
                local body = HttpService:JSONDecode(raw)
                local servers = {}
                if body and body.data then
                    for _, v in next, body.data do
                        if type(v) == "table"
                           and tonumber(v.playing)
                           and tonumber(v.maxPlayers)
                           and v.playing < v.maxPlayers
                           and v.id ~= game.JobId then
                            table.insert(servers, {id = v.id, playing = tonumber(v.playing)})
                        end
                    end
                end
                if #servers > 0 then
                    -- sort ascending → pick server with fewest players
                    table.sort(servers, function(a, b) return a.playing < b.playing end)
                    TeleportService:TeleportToPlaceInstance(game.PlaceId, servers[1].id, LocalPlayer)
                else
                    shopBtn.Text = "No Server Found"
                    task.delay(2, function() shopBtn.Text = "Server Hop" end)
                end
            end)
        end)
    end)

    -- ── Thin divider before Jump to Server section ───────────────
    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 126)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel = 0
        div.Parent = ServerWrapper
    end

    -- ── Job ID input label (y=130) ────────────────────────────────
    local jtsLbl = Instance.new("TextLabel")
    jtsLbl.Size               = UDim2.new(1, -8, 0, 12)
    jtsLbl.Position           = UDim2.new(0, 4, 0, 130)
    jtsLbl.BackgroundTransparency = 1
    jtsLbl.Text               = "Jump to Server  (Job ID)"
    jtsLbl.TextColor3         = Color3.fromRGB(150, 150, 150)
    jtsLbl.TextSize           = 9
    jtsLbl.Font               = Enum.Font.Gotham
    jtsLbl.TextXAlignment     = Enum.TextXAlignment.Left
    jtsLbl.Parent             = ServerWrapper

    -- ── Job ID inputbox (y=145) ───────────────────────────────────
    local jtsInput = Instance.new("TextBox")
    jtsInput.Size               = UDim2.new(1, -8, 0, 18)
    jtsInput.Position           = UDim2.new(0, 4, 0, 145)
    jtsInput.BackgroundColor3   = Color3.fromRGB(20, 20, 20)
    jtsInput.BorderSizePixel    = 0
    jtsInput.Text               = ""
    jtsInput.PlaceholderText    = "Paste Job ID here..."
    jtsInput.TextColor3         = Color3.fromRGB(255, 255, 255)
    jtsInput.PlaceholderColor3  = Color3.fromRGB(80, 80, 80)
    jtsInput.TextSize           = 9
    jtsInput.Font               = Enum.Font.Gotham
    jtsInput.TextXAlignment     = Enum.TextXAlignment.Left
    jtsInput.ClearTextOnFocus   = false
    jtsInput.Parent             = ServerWrapper
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = jtsInput end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = jtsInput end

    -- ── Jump to Server button (y=168) ─────────────────────────────
    local jtsBtn = makeActionBtn(ServerWrapper, "Jump to Server", 168)
    jtsBtn.MouseButton1Click:Connect(function()
        local targetJobId = jtsInput.Text
        if not targetJobId or targetJobId == "" then return end
        pcall(function()
            TeleportService:TeleportToPlaceInstance(game.PlaceId, targetJobId, LocalPlayer)
        end)
    end)

end -- end SERVER/NETWORKING

-- ========================
-- AUTO FARM TAB (Tab3)
-- Speed Farm: Auto Treadmill + Auto Rebirth
-- Win Farm: Auto Win + Fly Speed slider
-- ========================

do -- === AUTO FARM (scoped) ===

    -- ── Farm state variables ──────────────────────────────────────
    local treadmillActive = false
    local treadmillThread = nil
    local rebirthActive   = false
    local rebirthThread   = nil
    local rbTierLabel_af  = nil  -- forward declare
    local rbLevelLabel_af = nil  -- forward declare
    local winFarmActive   = false
    local winFarmThread   = nil
    local winFlySpeed     = 6.7  -- default fly speed

    local REBIRTH_TIERS = {
        {level = 15},  {level = 25},  {level = 40},  {level = 60},
        {level = 75},  {level = 100}, {level = 125}, {level = 150},
        {level = 175}, {level = 200}, {level = 225}, {level = 250},
        {level = 300}, {level = 400}, {level = 500}, {level = 600},
        {level = 750}, {level = 1000}
    }

    -- ── Stop / Start helpers ──────────────────────────────────────
    local function stopRebirth_af()
        rebirthActive = false
        rebirthThread = nil
    end

    local function startRebirth_af()
        rebirthActive = true
        rebirthThread = task.spawn(function()
            local ClientState = require(ReplicatedStorage:WaitForChild("ClientState"))
            local RebirthRemote = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("Rebirth")
            while rebirthActive do
                local state = ClientState:Get()
                local currentRebirths = state.Rebirths or 0
                local currentLevel    = state.Level    or 0
                local nextTier = REBIRTH_TIERS[currentRebirths + 1]
                if rbTierLabel_af and rbLevelLabel_af then
                    rbTierLabel_af.Text = "Rebirth Tier: " .. (currentRebirths + 1) .. " / " .. #REBIRTH_TIERS
                    if nextTier then
                        rbLevelLabel_af.Text = "Level Req: " .. currentLevel .. " / " .. nextTier.level
                    else
                        rbLevelLabel_af.Text = "Level Req: MAX"
                    end
                end
                if nextTier and currentLevel >= nextTier.level then
                    RebirthRemote:FireServer()
                    task.wait(0.5)
                else
                    task.wait(1)
                end
            end
        end)
    end

    local function stopTreadmill_af()
        treadmillActive = false
        treadmillThread = nil
    end

    local function startTreadmill_af()
        treadmillActive = true
        treadmillThread = task.spawn(function()
            while treadmillActive do
                ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("UpdateSpeed"):FireServer("Treadmill")
                task.wait()
            end
        end)
    end

    -- ── Win Farm helpers ──────────────────────────────────────────
    local function isInsideHub_af(pos)
        local ok, result = pcall(function()
            local murSol = workspace.Structure.Stage0_HUB["Mur & Sol"]
            local cf, size = murSol:GetBoundingBox()
            local localPos = cf:PointToObjectSpace(pos)
            return math.abs(localPos.X) < size.X / 2
                and math.abs(localPos.Y) < size.Y / 2
                and math.abs(localPos.Z) < size.Z / 2
        end)
        if ok and result ~= nil then return result end
        return false
    end

    local function flyTo_af(targetPos)
        local char = LocalPlayer.Character
        if not char then return false end
        local root = char:FindFirstChild("HumanoidRootPart")
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if not root or not humanoid then return false end

        for _, v in ipairs(root:GetChildren()) do
            if v:IsA("BodyGyro") or v:IsA("BodyVelocity") then v:Destroy() end
        end

        humanoid.PlatformStand = true
        local BG = Instance.new("BodyGyro")
        BG.P = 9e4; BG.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
        BG.CFrame = root.CFrame; BG.Parent = root
        local BV = Instance.new("BodyVelocity")
        BV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        BV.Velocity = Vector3.new(0, 0, 0); BV.Parent = root

        local success = true
        while winFarmActive do
            if isInsideHub_af(root.Position) then success = false; break end
            local diff = targetPos - root.Position
            if diff.Magnitude < 3 then break end
            local dir = diff.Unit
            BV.Velocity = dir * (winFlySpeed * 50)
            BG.CFrame = CFrame.lookAt(root.Position, root.Position + dir)
            task.wait()
        end

        pcall(function() BV.Velocity = Vector3.new(0, 0, 0) end)
        pcall(function() BG:Destroy() end)
        pcall(function() BV:Destroy() end)
        pcall(function() humanoid.PlatformStand = false end)
        return success
    end

    local function stopWinFarm_af()
        winFarmActive = false
        winFarmThread = nil
        local char = LocalPlayer.Character
        if char then
            local root = char:FindFirstChild("HumanoidRootPart")
            if root then
                local bv = root:FindFirstChildOfClass("BodyVelocity")
                local bg = root:FindFirstChildOfClass("BodyGyro")
                if bv then bv:Destroy() end
                if bg then bg:Destroy() end
            end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum.PlatformStand = false end
        end
    end

    local function startWinFarm_af()
        winFarmActive = true
        winFarmThread = task.spawn(function()
            while winFarmActive do
                local char = LocalPlayer.Character
                if not char then task.wait(1) continue end
                local root = char:FindFirstChild("HumanoidRootPart")
                if not root then task.wait(1) continue end

                task.wait(0.3)
                root.CFrame = CFrame.new(2, -5, 2)
                task.wait(0.3)

                if isInsideHub_af(root.Position) then task.wait(1) continue end

                if not flyTo_af(Vector3.new(-8348, 500, 1416)) then
                    task.wait() continue
                end
                if not winFarmActive then break end

                char = LocalPlayer.Character
                root = char and char:FindFirstChild("HumanoidRootPart")
                if root then
                    local bv = root:FindFirstChildOfClass("BodyVelocity")
                    local bg = root:FindFirstChildOfClass("BodyGyro")
                    if bv then bv:Destroy() end
                    if bg then bg:Destroy() end
                    local hum = char:FindFirstChildOfClass("Humanoid")
                    if hum then hum.PlatformStand = false end
                    task.wait(0.3)
                end
                if not winFarmActive then break end

                local wb = workspace:FindFirstChild("Structure")
                    and workspace.Structure:FindFirstChild("Stage15")
                    and workspace.Structure.Stage15:FindFirstChild("WinBlock14")

                if wb and root then
                    local winsVal = LocalPlayer.leaderstats and LocalPlayer.leaderstats:FindFirstChild("Wins")
                    if winsVal then
                        local prevWins = winsVal.Value
                        local anticheated = false
                        while winFarmActive and winsVal.Value == prevWins do
                            char = LocalPlayer.Character
                            root = char and char:FindFirstChild("HumanoidRootPart")
                            if not root then task.wait(0.3) continue end
                            if isInsideHub_af(root.Position) then
                                anticheated = true; task.wait(1); break
                            end
                            root.CFrame = CFrame.new(wb.Position + Vector3.new(0, 3, 0))
                            task.wait(0.2)
                            root.CFrame = root.CFrame + Vector3.new(0, -1, 0)
                            task.wait(0.2)
                        end
                        if anticheated then continue end
                    else
                        root.CFrame = CFrame.new(wb.Position + Vector3.new(0, 3, 0))
                    end
                end

                if winFarmActive then task.wait(4) end
            end
        end)
    end

    -- Register cleanup ke luar scope supaya cleanupAll boleh panggil
    _G.VORTX_stopTreadmill = stopTreadmill_af
    _G.VORTX_stopRebirth   = stopRebirth_af
    _G.VORTX_stopWinFarm   = stopWinFarm_af

    -- ── Dropdown constants ────────────────────────────────────────
    -- (AF_DROP_CLOSED_H, AF_DROP_OPEN_H, WF_DROP_CLOSED_H, WF_DROP_OPEN_H,
    --  afDropOpen, wfDropOpen all declared at script level above)

    local tweenI_af = TweenInfo.new(0.15, Enum.EasingStyle.Quart)

    -- ── Speed Farm dropdown ───────────────────────────────────────
    afWrapper = Instance.new("Frame")
    afWrapper.Name = "AutoFarmDrop"
    afWrapper.Size = UDim2.new(1, -10, 0, AF_DROP_CLOSED_H)
    afWrapper.Position = UDim2.new(0, 5, 0, 5)
    afWrapper.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    afWrapper.BorderSizePixel = 0
    afWrapper.ClipsDescendants = true
    afWrapper.Parent = Tab3AFContent
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = afWrapper end

    afHeader = Instance.new("TextButton")
    afHeader.Size = UDim2.new(1, 0, 0, 24)
    afHeader.BackgroundTransparency = 1
    afHeader.BorderSizePixel = 0
    afHeader.Text = "Speed Farm"
    afHeader.TextColor3 = Color3.fromRGB(255, 255, 255)
    afHeader.TextSize = 10
    afHeader.Font = Enum.Font.GothamBold
    afHeader.TextXAlignment = Enum.TextXAlignment.Left
    afHeader.Parent = afWrapper
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,8); p.Parent = afHeader end

    afArrow = Instance.new("TextLabel")
    afArrow.Size = UDim2.new(1, -8, 0, 24)
    afArrow.BackgroundTransparency = 1
    afArrow.Text = "▶"
    afArrow.TextColor3 = Color3.fromRGB(255, 255, 255)
    afArrow.TextSize = 10
    afArrow.Font = Enum.Font.GothamBold
    afArrow.TextXAlignment = Enum.TextXAlignment.Right
    afArrow.ZIndex = 2
    afArrow.Parent = afWrapper

    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 24)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.8
        div.BorderSizePixel = 0
        div.Parent = afWrapper
    end

    -- Toggle: Auto Treadmill
    local afTreadRow = Instance.new("Frame")
    afTreadRow.Size = UDim2.new(1, 0, 0, 28)
    afTreadRow.Position = UDim2.new(0, 0, 0, 26)
    afTreadRow.BackgroundTransparency = 1
    afTreadRow.Parent = afWrapper

    local afTreadLabel = Instance.new("TextLabel")
    afTreadLabel.Size = UDim2.new(1, -50, 1, 0)
    afTreadLabel.Position = UDim2.new(0, 10, 0, 0)
    afTreadLabel.BackgroundTransparency = 1
    afTreadLabel.Text = "Auto Treadmill"
    afTreadLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    afTreadLabel.TextSize = 10
    afTreadLabel.Font = Enum.Font.Gotham
    afTreadLabel.TextXAlignment = Enum.TextXAlignment.Left
    afTreadLabel.Parent = afTreadRow

    local afTreadBtn = Instance.new("TextButton")
    afTreadBtn.Size = UDim2.new(0, 36, 0, 16)
    afTreadBtn.Position = UDim2.new(1, -44, 0.5, -8)
    afTreadBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    afTreadBtn.BorderSizePixel = 0
    afTreadBtn.Text = ""
    afTreadBtn.Parent = afTreadRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = afTreadBtn end

    local afTreadKnob = Instance.new("Frame")
    afTreadKnob.Size = UDim2.new(0, 12, 0, 12)
    afTreadKnob.Position = UDim2.new(0, 2, 0.5, -6)
    afTreadKnob.BackgroundColor3 = Color3.fromRGB(160, 160, 160)
    afTreadKnob.BorderSizePixel = 0
    afTreadKnob.ZIndex = 2
    afTreadKnob.Parent = afTreadBtn
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = afTreadKnob end

    local afTreadStatus = Instance.new("TextLabel")
    afTreadStatus.Size = UDim2.new(0, 30, 1, 0)
    afTreadStatus.Position = UDim2.new(1, -80, 0, 0)
    afTreadStatus.BackgroundTransparency = 1
    afTreadStatus.Text = "OFF"
    afTreadStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
    afTreadStatus.TextSize = 9
    afTreadStatus.Font = Enum.Font.GothamBold
    afTreadStatus.Parent = afTreadRow

    afTreadBtn.MouseButton1Click:Connect(function()
        if treadmillActive then
            stopTreadmill_af()
            TweenService:Create(afTreadBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
            TweenService:Create(afTreadKnob, tweenI_af, {Position = UDim2.new(0, 2, 0.5, -6), BackgroundColor3 = Color3.fromRGB(160, 160, 160)}):Play()
            afTreadStatus.Text = "OFF"; afTreadStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
        else
            startTreadmill_af()
            TweenService:Create(afTreadBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
            TweenService:Create(afTreadKnob, tweenI_af, {Position = UDim2.new(1, -14, 0.5, -6), BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
            afTreadStatus.Text = "ON"; afTreadStatus.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)

    -- Toggle: Auto Rebirth
    local afRbRow = Instance.new("Frame")
    afRbRow.Size = UDim2.new(1, 0, 0, 28)
    afRbRow.Position = UDim2.new(0, 0, 0, 57)
    afRbRow.BackgroundTransparency = 1
    afRbRow.Parent = afWrapper

    local afRbLabel = Instance.new("TextLabel")
    afRbLabel.Size = UDim2.new(1, -50, 1, 0)
    afRbLabel.Position = UDim2.new(0, 10, 0, 0)
    afRbLabel.BackgroundTransparency = 1
    afRbLabel.Text = "Auto Rebirth"
    afRbLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    afRbLabel.TextSize = 10
    afRbLabel.Font = Enum.Font.Gotham
    afRbLabel.TextXAlignment = Enum.TextXAlignment.Left
    afRbLabel.Parent = afRbRow

    local afRbBtn = Instance.new("TextButton")
    afRbBtn.Size = UDim2.new(0, 36, 0, 16)
    afRbBtn.Position = UDim2.new(1, -44, 0.5, -8)
    afRbBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    afRbBtn.BorderSizePixel = 0
    afRbBtn.Text = ""
    afRbBtn.Parent = afRbRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = afRbBtn end

    local afRbKnob = Instance.new("Frame")
    afRbKnob.Size = UDim2.new(0, 12, 0, 12)
    afRbKnob.Position = UDim2.new(0, 2, 0.5, -6)
    afRbKnob.BackgroundColor3 = Color3.fromRGB(160, 160, 160)
    afRbKnob.BorderSizePixel = 0
    afRbKnob.ZIndex = 2
    afRbKnob.Parent = afRbBtn
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = afRbKnob end

    local afRbStatus = Instance.new("TextLabel")
    afRbStatus.Size = UDim2.new(0, 30, 1, 0)
    afRbStatus.Position = UDim2.new(1, -80, 0, 0)
    afRbStatus.BackgroundTransparency = 1
    afRbStatus.Text = "OFF"
    afRbStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
    afRbStatus.TextSize = 9
    afRbStatus.Font = Enum.Font.GothamBold
    afRbStatus.Parent = afRbRow

    afRbBtn.MouseButton1Click:Connect(function()
        if rebirthActive then
            stopRebirth_af()
            TweenService:Create(afRbBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
            TweenService:Create(afRbKnob, tweenI_af, {Position = UDim2.new(0, 2, 0.5, -6), BackgroundColor3 = Color3.fromRGB(160, 160, 160)}):Play()
            afRbStatus.Text = "OFF"; afRbStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
        else
            startRebirth_af()
            TweenService:Create(afRbBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
            TweenService:Create(afRbKnob, tweenI_af, {Position = UDim2.new(1, -14, 0.5, -6), BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
            afRbStatus.Text = "ON"; afRbStatus.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)

    -- Rebirth status info row
    local afRbInfoRow = Instance.new("Frame")
    afRbInfoRow.Size = UDim2.new(1, 0, 0, 32)
    afRbInfoRow.Position = UDim2.new(0, 0, 0, 88)
    afRbInfoRow.BackgroundTransparency = 1
    afRbInfoRow.Parent = afWrapper

    rbTierLabel_af = Instance.new("TextLabel")
    rbTierLabel_af.Size = UDim2.new(1, -10, 0, 14)
    rbTierLabel_af.Position = UDim2.new(0, 10, 0, 2)
    rbTierLabel_af.BackgroundTransparency = 1
    rbTierLabel_af.Text = "Rebirth Tier: -"
    rbTierLabel_af.TextColor3 = Color3.fromRGB(160, 160, 160)
    rbTierLabel_af.TextSize = 9
    rbTierLabel_af.Font = Enum.Font.Gotham
    rbTierLabel_af.TextXAlignment = Enum.TextXAlignment.Left
    rbTierLabel_af.Parent = afRbInfoRow

    rbLevelLabel_af = Instance.new("TextLabel")
    rbLevelLabel_af.Size = UDim2.new(1, -10, 0, 14)
    rbLevelLabel_af.Position = UDim2.new(0, 10, 0, 16)
    rbLevelLabel_af.BackgroundTransparency = 1
    rbLevelLabel_af.Text = "Level Req: -"
    rbLevelLabel_af.TextColor3 = Color3.fromRGB(160, 160, 160)
    rbLevelLabel_af.TextSize = 9
    rbLevelLabel_af.Font = Enum.Font.Gotham
    rbLevelLabel_af.TextXAlignment = Enum.TextXAlignment.Left
    rbLevelLabel_af.Parent = afRbInfoRow

    -- ── Win Farm dropdown ─────────────────────────────────────────
    wfWrapper = Instance.new("Frame")
    wfWrapper.Name = "WinFarmDrop"
    wfWrapper.Size = UDim2.new(1, -10, 0, WF_DROP_CLOSED_H)
    wfWrapper.Position = UDim2.new(0, 5, 0, 5 + AF_DROP_CLOSED_H + 5)
    wfWrapper.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    wfWrapper.BorderSizePixel = 0
    wfWrapper.ClipsDescendants = true
    wfWrapper.Parent = Tab3AFContent
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = wfWrapper end

    wfHeader = Instance.new("TextButton")
    wfHeader.Size = UDim2.new(1, 0, 0, 24)
    wfHeader.BackgroundTransparency = 1
    wfHeader.BorderSizePixel = 0
    wfHeader.Text = "Win Farm"
    wfHeader.TextColor3 = Color3.fromRGB(255, 255, 255)
    wfHeader.TextSize = 10
    wfHeader.Font = Enum.Font.GothamBold
    wfHeader.TextXAlignment = Enum.TextXAlignment.Left
    wfHeader.Parent = wfWrapper
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,8); p.Parent = wfHeader end

    wfArrow = Instance.new("TextLabel")
    wfArrow.Size = UDim2.new(1, -8, 0, 24)
    wfArrow.BackgroundTransparency = 1
    wfArrow.Text = "▶"
    wfArrow.TextColor3 = Color3.fromRGB(255, 255, 255)
    wfArrow.TextSize = 10
    wfArrow.Font = Enum.Font.GothamBold
    wfArrow.TextXAlignment = Enum.TextXAlignment.Right
    wfArrow.ZIndex = 2
    wfArrow.Parent = wfWrapper

    do
        local div = Instance.new("Frame")
        div.Size = UDim2.new(1, 0, 0, 1)
        div.Position = UDim2.new(0, 0, 0, 24)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.8
        div.BorderSizePixel = 0
        div.Parent = wfWrapper
    end

    -- Toggle: Auto Win
    local wfToggleRow = Instance.new("Frame")
    wfToggleRow.Size = UDim2.new(1, 0, 0, 28)
    wfToggleRow.Position = UDim2.new(0, 0, 0, 26)
    wfToggleRow.BackgroundTransparency = 1
    wfToggleRow.Parent = wfWrapper

    local wfToggleLabel = Instance.new("TextLabel")
    wfToggleLabel.Size = UDim2.new(1, -50, 1, 0)
    wfToggleLabel.Position = UDim2.new(0, 10, 0, 0)
    wfToggleLabel.BackgroundTransparency = 1
    wfToggleLabel.Text = "Auto Win"
    wfToggleLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    wfToggleLabel.TextSize = 10
    wfToggleLabel.Font = Enum.Font.Gotham
    wfToggleLabel.TextXAlignment = Enum.TextXAlignment.Left
    wfToggleLabel.Parent = wfToggleRow

    local wfToggleBtn = Instance.new("TextButton")
    wfToggleBtn.Size = UDim2.new(0, 36, 0, 16)
    wfToggleBtn.Position = UDim2.new(1, -44, 0.5, -8)
    wfToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    wfToggleBtn.BorderSizePixel = 0
    wfToggleBtn.Text = ""
    wfToggleBtn.Parent = wfToggleRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wfToggleBtn end

    local wfKnob = Instance.new("Frame")
    wfKnob.Size = UDim2.new(0, 12, 0, 12)
    wfKnob.Position = UDim2.new(0, 2, 0.5, -6)
    wfKnob.BackgroundColor3 = Color3.fromRGB(160, 160, 160)
    wfKnob.BorderSizePixel = 0
    wfKnob.ZIndex = 2
    wfKnob.Parent = wfToggleBtn
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wfKnob end

    local wfStatus = Instance.new("TextLabel")
    wfStatus.Size = UDim2.new(0, 30, 1, 0)
    wfStatus.Position = UDim2.new(1, -80, 0, 0)
    wfStatus.BackgroundTransparency = 1
    wfStatus.Text = "OFF"
    wfStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
    wfStatus.TextSize = 9
    wfStatus.Font = Enum.Font.GothamBold
    wfStatus.Parent = wfToggleRow

    wfToggleBtn.MouseButton1Click:Connect(function()
        if winFarmActive then
            stopWinFarm_af()
            TweenService:Create(wfToggleBtn, tweenI_af, {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
            TweenService:Create(wfKnob,      tweenI_af, {Position = UDim2.new(0, 2, 0.5, -6), BackgroundColor3 = Color3.fromRGB(160, 160, 160)}):Play()
            wfStatus.Text = "OFF"; wfStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
        else
            startWinFarm_af()
            TweenService:Create(wfToggleBtn, tweenI_af, {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
            TweenService:Create(wfKnob,      tweenI_af, {Position = UDim2.new(1, -14, 0.5, -6), BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
            wfStatus.Text = "ON"; wfStatus.TextColor3 = Color3.fromRGB(255, 255, 255)
        end
    end)

    -- Slider: Fly Speed
    local wfSliderRow = Instance.new("Frame")
    wfSliderRow.Size = UDim2.new(1, 0, 0, 34)
    wfSliderRow.Position = UDim2.new(0, 0, 0, 57)
    wfSliderRow.BackgroundTransparency = 1
    wfSliderRow.Parent = wfWrapper

    local wfSliderLabel = Instance.new("TextLabel")
    wfSliderLabel.Size = UDim2.new(1, -10, 0, 14)
    wfSliderLabel.Position = UDim2.new(0, 10, 0, 0)
    wfSliderLabel.BackgroundTransparency = 1
    wfSliderLabel.Text = "Fly Speed: 6.7"
    wfSliderLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
    wfSliderLabel.TextSize = 9
    wfSliderLabel.Font = Enum.Font.Gotham
    wfSliderLabel.TextXAlignment = Enum.TextXAlignment.Left
    wfSliderLabel.Parent = wfSliderRow

    local wfSliderTrack = Instance.new("Frame")
    wfSliderTrack.Size = UDim2.new(1, -20, 0, 6)
    wfSliderTrack.Position = UDim2.new(0, 10, 0, 20)
    wfSliderTrack.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    wfSliderTrack.BorderSizePixel = 0
    wfSliderTrack.Parent = wfSliderRow
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wfSliderTrack end

    local wfSliderFill = Instance.new("Frame")
    wfSliderFill.Size = UDim2.new(0.617, 0, 1, 0)  -- default 6.7
    wfSliderFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    wfSliderFill.BorderSizePixel = 0
    wfSliderFill.Parent = wfSliderTrack
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wfSliderFill end

    local wfSliderThumb = Instance.new("Frame")
    wfSliderThumb.Size = UDim2.new(0, 12, 0, 12)
    wfSliderThumb.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    wfSliderThumb.BorderSizePixel = 0
    wfSliderThumb.ZIndex = 2
    wfSliderThumb.Parent = wfSliderTrack
    wfSliderThumb.Position = UDim2.new(0.617, -6, 0.5, -6)
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(1,0); c.Parent = wfSliderThumb end

    -- Slider drag logic
    local wfSliding = false
    wfSliderTrack.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or
           input.UserInputType == Enum.UserInputType.MouseButton1 then
            wfSliding = true
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or
           input.UserInputType == Enum.UserInputType.MouseButton1 then
            wfSliding = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if not wfSliding then return end
        if input.UserInputType ~= Enum.UserInputType.Touch and
           input.UserInputType ~= Enum.UserInputType.MouseMovement then return end
        local trackPos = wfSliderTrack.AbsolutePosition.X
        local trackW   = wfSliderTrack.AbsoluteSize.X
        local relX = math.clamp(input.Position.X - trackPos, 0, trackW)
        local pct  = relX / trackW
        winFlySpeed = math.round((1.1 + pct * 8.9) * 10) / 10
        wfSliderFill.Size  = UDim2.new(pct, 0, 1, 0)
        wfSliderThumb.Position = UDim2.new(pct, -6, 0.5, -6)
        wfSliderLabel.Text = "Fly Speed: " .. string.format("%.1f", winFlySpeed)
    end)

    -- ── Tab2 recalc positions ─────────────────────────────────────
    local function recalcTab3AFPositions()
        local tweenI = TweenInfo.new(0.2, Enum.EasingStyle.Quart)
        local curY = 5
        local afH = afDropOpen and AF_DROP_OPEN_H or AF_DROP_CLOSED_H
        TweenService:Create(afWrapper, tweenI, {
            Position = UDim2.new(0, 5, 0, curY), Size = UDim2.new(1, -10, 0, afH)
        }):Play()
        curY = curY + afH + 5
        local wfH = wfDropOpen and WF_DROP_OPEN_H or WF_DROP_CLOSED_H
        TweenService:Create(wfWrapper, tweenI, {
            Position = UDim2.new(0, 5, 0, curY), Size = UDim2.new(1, -10, 0, wfH)
        }):Play()
        updateScrollState()
    end

    afHeader.MouseButton1Click:Connect(function()
        afDropOpen = not afDropOpen
        afArrow.Text = afDropOpen and "▼" or "▶"
        recalcTab3AFPositions()
    end)

    wfHeader.MouseButton1Click:Connect(function()
        wfDropOpen = not wfDropOpen
        wfArrow.Text = wfDropOpen and "▼" or "▶"
        recalcTab3AFPositions()
    end)

    -- ── cfgRead / cfgApply entries for Save Config ────────────────
    cfgRead["af_tread"] = function() return {en = treadmillActive} end
    cfgApply["af_tread"] = function(t)
        local wantOn = (t.en == true)
        if treadmillActive ~= wantOn then
            if wantOn then
                startTreadmill_af()
                TweenService:Create(afTreadBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
                TweenService:Create(afTreadKnob, tweenI_af, {Position = UDim2.new(1, -14, 0.5, -6), BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
                afTreadStatus.Text = "ON"; afTreadStatus.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                stopTreadmill_af()
                TweenService:Create(afTreadBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
                TweenService:Create(afTreadKnob, tweenI_af, {Position = UDim2.new(0, 2, 0.5, -6), BackgroundColor3 = Color3.fromRGB(160, 160, 160)}):Play()
                afTreadStatus.Text = "OFF"; afTreadStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
            end
        end
    end

    cfgRead["af_rebirth"] = function() return {en = rebirthActive} end
    cfgApply["af_rebirth"] = function(t)
        local wantOn = (t.en == true)
        if rebirthActive ~= wantOn then
            if wantOn then
                startRebirth_af()
                TweenService:Create(afRbBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
                TweenService:Create(afRbKnob, tweenI_af, {Position = UDim2.new(1, -14, 0.5, -6), BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
                afRbStatus.Text = "ON"; afRbStatus.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                stopRebirth_af()
                TweenService:Create(afRbBtn,  tweenI_af, {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
                TweenService:Create(afRbKnob, tweenI_af, {Position = UDim2.new(0, 2, 0.5, -6), BackgroundColor3 = Color3.fromRGB(160, 160, 160)}):Play()
                afRbStatus.Text = "OFF"; afRbStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
            end
        end
    end

    cfgRead["af_winfarm"] = function() return {en = winFarmActive} end
    cfgApply["af_winfarm"] = function(t)
        local wantOn = (t.en == true)
        if winFarmActive ~= wantOn then
            if wantOn then
                startWinFarm_af()
                TweenService:Create(wfToggleBtn, tweenI_af, {BackgroundColor3 = Color3.fromRGB(255, 255, 255)}):Play()
                TweenService:Create(wfKnob,      tweenI_af, {Position = UDim2.new(1, -14, 0.5, -6), BackgroundColor3 = Color3.fromRGB(0, 0, 0)}):Play()
                wfStatus.Text = "ON"; wfStatus.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                stopWinFarm_af()
                TweenService:Create(wfToggleBtn, tweenI_af, {BackgroundColor3 = Color3.fromRGB(60, 60, 60)}):Play()
                TweenService:Create(wfKnob,      tweenI_af, {Position = UDim2.new(0, 2, 0.5, -6), BackgroundColor3 = Color3.fromRGB(160, 160, 160)}):Play()
                wfStatus.Text = "OFF"; wfStatus.TextColor3 = Color3.fromRGB(120, 120, 120)
            end
        end
    end

    cfgRead["af_flyspeed"] = function() return {v = winFlySpeed} end
    cfgApply["af_flyspeed"] = function(t)
        if type(t.v) == "number" then
            winFlySpeed = math.clamp(t.v, 1.1, 10.0)
            local pct = (winFlySpeed - 1.1) / 8.9
            wfSliderFill.Size      = UDim2.new(pct, 0, 1, 0)
            wfSliderThumb.Position = UDim2.new(pct, -6, 0.5, -6)
            wfSliderLabel.Text     = "Fly Speed: " .. string.format("%.1f", winFlySpeed)
        end
    end

end -- end AUTO FARM

-- ========================
-- SAVE CONFIG (Tab3)
-- File: vort_savecfg_<PlaceId>.json  (per game)
-- Saves all CheatHub toggle / slider / inputbox state + Auto Farm state
-- Excludes: Waypoints (separate file), Spectate target (not serialisable)
-- ========================

do -- === SAVE CONFIG ===

    local cfgFileName = "vort_savecfg_" .. tostring(game.PlaceId) .. ".json"
    local cfgData     = {}    -- { [configName] = stateTable }
    local cfgAutoName = ""    -- config to auto-load on next execute
    local cfgSelName  = ""    -- currently selected config name in list

    -- ── File I/O ──────────────────────────────────────────────────
    local function cfgLoadFile()
        pcall(function()
            if not readfile then return end
            local raw = readfile(cfgFileName)
            if not raw or raw == "" then return end
            local dec = HttpService:JSONDecode(raw)
            if type(dec) == "table" then
                cfgData     = type(dec.configs)  == "table"  and dec.configs  or {}
                cfgAutoName = type(dec.autoload) == "string" and dec.autoload or ""
            end
        end)
    end

    local function cfgSaveFile()
        pcall(function()
            if not writefile then return end
            writefile(cfgFileName, HttpService:JSONEncode({
                configs  = cfgData,
                autoload = cfgAutoName,
            }))
        end)
    end

    cfgLoadFile()

    -- ── State helpers ─────────────────────────────────────────────
    local function cfgGetState()
        local t = {}
        for k, fn in pairs(cfgRead) do
            t[k] = fn()
        end
        return t
    end

    local function cfgSetState(state)
        if type(state) ~= "table" then return end
        for k, fn in pairs(cfgApply) do
            if state[k] ~= nil then
                pcall(fn, state[k])
            end
        end
    end

    -- ── Config name inputbox (left) + Save button (right) ─────────
    -- Row y=8, height=22
    local cfgNameBox = Instance.new("TextBox")
    cfgNameBox.Size              = UDim2.new(1, -54, 0, 22)
    cfgNameBox.Position          = UDim2.new(0, 6, 0, 8)
    cfgNameBox.BackgroundColor3  = Color3.fromRGB(20, 20, 20)
    cfgNameBox.BorderSizePixel   = 0
    cfgNameBox.Text              = ""
    cfgNameBox.PlaceholderText   = "Config name..."
    cfgNameBox.TextColor3        = Color3.fromRGB(255, 255, 255)
    cfgNameBox.PlaceholderColor3 = Color3.fromRGB(80, 80, 80)
    cfgNameBox.TextSize          = 10
    cfgNameBox.Font              = Enum.Font.Gotham
    cfgNameBox.TextXAlignment    = Enum.TextXAlignment.Left
    cfgNameBox.ClearTextOnFocus  = false
    cfgNameBox.Parent            = Tab4ContentPanel
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = cfgNameBox end
    do local p = Instance.new("UIPadding"); p.PaddingLeft = UDim.new(0,6); p.Parent = cfgNameBox end

    local cfgSaveBtn = Instance.new("TextButton")
    cfgSaveBtn.Size              = UDim2.new(0, 44, 0, 22)
    cfgSaveBtn.Position          = UDim2.new(1, -50, 0, 8)
    cfgSaveBtn.BackgroundColor3  = Color3.fromRGB(30, 30, 30)
    cfgSaveBtn.BorderSizePixel   = 0
    cfgSaveBtn.Text              = "Save"
    cfgSaveBtn.TextColor3        = Color3.fromRGB(255, 255, 255)
    cfgSaveBtn.TextSize          = 10
    cfgSaveBtn.Font              = Enum.Font.GothamBold
    cfgSaveBtn.Parent            = Tab4ContentPanel
    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,4); c.Parent = cfgSaveBtn end
    do
        local s = Instance.new("UIStroke")
        s.Color = Color3.fromRGB(255, 255, 255)
        s.Thickness = 1
        s.Transparency = 0.7
        s.Parent = cfgSaveBtn
    end

    -- ── Load / Auto Load / Delete row (y=36, height=20) ───────────
    local btnRowFrame = Instance.new("Frame")
    btnRowFrame.Size              = UDim2.new(1, -12, 0, 20)
    btnRowFrame.Position          = UDim2.new(0, 6, 0, 36)
    btnRowFrame.BackgroundTransparency = 1
    btnRowFrame.BorderSizePixel   = 0
    btnRowFrame.Parent            = Tab4ContentPanel

    local cfgLoadBtn, cfgAutoLoadBtn, cfgDeleteBtn
    do
        local function mkCfgBtn(txt, ps, po)
            local b = Instance.new("TextButton")
            b.Size              = UDim2.new(1/3, -4, 1, 0)
            b.Position          = UDim2.new(ps, po, 0, 0)
            b.BackgroundColor3  = Color3.fromRGB(30, 30, 30)
            b.BorderSizePixel   = 0
            b.Text              = txt
            b.TextColor3        = Color3.fromRGB(255, 255, 255)
            b.TextSize          = 10
            b.Font              = Enum.Font.Gotham
            b.Parent            = btnRowFrame
            do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = b end
            do
                local s = Instance.new("UIStroke")
                s.Color = Color3.fromRGB(255, 255, 255)
                s.Thickness = 1
                s.Transparency = 0.8
                s.Parent = b
            end
            return b
        end
        cfgLoadBtn     = mkCfgBtn("Load",      0,   0)
        cfgAutoLoadBtn = mkCfgBtn("Auto Load", 1/3, 2)
        cfgDeleteBtn   = mkCfgBtn("Delete",    2/3, 4)
    end

    -- ── Thin divider (y=62) ───────────────────────────────────────
    do
        local div = Instance.new("Frame")
        div.Size             = UDim2.new(1, -12, 0, 1)
        div.Position         = UDim2.new(0, 6, 0, 62)
        div.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        div.BackgroundTransparency = 0.85
        div.BorderSizePixel  = 0
        div.Parent           = Tab4ContentPanel
    end

    -- ── Scrollable config list (y=66, fills rest of panel) ────────
    local cfgListScroll = Instance.new("ScrollingFrame")
    cfgListScroll.Size               = UDim2.new(1, -12, 1, -68)
    cfgListScroll.Position           = UDim2.new(0, 6, 0, 66)
    cfgListScroll.BackgroundTransparency = 1
    cfgListScroll.BorderSizePixel    = 0
    cfgListScroll.ScrollBarThickness = 4
    cfgListScroll.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
    cfgListScroll.CanvasSize         = UDim2.new(0, 0, 0, 0)
    cfgListScroll.AutomaticCanvasSize = Enum.AutomaticSize.None
    cfgListScroll.ElasticBehavior    = Enum.ElasticBehavior.Never
    cfgListScroll.Parent             = Tab4ContentPanel

    -- ── Render config list ────────────────────────────────────────
    local renderCfgList
    renderCfgList = function()
        for _, ch in ipairs(cfgListScroll:GetChildren()) do ch:Destroy() end
        local totalH = 0
        local names = {}
        for k in pairs(cfgData) do table.insert(names, k) end
        table.sort(names)
        for _, nm in ipairs(names) do
            do
                local isSel  = (nm == cfgSelName)
                local isAuto = (nm == cfgAutoName)

                local row = Instance.new("Frame")
                row.Size              = UDim2.new(1, 0, 0, 26)
                row.Position          = UDim2.new(0, 0, 0, totalH)
                row.BackgroundColor3  = Color3.fromRGB(40, 40, 40)
                row.BackgroundTransparency = isSel and 0 or 1
                row.BorderSizePixel   = 0
                row.Parent            = cfgListScroll
                if isSel then
                    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = row end
                end

                local nameLbl = Instance.new("TextLabel")
                nameLbl.Size              = UDim2.new(1, isAuto and -48 or -6, 1, 0)
                nameLbl.Position          = UDim2.new(0, 6, 0, 0)
                nameLbl.BackgroundTransparency = 1
                nameLbl.Text              = nm
                nameLbl.TextColor3        = Color3.fromRGB(255, 255, 255)
                nameLbl.TextSize          = 10
                nameLbl.Font              = isSel and Enum.Font.GothamBold or Enum.Font.Gotham
                nameLbl.TextXAlignment    = Enum.TextXAlignment.Left
                nameLbl.TextTruncate      = Enum.TextTruncate.AtEnd
                nameLbl.Parent            = row

                if isAuto then
                    local autoTag = Instance.new("TextLabel")
                    autoTag.Size              = UDim2.new(0, 38, 0, 16)
                    autoTag.Position          = UDim2.new(1, -42, 0.5, -8)
                    autoTag.BackgroundColor3  = Color3.fromRGB(55, 55, 55)
                    autoTag.BackgroundTransparency = 0
                    autoTag.Text              = "AUTO"
                    autoTag.TextColor3        = Color3.fromRGB(255, 255, 255)
                    autoTag.TextSize          = 8
                    autoTag.Font              = Enum.Font.GothamBold
                    autoTag.TextXAlignment    = Enum.TextXAlignment.Center
                    autoTag.Parent            = row
                    do local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0,3); c.Parent = autoTag end
                end

                -- invisible hitbox for click detection
                local hitbox = Instance.new("TextButton")
                hitbox.Size              = UDim2.new(1, 0, 1, 0)
                hitbox.BackgroundTransparency = 1
                hitbox.Text              = ""
                hitbox.Parent            = row

                local nmRef = nm
                hitbox.MouseButton1Click:Connect(function()
                    if cfgSelName == nmRef then
                        cfgSelName = ""
                    else
                        cfgSelName = nmRef
                        cfgNameBox.Text = nmRef
                    end
                    renderCfgList()
                end)
            end
            totalH = totalH + 26
        end
        cfgListScroll.CanvasSize         = UDim2.new(0, 0, 0, totalH)
        local visH = cfgListScroll.AbsoluteSize.Y
        cfgListScroll.ScrollBarThickness = (totalH > visH and visH > 0) and 4 or 0
    end

    -- ── Button logic ──────────────────────────────────────────────

    -- Save: captures current state; replaces existing config of same name
    cfgSaveBtn.MouseButton1Click:Connect(function()
        local nm = cfgNameBox.Text
        if nm == "" then return end
        cfgData[nm] = cfgGetState()
        cfgSaveFile()
        cfgSelName = nm
        renderCfgList()
    end)

    -- Load: apply selected config
    cfgLoadBtn.MouseButton1Click:Connect(function()
        if cfgSelName == "" or cfgData[cfgSelName] == nil then return end
        cfgSetState(cfgData[cfgSelName])
        cfgNameBox.Text = cfgSelName
    end)

    -- Auto Load: toggle auto-load for selected config (fires on next execute)
    cfgAutoLoadBtn.MouseButton1Click:Connect(function()
        if cfgSelName == "" then return end
        if cfgAutoName == cfgSelName then
            cfgAutoName = ""       -- toggle off
        else
            cfgAutoName = cfgSelName
        end
        cfgSaveFile()
        renderCfgList()
    end)

    -- Delete: remove selected config
    cfgDeleteBtn.MouseButton1Click:Connect(function()
        if cfgSelName == "" or cfgData[cfgSelName] == nil then return end
        cfgData[cfgSelName] = nil
        if cfgAutoName == cfgSelName then cfgAutoName = "" end
        cfgSaveFile()
        cfgSelName = ""
        cfgNameBox.Text = ""
        renderCfgList()
    end)

    -- ── Re-render list when Tab3 (Save Config) becomes active ────
    Tab3.MouseButton1Click:Connect(function()
        renderCfgList()
    end)

    -- ── Auto-load on script execute ───────────────────────────────
    if cfgAutoName ~= "" and cfgData[cfgAutoName] ~= nil then
        task.defer(function()
            cfgSetState(cfgData[cfgAutoName])
        end)
    end

end -- end SAVE CONFIG

-- ========================
-- DROPDOWN TOGGLE LOGIC
-- ========================

-- CheatHub LEFT recalc: Movement → Camera → Player → Misc
local function recalcCheatDropPositions()
    local tweenI   = TweenInfo.new(0.2, Enum.EasingStyle.Quart)
    local currentY = 5

    local movH = cheatDropState.movOpen and MOV_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(FlyWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,movH)
    }):Play()
    currentY = currentY + movH + DROP_GAP

    local camH = cheatDropState.camOpen and CAM_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(CameraWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,camH)
    }):Play()
    currentY = currentY + camH + DROP_GAP

    local playerH = cheatDropState.playerOpen and PLAYER_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(PlayerWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,playerH)
    }):Play()
    currentY = currentY + playerH + DROP_GAP

    local miscH = cheatDropState.miscOpen and MISC_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(MiscWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,miscH)
    }):Play()

    updateScrollState()
end

-- CheatHub RIGHT recalc: Teleport → Anti → Server
local function recalcRightDropPositions()
    local tweenI   = TweenInfo.new(0.2, Enum.EasingStyle.Quart)
    local currentY = 5

    local tpH = cheatDropState.tpOpen and EMPTY_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(TeleportWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,tpH)
    }):Play()
    currentY = currentY + tpH + DROP_GAP

    local antiH = cheatDropState.antiOpen and ANTI_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(AntiWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,antiH)
    }):Play()
    currentY = currentY + antiH + DROP_GAP

    local serverH = cheatDropState.serverOpen and SERVER_DROP_OPEN_H or DROP_CLOSED_H
    TweenService:Create(ServerWrapper, tweenI, {
        Position = UDim2.new(0,5,0,currentY), Size = UDim2.new(1,-10,0,serverH)
    }):Play()

    updateScrollState()
end

-- ── CheatHub LEFT headers ─────────────────────────────────────
FlyHeader.MouseButton1Click:Connect(function()
    cheatDropState.movOpen = not cheatDropState.movOpen
    FlyArrow.Text = cheatDropState.movOpen and "▼" or "▶"
    recalcCheatDropPositions()
end)
CamHeader.MouseButton1Click:Connect(function()
    cheatDropState.camOpen = not cheatDropState.camOpen
    CamArrow.Text = cheatDropState.camOpen and "▼" or "▶"
    recalcCheatDropPositions()
end)
PlayerHeader.MouseButton1Click:Connect(function()
    cheatDropState.playerOpen = not cheatDropState.playerOpen
    PlayerArrow.Text = cheatDropState.playerOpen and "▼" or "▶"
    recalcCheatDropPositions()
end)
MiscHeader.MouseButton1Click:Connect(function()
    cheatDropState.miscOpen = not cheatDropState.miscOpen
    MiscArrow.Text = cheatDropState.miscOpen and "▼" or "▶"
    recalcCheatDropPositions()
end)

-- ── CheatHub RIGHT headers ────────────────────────────────────
TeleportHeader.MouseButton1Click:Connect(function()
    cheatDropState.tpOpen = not cheatDropState.tpOpen
    TeleportArrow.Text = cheatDropState.tpOpen and "▼" or "▶"
    recalcRightDropPositions()
end)
AntiHeader.MouseButton1Click:Connect(function()
    cheatDropState.antiOpen = not cheatDropState.antiOpen
    AntiArrow.Text = cheatDropState.antiOpen and "▼" or "▶"
    recalcRightDropPositions()
end)
ServerHeader.MouseButton1Click:Connect(function()
    cheatDropState.serverOpen = not cheatDropState.serverOpen
    ServerArrow.Text = cheatDropState.serverOpen and "▼" or "▶"
    recalcRightDropPositions()
end)

-- ========================
-- SEARCH FILTER (CheatHub only)
-- ========================

local function applySearch(query)
    local q = query:lower()

    -- CheatHub LEFT panel
    do
        local items = {
            { FlyWrapper,    "movement" },
            { CameraWrapper, "camera"   },
            { PlayerWrapper, "player"   },
            { MiscWrapper,   "misc"     },
        }
        local curY = 5
        for _, it in ipairs(items) do
            local show = q == "" or it[2]:find(q, 1, true) ~= nil
            it[1].Visible = show
            if show then
                it[1].Position = UDim2.new(0, 5, 0, curY)
                curY = curY + it[1].Size.Y.Offset + DROP_GAP
            end
        end
    end

    -- CheatHub RIGHT panel
    do
        local items = {
            { TeleportWrapper, "teleport" },
            { AntiWrapper,     "anti"     },
            { ServerWrapper,   "server"   },
        }
        local curY = 5
        for _, it in ipairs(items) do
            local show = q == "" or it[2]:find(q, 1, true) ~= nil
            it[1].Visible = show
            if show then
                it[1].Position = UDim2.new(0, 5, 0, curY)
                curY = curY + it[1].Size.Y.Offset + DROP_GAP
            end
        end
    end

    updateScrollState()
end

-- ========================
-- INIT
-- ========================

MainFrame:GetPropertyChangedSignal("AbsoluteSize"):Connect(updateLayout)

SearchBar:GetPropertyChangedSignal("Text"):Connect(function()
    if SearchBarContainer and SearchBarContainer.Visible then
        applySearch(SearchBar.Text)
    end
end)

setActiveTab(Tab1)
Tab1.MouseButton1Click:Connect(function()
    setActiveTab(Tab1)
    task.defer(function()
        updateLayout()
        updateScrollState()
    end)
end)
Tab2.MouseButton1Click:Connect(function()
    setActiveTab(Tab2)
    task.defer(function()
        updateLayout()
        applySearch(SearchBar.Text)
        updateScrollState()
    end)
end)
Tab3.MouseButton1Click:Connect(function()
    setActiveTab(Tab3)
    task.defer(function()
        updateLayout()
    end)
end)

setupDrag(TopBar)
setupDrag(LeftPanel)
task.defer(updateLayout)
updateScrollState()

-- ============================================================
-- CLEANUP FUNCTION
-- Stop semua active farm threads + destroy GUI.
-- Dipanggil oleh: CloseBtn, atau execute baru (via _G guard).
-- ============================================================
local function cleanupAll()
    pcall(function() if _G.VORTX_stopTreadmill then _G.VORTX_stopTreadmill() end end)
    pcall(function() if _G.VORTX_stopRebirth   then _G.VORTX_stopRebirth()   end end)
    pcall(function() if _G.VORTX_stopWinFarm   then _G.VORTX_stopWinFarm()   end end)
    pcall(function() ScreenGui:Destroy() end)
    _G.UITemplate_Cleanup   = nil
    _G.VORTX_stopTreadmill  = nil
    _G.VORTX_stopRebirth    = nil
    _G.VORTX_stopWinFarm    = nil
end

-- Register ke _G — execute baru akan panggil ini dulu
_G.UITemplate_Cleanup = cleanupAll
